# Azure Infrastructure

Place Azure deployment templates, scripts, and configuration examples here.

Do not commit Azure credentials, tokens, subscription secrets, or generated credential files. Every Azure command must be recorded in `docs/run_log.md`.
