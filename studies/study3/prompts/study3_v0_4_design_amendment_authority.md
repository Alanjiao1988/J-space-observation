You are working in the existing GitHub repository Alanjiao1988/J-space-observation.

This authority permits exactly one model-free operator amendment round that produces Study 3 draft-v0.4. It accepts the valid second independent methods-review disposition against draft-v0.3, repairs the design choices specified below, validates the amended documents and publishes them by strict non-force fast-forward. It does not freeze Study 3 and does not authorize any bank, seed, tokenizer, model, GPU or empirical operation.

0. Required endpoint

The round may end only at:

STUDY3_INTERFACE_CALIBRATION_PROTOCOL_DRAFT_V0_4_COMPLETE_AWAITING_THIRD_INDEPENDENT_METHODS_REVIEW

The inherited review disposition is:

STUDY3_V0_3_METHODS_REVIEW_REJECTED_AMENDMENT_REQUIRED

The operator action is:

OPERATOR_AMENDMENT_ROUND_FOR_DRAFT_V0_4

This endpoint means only that an amended, unfrozen design exists for another independent review. It is not a preregistration, scientific result, protocol freeze, positive-reference selection, execution authority or evidence row.

The round must end with all of the following still true:

frozen = false

execution_authorized = false

bank_authorized = false

seed_authorized = false

model_operations_authorized = false

winner_selected = false

positive_reference_selected = false

confirmation_access_authorized = false

every empirical/model operation counter is exactly zero

the only successor is a fresh-session third independent methods review of draft-v0.4

Do not produce a freeze, P3-Q, bank, seed, Azure GPU, development, confirmation or mechanistic-execution prompt. Do not execute the next independent review in this drafting session.

1. Authority hierarchy and hard boundaries

This prompt is the sole authority for this amendment round. Repository documents describe prior state but do not enlarge this authority. The second independent review is authoritative evidence about defects in draft-v0.3; reviewer recommendations are not automatically adopted except where this authority makes an explicit operator choice.

The following historical states remain closed and immutable:

Study 1: INSUFFICIENT_BEHAVIORAL_SUPPORT_FOR_VALIDITY

Study 2 scientific state: STUDY2_PROTOCOL_V1_CLOSED_ON_DEVELOPMENT_FEASIBILITY

Study 2 documentation state: STUDY2_PROTOCOL_V1_TERMINAL_DOCUMENTATION_COMPLETE

evidence-ledger tail: EV-0016

Do not reopen, reinterpret or modify Study 1 or Study 2. Do not convert a design-review finding into empirical evidence or into a limitation of an executed measurement. The original research question remains unanswered.

1.1 Absolute zero-operation boundary

This round must perform exactly zero of every empirical or model operation:

model downloads: 0

immutable model-revision resolutions by downloading: 0

tokenizer constructions or tokenizations: 0

model-weight loads: 0

forward passes or decode steps: 0

sequence scorings: 0

generations: 0

activation extractions: 0

hooks, lenses, probes, patches and ablations: 0

model/provider API calls: 0

GPU jobs: 0

task-bank rows: 0

qualification-bank rows: 0

development-bank rows: 0

confirmation-bank rows or accesses: 0

random seeds, salts or beacons drawn, selected, revealed or consumed: 0

interface selections: 0

positive-reference selections: 0

result rows: 0

scientific evidence rows: 0

Phase 1.0D or RQ2/S4 operations: 0

Allowed computation is limited to committed-blob inspection, hashing, schema validation, deterministic model-free design arithmetic, exact-binomial arithmetic, exhaustive construction/support enumeration, static tests, repository tests and Git inspection.

Do not use GitHub Actions. Do not run local pytest or treat local decision-bearing statistical calculations as evidence. Final validation evidence must come from clean CPU-only Azure ACR jobs bound to exact commits. Local read-only inspection, document assembly, syntax checking and non-authoritative debugging are permitted only when precisely disclosed in the receipt and final handoff.

1.2 No hidden relaxation

The amendment may not obtain acceptance by:

weakening a null floor, alternative, alpha or claim without the explicit change in this authority;

treating not_applicable as pass;

pooling over a failed atomic cell;

relabelling a presentation-effect claim as robust correctness while retaining effect/invariance language elsewhere;

labelling per-cell power as family, profile, selection, confirmation or end-to-end power;

assuming independence between cells for a binding lower bound;

claiming exact-binomial validity without the registered sampling model in section 4.2;

using a deterministic complete-block enumeration as if it were an iid sample;

selecting or implicitly preferring a positive reference;

creating future execution authority.

2. Mandatory starting-state preflight

Start in a fresh Copilot CLI session/worktree based on fetched remote main. Branch and worktree names are non-authoritative metadata.

Before modifying any repository path, independently verify from committed blobs:

repository: Alanjiao1988/J-space-observation

expected published starting commit: bc98e5c98a2d4e273142c91497b7600ce751bade

expected starting tree: ab8798e802246d2f725c35daff2c0f61a77e31c2

reviewed draft-v0.3 commit: 2b36f5321d830ea6f70fff2b7bbca3cb93394046

reviewed draft-v0.3 tree: 98d71cb35cca7b55d8f96f131064a5b9654dd3c7

2b36f53…bc98e5c is a strict forward history with exactly 17 changed paths: 8 added and 9 modified, with no deletion, rename, copy or type change

review state: STUDY3_DRAFT_V0_3_SECOND_INDEPENDENT_METHODS_REVIEW_COMPLETE_AWAITING_OPERATOR_ACTION

review disposition: STUDY3_V0_3_METHODS_REVIEW_REJECTED_AMENDMENT_REQUIRED

new-review counts: 10 findings = 2 BLOCKING + 6 MAJOR + 2 MINOR

the structured new_findings array and finding headings support 2 BLOCKING + 6 MAJOR + 2 MINOR, while disposition_basis contains the inconsistent phrase Two BLOCKING and eight MAJOR methods defects remain; preserve both historical bytes, record this as a non-dispositive historical narrative-count mismatch and do not silently rewrite either source

inherited-review adjudication: 16 VERIFIED_RESOLVED and 4 PARTIALLY_RESOLVED

all 22 inherited unresolved items were adjudicated; only UR-22 remains UNRESOLVED_BLOCKING_OPERATOR_DECISION

current registry tails are the maximum IDs, not the last textual occurrences: D42, M-30, AR-0250

paper/evidence_ledger.csv is byte-identical to the reviewed start, is 25,241 bytes, contains the unchanged 16 evidence records and ends at EV-0016

both protected Phase 1.0D rollups and every registered Study 1/2 protected blob match their committed identities

every Study 3 empirical/model operation counter is zero; there is no bank, seed, winner, selected interface, selected RP or confirmation access

all eight Study 3 authority flags are false

HEAD, fetched origin/main, index and working tree are clean and content-identical at the start

Verify the second-review authority as a committed blob:

path: studies/study3/prompts/study3_v0_3_independent_methods_review_authority.md

bytes: 40907

logical lines: 959

LF count: 958

CR count: 0

trailing LF: false

SHA-256: d58ec53b29846d26e3f4a7f1f45b4b8625a4d59147c9cd5f687f4f86d6f20e62

Re-read the second-review Markdown, JSON, schema, receipt, independent recalculation and tables from the starting commit. Do not rely only on this authority's summary or on the drafting implementation.

If the remote moved, a protected digest differs, the worktree is dirty, the review object is inconsistent, or the starting state cannot be reconstructed exactly, stop with:

BLOCKED_ON_STUDY3_V0_4_AMENDMENT_STARTING_STATE_INTEGRITY

Do not merge, rebase, reset, cherry-pick, force-push or repair a protected historical object.

On success, record:

STUDY3_V0_4_AMENDMENT_STARTING_STATE_ACCEPTED_CONTENT_IDENTITY_BRANCH_METADATA_NONAUTHORITATIVE

3. Required reading and immutable review history

Read at minimum:

studies/study3/reviews/v0_3_independent_methods_review.md

studies/study3/reviews/v0_3_independent_methods_review.json

studies/study3/reviews/v0_3_independent_methods_review.schema.json

studies/study3/methods_review_receipt_v0_3.json

studies/study3/analysis/independent_methods_recalculation_v0_3.py

studies/study3/analysis/independent_methods_recalculation_tables_v0_3.json

studies/study3/prompts/study3_v0_3_independent_methods_review_authority.md

all three studies/study3/reviews/v0_3_operator_amendment.* files

studies/study3/design_receipt_v0_3.json

studies/study3/analysis/independent_methods_review_packet_v0_3.md

all current protocol, statistics, traceability, dossier and routing files under studies/study3/

tests/test_study3_design.py

tests/test_study3_methods_review.py

tests/test_study3_methods_review_v0_3.py

the current decision, methods, run and artifact registries

the v0.2 independent-review records needed to preserve the complete methods history

The following are immutable historical objects in this round and may not be edited:

every v0_3_independent_methods_review.* file

methods_review_receipt_v0_3.json

both independent_methods_recalculation_v0_3* files

study3_v0_3_independent_methods_review_authority.md

tests/test_study3_methods_review_v0_3.py

every v0_3_operator_amendment.* file

design_receipt_v0_3.json

independent_methods_review_packet_v0_3.md

study3_v0_3_design_amendment_authority.md

every v0.2/v0.1 review, authority, receipt, packet and recalculation artifact

tests/test_study3_methods_review.py

The current protocol, planning script/tables, traceability, methods sources, dossier and current design test are mutable because that is the purpose of this amendment. Git history preserves their reviewed draft-v0.3 bytes.

Both historical review test modules already read reviewed objects from explicit commits. Do not edit or weaken them. They must continue to pass after mutable current-draft files change.

4. Operator decisions adopted for draft-v0.4

These are design decisions, not measurements. They are proposed into draft-v0.4 and remain subject to the third independent methods review.

4.1 I3 is joint robust correctness, not presentation invariance

Adopt the narrow repair path for S3MR2-001. Do not introduce a new equivalence test, paired-effect test, non-inferiority margin, Tango procedure, discordance grid or any other presentation-effect family.

For each registered one-factor contrast cell, the I3 independent unit remains one base_item_contrast_cluster containing exactly two content-equivalent variants:

the registered baseline presentation for that contrast cell;

the registered one-factor transformed presentation.

Define the sole gate-bearing indicator as:

J_joint_correct = 1 if and only if both variants are scored correct against the same unique registered ground truth; otherwise 0.

The estimand is:

p_joint = Pr(J_joint_correct = 1) over the registered item-generating distribution for that atomic contrast cell.

The I3 null remains p_joint <= 9/10; the lowest alternative of interest remains p_joint = 97/100.

Make all of the following semantic changes consistently across JSON, Markdown, schema, packet, traceability, tests, tables, amendment record and routing text:

replace the construct pairwise presentation invariance and correctness with joint robust correctness across a registered presentation pair;

replace the I3 gate question with whether the joint-correctness probability under both registered presentations exceeds the registered floor;

replace VT6 with joint correctness across registered answer-position and label-permutation pairs;

replace VT7 with joint correctness across a small registered rendering-pair set;

state that failure localizes inadequate joint robust correctness for the named pair but does not identify the direction, magnitude or existence of a presentation effect;

state that pass means only that the registered joint-correctness floor was cleared for every applicable pairwise cell;

prohibit the terms invariance, equivalence, no presentation effect, presentation-effect size, stable, unaffected by presentation and semantic equivalents from every active claim, gate question, what_fails, validation-target interpretation and success statement;

permit those terms only in clearly labelled historical, retired-procedure, limitation-of-claim or prohibited-claim text;

narrow the Study 3 research question and maximum pass claim accordingly.

J_inv, J_cor and the old J_both may remain only as historical names or descriptive audit quantities with zero gate, selection, confirmation, ranking, rescue and claim authority. Prefer replacing the active primary name with J_joint_correct so the algebraic identity is explicit. If descriptive same-answer or marginal-correctness summaries are retained, label them DESCRIPTIVE_ONLY_NO_DECISION_AUTHORITY and enumerate every authority they do not carry.

The full 4×4 ordered outcome lattice over {correct, wrong_a, wrong_b, invalid} for the two variants must be published and tested. Exactly one lattice case family passes the primary indicator: both variants correct. Stable wrong, stable invalid, mixed correctness and two different wrong answers all fail.

The claim ceiling must be profile-specific:

S1: only the seven applicable K5 cells and two applicable K6 cells;

S2: only the two applicable K6 cells;

S3: only the two applicable K6 cells, subject to its separately registered current single-token applicability;

S4: descriptive only, never part of an interface-selection or confirmation claim.

Passing an applicable I3 cell supports only this statement:

Under the registered item-generating distribution for this named contrast cell, the probability that both the baseline and the named transformed presentation are scored correct exceeded the registered floor.

It supports no statement that the two marginal accuracies are equal or that the transformation has no effect.

4.2 Register the stochastic item-sampling model

Repair S3MR2-010 substantively. Preserve exact-binomial inference, but supply the sampling model that licenses it. No bank or seed is created in this round.

Add a normative sampling_frame_v0_4 section with an executable specification for every gate-bearing atomic cell. It must contain, at minimum:

the named finite or countable item-generating support;

the generator family/version identifier;

every sampled parameter and its support;

every exact-rational sampling weight, with weights summing to exactly one;

deterministic validity predicates and fail-closed rejection conditions;

the independent unit;

the split namespace;

the gate, profile-applicability, role, operation-family, depth and contrast-cell namespaces;

the draw-with-replacement rule;

the cross-role/profile item-reuse rule;

the cross-cell and cross-split non-reuse/partition rule;

the future seed-generation, commitment, derivation and audit procedure;

the estimand induced by the distribution;

the precise conditions under which exact-binomial inference is valid.

Define two levels explicitly so item reuse cannot be confused with the independent sampling unit:

a sampling cell is identified by split, gate/component, task stratum or operation-family×depth stratum, and contrast ID where applicable; it excludes interface profile and checkpoint role;

an evaluation cell is one sampling cell crossed with one applicable interface profile and one checkpoint role.

One iid item stream is drawn per sampling cell and is then reused across its applicable evaluation cells. Thus every evaluation cell has an iid Bernoulli sample marginally, while evaluation cells sharing the sampled items may be dependent. Separate sampling cells use disjoint namespaces.

The binding stochastic model is:

Within each gate-bearing atomic cell, base-item units or base-item contrast-cluster units are independent draws with replacement from that cell's registered generator distribution.

The deterministic model/scorer maps each independently drawn unit to one Bernoulli success indicator. All inferential randomness comes from the registered item draw, not from model sampling.

Duplicate generator tuples are legitimate iid draws and must be retained. Do not redraw for uniqueness, difficulty, balance, model response or any post-draw property. Record duplicate counts descriptively later; duplicates cannot be removed or rescued.

Deterministic validity rejection is permitted only inside the pre-registered generator specification and before any model operation. Rejection sampling must therefore define the conditional target distribution exactly; its acceptance predicate may not be changed after a seed exists.

Development, confirmation and future P3-Q supports are made physically disjoint by pre-registered outcome-blind namespace partitions of the generator key. The partition rule is frozen before any seed draw.

Distinct sampling cells use disjoint namespaces. The same sampled base-item identities are reused across target roles and across applicable interface profiles within a sampling cell for comparability, and S3 may reuse S2 logits under the registered single-token identity. This creates dependence among evaluation cells, which is expressly allowed and is handled by the arbitrary-dependence error and power bounds in section 4.3.

Operation family and depth remain explicit atomic-cell dimensions where already registered. Template family, nuisance condition and other generator parameters are independently sampled as part of the registered tuple with exact rational weights; do not assert independence merely because two prompts have different IDs.

For K5, replace deterministic complete-block bank assignment with a registered iid nuisance-condition distribution:

retain the exhaustive 32-state support generated by 4 correct positions × 4 correct displayed-symbol indices × 2 label alphabets;

prove the constructor maps each of the 32 support states correctly and each of the seven contrasts changes exactly one registered factor;

draw the baseline nuisance state independently and uniformly with probability 1/32 per state for every base-item contrast cluster;

do not force each consecutive group of 32 bank items to contain every state exactly once;

do not claim that n must be a multiple of 32 for statistical validity;

report realized state counts later as descriptive balance diagnostics with no gate or rescue authority.

The design-time constructor fixture may still enumerate all 32 states deterministically. Clearly distinguish exhaustive fixture/support enumeration from future stochastic bank sampling.

Future randomization, still unauthorized in this round, must use:

one first-draw-only 256-bit master seed per split from the Azure/OS cryptographic random source under a separate explicit seed authority;

an immediate committed hash/receipt for each seed before any bank materialization or model operation;

no redraw, seed shopping or substitution;

a registered domain-separation encoding over study, protocol version, split, atomic-cell ID and draw ordinal;

a deterministic cryptographic expansion/rejection-sampling algorithm whose implementation and test vectors are frozen before the first seed draw;

confirmation-seed isolation and inaccessibility until the separately authorized one-shot confirmation stage;

full later disclosure sufficient to regenerate the bank after the relevant seal permits it.

The exact seed value, generator implementation blob and realized bank remain null/empty and unauthorized in draft-v0.4. Null here means not yet selected, not zero. The protocol specification must nevertheless be complete enough that a later bank-construction authority implements it without choosing a sampling model.

If any atomic cell lacks a complete generator distribution or requires an adaptive post-draw choice, fail closed. Do not silently downgrade that cell to descriptive and do not claim exact-binomial validity for it.

4.3 Register family/profile/end-to-end power, not only per-cell power

Repair S3MR2-002 by adopting a binding end-to-end power design. Do not merely relabel the existing low joint operating characteristics as acceptable.

Keep the development type-I architecture:

study-level development false-qualification bound: exact rational 1/200;

fixed selectable-profile denominator: 3 for S1, S2 and S3, never data-dependently reduced;

per-profile/component development level: exact rational 1/600;

within a profile every applicable atomic cell is conjunctive; the intersection-union size is bounded by the component/cell level;

S4 is excluded from every success union.

Keep one-shot confirmation for exactly one development-selected profile at component/cell level 1/200 on a physically disjoint split.

Register the maximum selectable-profile cell count as m_max = 43, derived as follows:

Profile

I1a/I1b/I3 cells at p0=9/10

I2 cells at p0=1/2

I4 cells at p0=4/5

Total gate-bearing cells

S1

33

6

4

43

S2

9

6

4

19

S3

9

6

4

19

S4 has 39 diagnostic cells and no I4 cells, but is never selectable and is excluded from the binding power family.

Adopt these exact-rational type-II allocations:

per-stage profile false-negative budget: 19/400 = 0.0475;

per-cell false-negative budget: (19/400) / 43 = 19/17200;

per-cell power target: 1 - 19/17200 = 17181/17200 ≈ 0.998895348837;

development profile-eligibility power lower bound under arbitrary dependence: 1 - 43 × 19/17200 = 381/400 = 0.9525;

confirmation-conjunction power lower bound under arbitrary dependence: 381/400 = 0.9525;

panel false-qualification budget used in the end-to-end bound: 1/200 = 0.005;

end-to-end lower bound: 1 - 19/400 - 1/200 - 19/400 = 9/10.

The binding end-to-end power claim applies only under this fully stated least-favourable alternative configuration:

I0 passes;

at least one selectable profile has every applicable atomic-cell success probability at or above that cell's registered p1;

any higher-priority profile that is not adequate lies in its registered profile null, meaning at least one required cell is at or below its p0;

development and confirmation follow the frozen selection order with no rescue or substitution;

the selected adequate profile remains at or above p1 on the confirmation-generating distribution.

No independence among cells, profiles, roles or stages is used for the binding lower bound. The proof is a union bound over: failure of the designated adequate profile to qualify in development, false qualification of a higher-priority null profile, and failure of the selected adequate profile in confirmation. Any independence-based product may be reported only as a labelled sensitivity analysis with no authority.

Explicitly state that the power guarantee does not cover profiles in the indifference region between p0 and p1, distribution shift between development and confirmation, I0 failure, an invalid sampling frame or protocol deviations.

Re-derive exact-binomial sizes over every positive integer n; the old 32-block admissibility restriction is retired. Adopt the smallest development n meeting the new per-cell power target, and reuse that n conservatively for confirmation:

Gate family

p0

p1

Development alpha

n

Development pass count

Development power at p1

Confirmation alpha

Confirmation pass count

Confirmation power at p1

I1a / I1b / I3

9/10

97/100

1/600

413

389

0.999129439838

1/200

388

0.999609916012

I2

1/2

7/10

1/600

214

129

0.999042859186

1/200

127

0.999646587923

I4

4/5

9/10

1/600

448

383

0.999005509196

1/200

381

0.999626931069

The drafting implementation must derive, not copy, every threshold, tail, power, minimum n, cell count and joint bound. It must use exact rational inputs and integer binomial-tail arithmetic. The expected development null tails are, for independent cross-checking:

I1a/I1b/I3: 0.001664632930 at 389/413;

I2: 0.001597676081 at 129/214;

I4: 0.001620609599 at 383/448.

The expected confirmation null tails are:

I1a/I1b/I3: 0.003020762720 at 388/413;

I2: 0.003765544908 at 127/214;

I4: 0.003582895662 at 381/448.

Tests must prove each development n is the smallest unrestricted positive integer that reaches 17181/17200 at its registered p1 and alpha, and each pass count is the smallest count controlling its registered null. The derivation script may not contain the expected pass counts, powers or minimum sample sizes as reachable output literals. The confirmation sizes are intentionally inherited from development and need not be confirmation-minimal; label them conservative reuse, not minimal confirmation sizes.

Correct the power vocabulary everywhere:

target_power = 17181/17200 is explicitly per atomic cell;

profile_stage_power_floor = 381/400 is a union-bound lower bound under arbitrary cell dependence;

study_end_to_end_power_floor = 9/10 has the stated least-favourable configuration;

selection-return and confirmation operating characteristics are separately named;

a STOP outcome reports the realized registered gate outcome, not a proof that no population-level adequate interface exists.

4.4 I4, S4 and the unresolved positive reference

Repair S3MR2-003, S3MR2-004 and the operator input S3MR2-007 consistently:

I4 applies only to selectable S1, S2 and S3;

S4 is not_applicable for I4 in every protocol, truth-table, hierarchy, statistics, projection and confirmation field;

no confirmation applicability list may contain S4;

confirmation applicability is the intersection of the gate's registered selectable profiles with the single profile selected before confirmation;

I1b confirmation applies only to label-bearing selectable S1;

K5 confirmation applies only to label-bearing selectable S1;

S2/S3 K5 and I1b remain not_applicable, never pass.

OD2 remains UNRESOLVED_BLOCKING_OPERATOR_DECISION: select no checkpoint, revision, tokenizer, wrapper or candidate.

However, register this binding ordering constraint for the later OD2 authority:

P3Q one-sided competence lower bound >= 19/20 > I4 p1 = 9/10 > I4 p0 = 4/5.

The later OD2/P3-Q authority may choose a stronger P3-Q floor but not a weaker one. It must still freeze the checkpoint identity and immutable revision, a canonical qualification interface external to S1–S4, the RP-specific I4 wrapper, a physically isolated P3-Q bank and seed, exact n/alpha/rejection rule, operation-family/depth treatment, stop rule and provenance. None is chosen here.

The I4 claim remains narrow: an independently prequalified reference meeting the P3-Q lower-bound constraint retained competence above the registered I4 floor through the named candidate interface. It does not establish target-model competence and creates no mechanistic authority.

4.5 Total deterministic state machine

Repair S3MR2-006 by separating I0 from profile adequacy.

Publish one executable finite-state graph with exactly one legal next state for every event:

Q0_INSTRUMENT: run only deterministic/model-free I0 fixtures.

pass → Q1_DEVELOPMENT

fail, error or ambiguity → STOP_INSTRUMENT_DEFECT

Q1_DEVELOPMENT: evaluate every applicable atomic cell for every selectable profile under the registered development rules.

completed validly → Q2_SELECTION

protocol/integrity error → the separately registered fail-closed integrity stop; do not reinterpret it as a scientific gate failure

Q2_SELECTION: walk S2, then S3 if applicable, then S1; select the first eligible and applicable profile.

a profile selected → Q3_CONFIRMATION_PENDING_SEPARATE_AUTHORITY

none selected → STOP_NO_SELECTABLE_INTERFACE_REMAINS

Q3_CONFIRMATION may be entered only under a later explicit confirmation authority for that one preselected profile.

pass → CALIBRATED_PENDING_SEPARATE_SUBSTANTIVE_AUTHORITY

fail → STOP_CONFIRMATION_FAILED

error or ambiguity → STOP_CONFIRMATION_SPENT_ON_ERROR

The old 16-row profile-eligibility map may remain as the I0=PASS subtable, but it is not the whole state machine. Add the explicit I0!=PASS branch and make STOP_INSTRUMENT_DEFECT reachable. Prove every registered terminal/stop state is reachable from at least one valid input and no input has two next states.

STOP_INSTRUMENT_DEFECT must state that nothing was measured about any interface. STOP_NO_SELECTABLE_INTERFACE_REMAINS must state only that no candidate passed the registered development gates in that realized run. Neither may be promoted into a model-capability conclusion.

4.6 Operation ontology, S4 cost and unit repair

Repair S3MR2-005, S3MR2-008 and S3MR2-009 and recompute the entire projection from the amended sample sizes. These are planning quantities only and authorize nothing.

For the deterministic I0 fixture stream, register:

base_item_contrast_clusters = 232;

base_items = 232 for the cluster-derived fixture base items, not 464;

cluster_rendered_rows = 464;

noncluster_fixture_rows = 38;

rendered_rows = scored_rows = 502;

zero model operations.

Extend the unit registry and dimensional identities so every stream that reports clusters proves cluster_rendered_rows = clusters × variants_per_cluster, while non-cluster fixture rows are named separately.

With I1a/I1b/I3 n=413, I2 n=214 and I4 n=448, derive these target-role development quantities:

Profile/stream

Base items

Contrast clusters

Rendered/scored rows per target role

Target roles

Forward/logit evaluations

S1

1,254

3,717

8,688

3

26,064

S2

841

826

2,493

3

7,479

S3

same sampled items/logits as S2 in the current one-token domain

same as S2

0 incremental

3

0 incremental

target-role development total

—

—

—

—

33,543

The RP I4 development stream uses four operation-family×depth cells over two distinct scoring streams, S1 and S2; S3 reuses S2 under the current single-token identity. Its projection is:

448 × 4 × 2 = 3,584 rendered/scored rows and forward/logit evaluations.

The one-shot confirmation upper bound uses S1 as the most expensive selectable profile:

target-role rows: 26,064;

RP I4 rows: 448 × 4 = 1,792;

total rendered/scored rows and forward/logit evaluations: 27,856.

The S4 diagnostic generation stream uses the S1-shaped target bank and is never selectable:

generation calls: 26,064;

maximum new tokens per generation: 16;

generated-token upper bound: 417,024;

prompt-prefill sequence evaluations: 26,064;

incremental decode sequence evaluations upper bound: 390,960;

total unbatched sequence-level model-evaluation upper bound: 417,024.

Register distinct operation units for:

rendered rows;

scored rows;

restricted-vocabulary logit reads;

sequence-level prefill evaluations;

incremental decode evaluations;

total sequence-level model-evaluation equivalents;

generation calls;

generated tokens;

runtime batched forward calls.

Do not equate a sequence-level evaluation with a runtime batched call. The latter depends on future batch packing and remains a separately measured execution quantity. The planning table must nonetheless contain the non-null 417,024 sequence-evaluation upper bound; it may not leave S4 forward cost unmapped.

The P3-Q stream remains numerically null with the explicit status UNRESOLVED_BLOCKING_OPERATOR_DECISION_OD2. Do not publish a grand total that treats this null as zero.

Every operation count must be generated from a single structured source used by JSON, Markdown, packet and tests. No hand-copied divergent totals.

5. Required closure matrix

Create a v0.4 operator-amendment matrix that records each finding exactly once, preserves the reviewer's original text/severity and labels every repair PROPOSED_RESOLVED_SUBJECT_TO_THIRD_INDEPENDENT_METHODS_REVIEW rather than self-approving it.

At minimum, the matrix must show:

S3MR2-001 BLOCKING: I3 narrowed to joint robust correctness; all presentation-effect/invariance claims prohibited.

S3MR2-002 BLOCKING: binding arbitrary-dependence profile and end-to-end power targets, sizes and bounds registered.

S3MR2-003 MAJOR: S4 consistently I4-not-applicable and projection reconciled.

S3MR2-004 MAJOR: S4 removed from confirmation applicability; applicability intersection rule explicit.

S3MR2-005 MAJOR: S4 decode/prefill/sequence-evaluation ontology and upper bounds registered.

S3MR2-006 MAJOR: I0 separated; STOP_INSTRUMENT_DEFECT reachable and unique.

S3MR2-007 MAJOR operator input: P3-Q/I4 ordering constraint registered; OD2 still unresolved.

S3MR2-008 MINOR: I3 claim ceiling qualified by profile/applicable cells.

S3MR2-009 MINOR: I0 base-item and rendered-row units repaired.

S3MR2-010 MAJOR: iid-with-replacement generator sampling frame, seed lifecycle, clustering/reuse and split rules registered.

Also carry all 20 first-review findings and all UR-01 through UR-22 without erasing history. The four first-review findings labelled PARTIALLY_RESOLVED by the second reviewer may be proposed fully resolved only through the corresponding v0.4 repair and remain review-pending. UR-22 and OD2 remain unresolved blocking operator decisions. No method finding may be silently moved into OD2 to avoid repair.

The amendment record must separate:

reviewed fact;

exact deterministic derivation;

operator design choice;

reviewer recommendation adopted here;

unresolved operator choice;

future implementation requirement;

empirical result, of which there are none.

It must also record the historical review count mismatch exactly: the ten structured findings remain 2 BLOCKING + 6 MAJOR + 2 MINOR, while the immutable disposition-basis sentence says 2 BLOCKING + 8 MAJOR. This mismatch does not reverse the rejection because both blocking findings and the substantive sampling-model defect independently require amendment. Do not edit the review to repair it and do not propagate 8 MAJOR as the structured finding count.

6. Required artifacts and exact mutation ceiling

The changed path set must be exactly the following 25 paths: 6 added and 19 modified. No deletion, rename, copy or type change.

6.1 Six added paths

studies/study3/reviews/v0_4_operator_amendment.md

studies/study3/reviews/v0_4_operator_amendment.json

studies/study3/reviews/v0_4_operator_amendment.schema.json

studies/study3/analysis/independent_methods_review_packet_v0_4.md

studies/study3/prompts/study3_v0_4_design_amendment_authority.md

studies/study3/design_receipt_v0_4.json

6.2 Nineteen modified paths

studies/study3/README.md

studies/study3/RESEARCH_CHARTER_DRAFT.md

studies/study3/NEXT_THREAD_HANDOFF.md

studies/study3/protocol/interface_calibration_protocol_draft.md

studies/study3/protocol/interface_calibration_protocol_draft.json

studies/study3/protocol/interface_calibration_protocol.schema.json

studies/study3/analysis/design_statistics.py

studies/study3/analysis/design_statistics_tables.json

studies/study3/analysis/study2_to_study3_design_traceability.md

studies/study3/references/methods_sources.md

studies/study3/references/positive_reference_dossier.md

tests/test_study3_design.py

README.md

studies/README.md

reports/current_status.md

docs/decision_log.md

docs/run_log.md

paper/methods_ledger.md

paper/artifact_index.csv

Any need to touch a 26th path is a hard stop requiring new authority. Do not modify:

any Study 1 or Study 2 path;

paper/evidence_ledger.csv;

paper/limitations_ledger.md;

paper/claim_evidence_matrix.md;

any historical v0.1/v0.2/v0.3 review, amendment, authority, receipt, packet or recalculation object listed in section 3;

either historical methods-review test module;

dependencies, lockfiles, Dockerfiles, runtime/model source, infrastructure or workflows;

protected rollup inputs or frozen artifacts.

Commit this authority verbatim at the required prompt path. Preserve its source bytes exactly and report committed-blob bytes, LF/CR/trailing-newline properties and SHA-256. Do not add a wrapper, title, header or trailing commentary.

Inspect the registries and allocate only the next contiguous IDs after the verified maximum tails. Expected first IDs are D43, M-31 and AR-0251, but the fetched committed registries are authoritative. Register every new/amended artifact required by the existing artifact-index policy, with no gap or duplicate. Add no EV row and no limitations row.

The v0.4 receipt must bind every new/amended Study 3 core artifact by committed-blob bytes and SHA-256, record all zero counters and explicitly separate:

paths observed at the receipt commit;

prospectively declared tail paths such as the artifact index/run log if committed later;

the final independently observed changed-path set.

Do not claim that a receipt observed a future commit.

7. Committed validation requirements

7.1 Design, statistics and sampling-model tests

Update tests/test_study3_design.py so a clean ACR run proves at least:

JSON/schema/Markdown state parity for draft-v0.4;

no frozen, authorized or selected state;

exact zero operation counters, including rejection of any injected nonzero or unregistered counter key;

exactly two variants per applicable I3 pairwise cluster;

no K5×K6 cross-product and no active all-transformations cluster;

J_joint_correct truth over all 16 ordered outcome cases;

old J indicators, if retained, have descriptive-only status and no reachable decision path;

active VT6/VT7, gate question, what_fails and claim ceiling contain no presentation-effect, invariance or equivalence claim;

per-profile I3 applicability and claim ceiling are exact;

all nine I3 cells use disjoint cell namespaces and appropriate profile applicability;

K5's 32-state support is complete and every state has exact weight 1/32;

future K5 bank sampling is iid with replacement, not deterministic complete-block assignment;

every gate-bearing atomic cell has a complete sampling-frame entry;

all exact rational sampling weights sum to one and every validity predicate is pre-draw/model-free;

split partitions are disjoint and outcome-blind;

within-cell draws are with replacement and duplicate removal/redraw is prohibited;

cross-role/profile reuse and cross-cell dependence are stated explicitly;

actual seed values remain null, seed authority is false and no bank exists;

future seed procedure is first-draw-only, domain-separated and auditable;

exact-binomial assumptions match the registered item-generating estimand;

development alphas are 1/600; confirmation alphas are 1/200; denominator remains 3;

m_max=43 and S1/S2/S3 cell counts are derived from the truth table;

19/17200, 17181/17200, 381/400 and 9/10 reconstruct exactly;

the arbitrary-dependence union-bound proof contains no independence assumption;

all six development/confirmation threshold rows are derived from exact rational inputs;

development n=413,214,448 are smallest unrestricted positive integers meeting the registered per-cell power target;

pass counts 389,129,383 and 388,127,381 are minimal at their respective alphas;

no output target, pass count, tail, power or minimum n is copied as a reachable literal from this authority or a review table;

all power labels have correct per-cell/profile/selection/confirmation/end-to-end scope;

the least-favourable configuration and uncovered indifference region are explicit;

S4 is not I4-applicable, never selectable and absent from confirmation applicability;

I1b/K5 confirmation applicability is limited to S1;

OD2 remains unresolved; no checkpoint/model revision/tokenizer/wrapper is selected;

the P3-Q/I4 ordering constraint reconstructs 19/20 > 9/10 > 4/5;

I0 is a global precondition and its failure maps only to STOP_INSTRUMENT_DEFECT;

every registered state has exactly one legal next state and every terminal state is reachable;

I0 units reconstruct 232 clusters, 232 base items, 464 cluster rows, 38 non-cluster rows and 502 total rows;

revised development, RP-I4, confirmation and S4 operation totals derive from n and cell counts;

S4 derives 26,064 generation calls, 417,024 token/sequence-evaluation upper bound, 26,064 prefills and 390,960 incremental decode evaluations;

no grand total treats unresolved P3-Q as zero;

every S3MR2 finding, inherited S3MR finding and UR item appears exactly once in the amendment matrix;

no bank, seed, model result, evidence row or confirmation content exists.

Use negative mutations for every decision-bearing invariant. A test that only checks the committed happy path is insufficient.

design_statistics.py must derive its emitted tables from the protocol's exact rational parameters and structured cell/sampling registries. Its --check mode must recompute every output value-for-value. Include AST/static anti-transcription checks comparable in strength to the prior round.

7.2 Historical-review regression

Do not edit either historical review test. Run both:

tests/test_study3_methods_review.py for the v0.2 review;

tests/test_study3_methods_review_v0_3.py for the v0.3 review.

They must continue to validate their reviewed objects at the explicit historical commits. The amendment must not make either review pass by weakening assertions, changing expected findings or rewriting history.

Also run both immutable independent recalculation scripts in their committed --check modes where supported. Agreement with a historical drafting implementation is regression evidence only and is not third-review independence.

7.3 ACR-only validation envelope

Run from clean CPU-only python:3.11-bookworm ACR clones bound to exact commits. Require GPU_COUNT=0 and CUDA_AVAILABLE=False. Use one test path per ACR invocation; the known --set TESTS="a b" word-splitting behavior must never be used to claim that two modules ran.

At minimum run, separately:

tests/test_study3_design.py

tests/test_study3_methods_review.py

tests/test_study3_methods_review_v0_3.py

studies/study3/analysis/design_statistics.py --check

the v0.2 independent recalculation --check

the v0.3 independent recalculation --check

Study 2 Stage T focused regression

Study 2 Stage B-D focused regression

both protected Phase 1.0D byte modules, one path per invocation

a static committed-blob/path/digest audit of all 25 changed paths and every new registry row

the full repository suite

The registered baseline at bc98e5c is 3,886 passed, 15 skipped and the same two registered test_parser_v3_seal_job failures. The amended total must equal the baseline plus exactly the net new tests actually added to tests/test_study3_design.py; report the arithmetic. Only the same two historical failures with identical node IDs and failure signatures may remain. Any new failure, unexplained skip change, unbound run or GPU-visible validation is a stop.

Run the focused and full validation envelope on the actual final publication candidate commit, not only on its parent. If tail registries require a final metadata commit, repeat every decision-bearing validation whose bound tree changed or prove by an explicit immutable-input argument why the result remains bound; default to rerunning.

Disclose every failed ACR run and every operator-side defect. Distinguish invocation/environment failures from repository failures. Never claim that a commit was validated by a run bound to another commit.

Local core.autocrlf effects are non-authoritative. Hash and compare committed blobs, and validate in clean LF-preserving Linux clones. Do not edit scientific or review artifacts merely to make a Windows working-tree byte check pass.

8. Commit and publication discipline

Use small additive commits with auditable messages.

Preserve all review history; amend only the exact 25 paths.

Before each decision-bearing validation, require a clean worktree and bind the exact commit/tree.

Re-fetch remote main immediately before publication.

Require fetched remote main to remain exactly bc98e5c98a2d4e273142c91497b7600ce751bade and a strict ancestor of the candidate.

If remote moved, stop; do not merge, rebase, cherry-pick, reset or overwrite concurrent work.

Publish only with non-force explicit fast-forward refspec: git push origin HEAD:refs/heads/main.

Never force-push.

Post-push fetch and verify HEAD == origin/main, exact tree, clean worktree, exact 25-path set, committed artifact hashes, both protected rollups and unchanged EV-0016.

Do not use GitHub Actions. Do not create a PR or side branch as a substitute for the required fast-forward publication.

9. Required final handoff

Return a complete handoff headed exactly:

STUDY3_INTERFACE_CALIBRATION_PROTOCOL_DRAFT_V0_4_COMPLETE_AWAITING_THIRD_INDEPENDENT_METHODS_REVIEW

Include:

final commit and tree;

ancestry and non-force fast-forward proof;

exact 25-path list and 6-added/19-modified counts;

new decision/method/artifact IDs and full committed-blob sizes/SHA-256 values;

byte identity of the committed v0.4 authority;

starting-state and protected-byte proofs;

explicit statement that both independent reviews remain valid rejections and were not edited;

a 10-row S3MR2 closure summary, inherited 20-row S3MR status summary and all UR dispositions;

the preserved non-dispositive mismatch between the structured 2/6/2 finding counts and the immutable 2 BLOCKING and 8 MAJOR disposition-basis phrase;

the exact I3 joint-robust-correctness estimand and prohibited effect/invariance claims;

the complete registered sampling frame and future seed lifecycle, with no seed or bank created;

development and confirmation statistical tables with units, tails, exact power and minimality proofs;

exact derivation of the 43-cell maximum, 17181/17200 per-cell target, 381/400 stage floor and 9/10 end-to-end floor;

the least-favourable alternative configuration and all exclusions from the power guarantee;

explicit statement that OD2 remains unresolved and no RP was selected;

the P3-Q/I4 ordering constraint;

the total I0/development/selection/confirmation state machine;

the revised decomposed operation projection, S3 reuse and S4 prefill/decode accounting;

focused, historical-review, statistics, protected-byte, static-audit and full-suite ACR results bound to the final publication commit;

every failed invocation and corrected operator-side defect;

every empirical/model operation counter at zero;

Study 1/2 unchanged and the evidence ledger still ending at EV-0016;

frozen=false, execution_authorized=false, no bank, seed, winner, RP or confirmation access;

only legal next action: a fresh-session third independent methods review of draft-v0.4.

Do not execute the third review in the drafting session. Do not predeclare its disposition. Do not provide or execute any freeze, P3-Q, bank, seed, model, GPU, development, confirmation or mechanistic prompt.