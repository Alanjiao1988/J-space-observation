Continue work in the existing repository Alanjiao1988/J-space-observation.

Run this authority only in a fresh Copilot CLI session and fresh platform-managed worktree based on remote main. The reviewer must not be the session or drafting party that authored draft-v0.3. Do not coordinate with, consult, or report intermediate conclusions to the drafting session. Procedural separation alone is insufficient: all decision-bearing calculations and logical derivations must also be independently implemented.

Use the downloaded authority file directly. Do not execute from rendered, retyped, reformatted, or copy-pasted text. Before any repository mutation, preserve this source at a session-neutral path, compute its byte count, line-ending state, trailing-newline state and SHA-256, and later commit those exact bytes verbatim at the required authority path.

This is a bounded, CPU-only, model-free review of the already-published draft-v0.3. It is not an amendment, protocol freeze, preregistration, positive-reference selection, bank-construction, seed, tokenizer, model, GPU, development, confirmation, or mechanistic-execution round.

Use UTC for every timestamp. Do not create another repository. Do not use GitHub Actions.

The required documentation state at the end is exactly:

STUDY3_DRAFT_V0_3_SECOND_INDEPENDENT_METHODS_REVIEW_COMPLETE_AWAITING_OPERATOR_ACTION

Return exactly one disposition, earned by the review:

STUDY3_V0_3_METHODS_REVIEW_ACCEPTED_AS_SPECIFIED

STUDY3_V0_3_METHODS_REVIEW_ACCEPTED_WITH_REQUIRED_CHANGES

STUDY3_V0_3_METHODS_REVIEW_REJECTED_AMENDMENT_REQUIRED

This authority does not prescribe which disposition to return. Agreement with the drafting implementation, successful existing tests, or the fact that draft-v0.3 responds to all prior findings is not by itself validation.

Neither the documentation state nor any disposition is a scientific result or execution authority.

1. Authority hierarchy and hard boundaries

This file is the sole operator authority for this review round. Repository documents describe prior state but do not enlarge it.

Study 1 remains closed at INSUFFICIENT_BEHAVIORAL_SUPPORT_FOR_VALIDITY.

Study 2 remains closed at scientific state STUDY2_PROTOCOL_V1_CLOSED_ON_DEVELOPMENT_FEASIBILITY and documentation state STUDY2_PROTOCOL_V1_TERMINAL_DOCUMENTATION_COMPLETE.

The valid first independent review remains immutable historical evidence. Its disposition was STUDY3_METHODS_REVIEW_REJECTED_AMENDMENT_REQUIRED against draft-v0.2.

Draft-v0.3 remains an unfrozen design. No interface, winner, positive reference, bank, seed, confirmation split, result, or scientific evidence exists.

All 26 paths changed by the draft-v0.3 amendment are the review object. Do not repair any of them during this review, including either existing Study 3 test module. A reviewer who edits the reviewed object has ceased to perform an independent review.

The reviewer may create only the eight new review outputs and update only the nine routing and registry files whitelisted in section 10.

OD2 remains a blocking operator decision. Assess whether its boundary and required statistical fields are sufficient, but do not select, prefer, pin, rank, revision-resolve, download, tokenize, load, run, prequalify, or substitute any checkpoint.

paper/evidence_ledger.csv must remain byte-identical, have 16 rows, and end at EV-0016. No evidence row is permitted.

Do not modify paper/limitations_ledger.md or paper/claim_evidence_matrix.md. Defects in an unfrozen design are review findings, not limitations of executed measurement.

No Study 1 or Study 2 path, protected rollup input, dependency, lockfile, container definition, runtime source, infrastructure path, script path, or workflow may be modified.

Zero empirical operations are permitted: zero downloads, revision resolution by download, tokenizer construction, tokenization, weight load, forward pass, sequence scoring, generation, activation extraction, hooks, lenses, probes, patches, ablations, provider calls, GPU jobs, bank rows, bank accesses, seeds, interface selections, positive-reference selections, model gate evaluations, result rows, evidence rows, confirmation accesses, Phase 1.0D operations, or RQ2/Study 4 operations.

Allowed computation is committed-blob inspection, hashing, schema validation, deterministic design enumeration, exact arithmetic, independent statistical recalculation, static analysis, Git inspection, and CPU-only ACR validation.

Do not run local pytest or any decision-bearing statistical calculation locally. Final review evidence must come from clean CPU-only Azure ACR jobs bound to exact commits. Any local read-only inspection, syntax check, document assembly, or non-authoritative debugging must be disclosed precisely.

Branch and worktree names are non-authoritative metadata. Commit, tree, ancestry, clean state, protected bytes, independent implementation, and bound ACR results are authoritative.

No disposition from this round directly authorizes freeze, P3-Q, bank construction, seed draw, model execution, development, confirmation, or mechanistic work.

If a later instruction conflicts with a prohibition here, the prohibition wins.

2. Mandatory starting-state and authority preflight

Fetch remote main before any change and verify from committed blobs:

repository: Alanjiao1988/J-space-observation

expected origin/main and starting commit: 2b36f5321d830ea6f70fff2b7bbca3cb93394046

expected starting tree: 98d71cb35cca7b55d8f96f131064a5b9654dd3c7

parent review commit: e4bcda3a487ea9c9a085e3943103a07501014431

parent review tree: fa1246fb72232212e29fced7e37dbef971601cfc

reviewed draft-v0.2 commit: 8a2c4a0b2a73c5d802988333f11ea6c22828f6f5

draft-v0.3 is exactly 3 commits ahead of e4bcda3a...

git diff e4bcda3a...2b36f532... contains exactly 26 paths: 6 added and 20 modified, with no deletion, rename, copy, or type change

starting state: STUDY3_INTERFACE_CALIBRATION_PROTOCOL_DRAFT_V0_3_COMPLETE_AWAITING_SECOND_INDEPENDENT_METHODS_REVIEW

registry tails: D41, M-29, AR-0240

evidence ledger: 25,241 bytes, SHA-256 3821730c45b7a58d3c582b38ba354eae77558fa4d419a51e9ff4fdf120411ff1, 16 rows ending EV-0016

every experimental operation counter is zero

frozen, execution_authorized, bank_authorized, seed_authorized, model_operations_authorized, winner_selected, positive_reference_selected, and confirmation_access_authorized are all false

OD2 and UR-22 remain blocking operator decisions; no positive reference is selected

both Phase 1.0D protected rollups and every Study 1/2 protected blob match their registered identities

HEAD, fetched origin/main, index, worktree, and untracked-path state are clean and content-identical

At minimum verify these draft-v0.3 committed blobs from Git, never from CRLF-converted working-tree bytes:

Path

Bytes

SHA-256

studies/study3/analysis/independent_methods_review_packet_v0_3.md

29040

62016c0f0512b616b5342e0a3be0e578dbeb4c97086b91e3618745e525aaa397

studies/study3/design_receipt_v0_3.json

26342

9067313a671a318fbedc75b6c486bb55015dbb337d09690f73fddbd573dc9e27

studies/study3/prompts/study3_v0_3_design_amendment_authority.md

34682

de85aeff25e827e49d3e7c60d517b50cc69649d66190995a804ab2bc44308667

studies/study3/reviews/v0_3_operator_amendment.md

50720

7d0014ff111974f13dca683e0cebf62f60a6b52e8f40c78bcfb90b3ebd2f7f96

studies/study3/reviews/v0_3_operator_amendment.json

46502

29a1b33dde48d6969edea49fcc73e56003d5ebc24233883728a295b9ec265271

studies/study3/reviews/v0_3_operator_amendment.schema.json

22608

a7411718527397fb8ea5177032cdcea1274e0fc6a2591b6bb052c50ecdae0edf

studies/study3/analysis/design_statistics.py

69226

8e279bbbd7e7322c8d823dc807bcdbc5d6a80c4e3f7e4a9385dd37e9b7eae4c5

studies/study3/analysis/design_statistics_tables.json

53100

a185b0145707b59d8c0a7da6438fc2f718f59175d37fdf40b4d38e98c79035c2

studies/study3/protocol/interface_calibration_protocol_draft.json

206696

db2d51cce9971e916b3a02a5da0bb1a1e6a1c271bb79162e3c8515836db60a09

studies/study3/protocol/interface_calibration_protocol_draft.md

97393

670968eac4ec78f45e6eaaa270a4666957df5c7127dc7e4d7dfe4e71dac41633

studies/study3/protocol/interface_calibration_protocol.schema.json

140007

1f15d2434133f24dfd7b1add908c9f5904b83e9317d17cc51c13c430743f4f89

studies/study3/analysis/study2_to_study3_design_traceability.md

21049

17037a5ca04354db3bdd489d89653056f99960942e7979fbf8a3ecfba7aee620

studies/study3/references/methods_sources.md

20916

7efa77c33c3b8dae406efe4aa3f8b57c644fa4242140d72c1a3ff2528d37ef9a

studies/study3/references/positive_reference_dossier.md

11734

f300a4cbad9d809b0d5e878e9de43ddd0eb85bdb6737b0a8a3bbbcc650323e24

tests/test_study3_design.py

93590

4b7f831878b2c58e98be13db44af560c4d8a2b31301a168e47c68c4b1250a633

tests/test_study3_methods_review.py

60781

331d2a7644ee3256d7a145fa8ba83d0b02dcfd1faa1ed8989b726c1c656509ba

Also verify the first-review authority remains 34,624 bytes, SHA-256 ec207bb595490417078ec904c71f6bb1fda2035006dded8488a2f9071dad4968, LF-only, with no trailing newline, and that every first-review output is byte-identical to e4bcda3a....

Verify the source authority for this round before touching the repository. Record its observed absolute path, bytes, LF/CR counts, trailing-newline state and SHA-256. The source must be the downloaded file, not a rendered representation. Later verify the committed authority blob is byte-identical to that source.

If remote main moved, any required blob differs, the worktree is dirty, the authority source cannot be verified, the protected state differs, or the 26-path review object cannot be reconstructed exactly, stop at:

BLOCKED_ON_STUDY3_V0_3_SECOND_METHODS_REVIEW_STARTING_STATE_INTEGRITY

Do not reset, merge, rebase, cherry-pick, force-push, repair, or replay around a discrepancy.

On success record:

STUDY3_V0_3_SECOND_METHODS_REVIEW_STARTING_STATE_ACCEPTED_CONTENT_IDENTITY_BRANCH_METADATA_NONAUTHORITATIVE

3. Required reading and immutable review object

Read in full before writing a calculation or disposition:

root README.md, studies/README.md, and reports/current_status.md

studies/study3/README.md, RESEARCH_CHARTER_DRAFT.md, and NEXT_THREAD_HANDOFF.md

all three current protocol files

analysis/independent_methods_review_packet_v0_3.md

analysis/design_statistics.py and analysis/design_statistics_tables.json

analysis/study2_to_study3_design_traceability.md

references/methods_sources.md and references/positive_reference_dossier.md

all three reviews/v0_3_operator_amendment.* files

design_receipt_v0_3.json

the committed draft-v0.3 amendment authority

both existing Study 3 test modules

the complete first independent review, receipt, independent recalculation and tables

the exact draft-v0.2 packet and reviewed bytes at 8a2c4a0b...

current decision, run, methods, artifact, evidence, limitations, and claim-evidence registries

Study 2 terminal handoff, Stage B-D interpretation erratum, and post-hoc interface diagnostic only to verify provenance and non-reuse boundaries

The 26 draft-v0.3 amendment paths and every prior review artifact are immutable during this review. Read them; do not edit them. Study 1/2 measurements and post-hoc diagnostics are not pilot data for choosing Study 3 parameters.

4. Independence protocol

Create a new implementation at:

studies/study3/analysis/independent_methods_recalculation_v0_3.py

It must not import, execute, dynamically load, copy functions or constants from, or derive its control flow from either:

studies/study3/analysis/design_statistics.py

studies/study3/analysis/independent_methods_recalculation.py

Requirements:

Derive formulas and algorithms independently from the protocol hypotheses and English-language primary statistical sources.

Use independently named and structured functions.

Do not inspect drafting-table numeric outputs until the independent parameter extraction and calculations have been committed in a candidate review implementation. Record that ordering.

Provide emit and --check modes, deterministic canonical output, fail-closed parameter validation, and no network, model, tokenizer, bank, result, or prior-evidence access.

Recompute exact-binomial thresholds and power from exact rational inputs, never decimal policy renderings.

Independently enumerate the I3 outcome truth table, K5/K6 construction laws, the full development selection map, cell counts, and all operation projections.

Independently derive family-level and profile-level power implications; do not copy per-cell power and label it overall power.

Include at least one closed-form identity, exhaustive small-case enumeration, or published-example check for every statistical family. Agreement only with drafting bytes is not validation.

Only after independent values exist, compare them field-by-field with the drafting tables and classify every difference.

Treat every number as a proposed design parameter, not an observation or model result.

Use primary sources at minimum:

Clopper and Pearson (1934), Biometrika 26(4):404-413, DOI 10.1093/biomet/26.4.404, for exact binomial limits and the binomial sampling model.

Berger and Hsu (1996), Statistical Science 11(4):283-319, DOI 10.1214/ss/1032280304, for intersection-union logic.

Tango (1998), PMID 9595618, Hsueh, Liu and Chen (2001), PMID 11414572, and Liu et al. (2002), PMID 11782062, only to verify that the previously defective paired procedure is genuinely retired from decision authority.

Additional sources must also be primary literature or official model documentation. Source agreement does not replace checking whether the protocol's actual assumptions satisfy the cited method.

5. Mandatory inherited-repair audit

The review JSON must contain exactly one adjudication for each S3MR-001 through S3MR-020 and exactly one for each UR-01 through UR-22.

Allowed inherited-item statuses:

VERIFIED_RESOLVED

PARTIALLY_RESOLVED

NOT_RESOLVED

CORRECTLY_RETAINED_AS_BLOCKING_OPERATOR_DECISION

The last status is valid only for the intentionally unresolved OD2/UR-22 boundary and is not acceptance of a missing methods definition that draft-v0.3 claims to settle.

For each inherited item, cite exact artifact paths and field names, state whether the repair is substantive or cosmetic, and explain whether it closes the original defect without creating a new one. Do not accept the amendment record's PROPOSED_RESOLVED... label as proof.

At minimum re-adjudicate:

the I3 unit and identifiable denominator;

stable-wrong and stable-invalid scoring;

implementation of exact rational 1/600 in every development component;

withdrawal of the false Tango conservativeness assertion;

absence of the finite discordance grid from all decision paths;

removal of active p0 = 0.95;

one unambiguous null and unit in every statistical row;

non-degenerate rejection regions;

exactly seven one-factor K5 cells and two disjoint K6 cells;

no K5 × K6 cross product;

fixed denominator 3 under every selection-map state;

complete I5 lifecycle;

correct tail names and dossier references;

complete operation decomposition.

Any inherited blocking defect that is not actually resolved prohibits acceptance as specified.

6. Mandatory new audit targets

Treat every question below as a candidate issue to adjudicate independently. The authority does not prescribe a favorable or unfavorable answer.

6.1 I3 estimand and construct validity

Verify the independent unit is exactly one base_item_contrast_cluster containing two variants in every applicable cell.

Re-derive J_inv, J_cor, and J_both for every possible pair of valid, invalid, correct, and incorrect outputs.

Verify stable-wrong, stable-invalid, mixed correct/wrong, and mixed valid/invalid cases all fail the primary gate.

The protocol defines J_cor = 1 only when both variants are correct and records that, under unique ground truth, J_cor implies J_inv. Determine whether J_both is therefore mathematically identical to “both variants correct.”

If it is identical, decide whether the permitted claim is a joint robust-correctness floor or a presentation-invariance/equivalence claim. A redundant conjunction may be legible but must not imply evidence the statistic does not identify.

Analyze explicit counterexamples, including a baseline accuracy of 1.00 with transformed accuracy 0.91 and nested failures, and a model with stable high joint correctness but a material presentation decrement. State whether such processes can pass the registered p0 = 0.90 gate and what claim ceiling follows.

Decide whether retiring all paired effect/equivalence inference leaves the stated scientific construct adequately measured by Pr(J_both=1), or whether the protocol still uses language that requires a presentation-effect estimand.

Verify K5 cells change exactly one registered factor, K6 holds the cue and all non-varied bytes fixed, and complete-block assignment cannot alias item difficulty with contrast condition.

Verify disjoint base-item identities across contrast cells and splits. Determine whether items are reused across roles or profiles and whether that affects any claimed sampling or power calculation.

Verify not_applicable never becomes a pass, zero effect, denominator element, multiplicity relaxation, or evidence of robustness.

6.2 Exact-binomial model and sampling frame

For I1a, I1b, I2, I3, and I4, separately for development and confirmation:

Recompute p0, p1, exact alpha, n, pass count, exact null tail, and power using integer/Fraction arithmetic.

Verify the reported thresholds: development I1a/I1b/I3 244 of 256, I2 82 of 128, I4 224 of 256; confirmation I1a/I1b/I3 243 of 256, I2 80 of 128, I4 222 of 256.

Search the full admissible n grid implied by each balancing rule. Report the smallest admissible n meeting each registered per-cell target and whether 256/128 is justified.

Explain the substantive basis for each p0 and p1. A value is not justified because it makes a preferred n pass.

State the stochastic model that makes an exact binomial test valid: sampling frame, independent unit, common-p or justified worst-case interpretation, with/without-replacement rule, item-generation clusters, and deterministic versus stochastic model outputs.

Draft-v0.3 says no random draw occurred in the design round. Determine what later bank/seed authority must do to make the registered binomial superpopulation claim valid. If the future bank is a fixed finite census or clustered deterministic set, determine whether binomial inference remains justified or whether a finite-population, stratified, clustered, or descriptive interpretation is required.

Identify every place repeated templates, shared base items, paired variants, roles, profiles, or operation families could violate the assumed independent-unit model.

6.3 Type-I multiplicity and executable selection

Write formal null and alternative sets for:

every atomic cell;

the conjunction of applicable cells within one profile;

the union over the three selectable profiles;

development selection;

the one selected confirmation profile;

I4 across operation family and depth;

S4 diagnostics, which have no selection authority.

Then adjudicate:

whether intersection-union logic actually matches the profile claim;

whether per-component development alpha 1/600 bounds a false-qualified profile at 1/600 without an additional within-profile correction;

whether the union over the fixed three-profile denominator is bounded by 1/200 under arbitrary dependence;

whether retaining denominator 3 when S3 is inactive is pre-data and conservative rather than data-dependent;

whether the 16-row selection map is total, deterministic, and consistent with order S2 → S3 → S1;

whether S3 is impossible to select under the current single-token domain and cannot become selectable without a new multi-token authority;

whether one-shot independent confirmation at component alpha 1/200 needs no across-profile correction after exactly one profile is selected on a physically disjoint development split;

whether every selection, stop, tie, inapplicability, and error state has exactly one legal next state;

whether any descriptive Clopper-Pearson band, label diagnostic, pooled statistic, J_inv, J_cor, or S4 observation can rescue a failed gate or influence selection.

Return a machine-executable decision graph independently authored by the reviewer.

6.4 Family-level and profile-level power

The draft reports target power 9/10 and verifies it separately for each atomic cell. It does not visibly establish that an entire eligible profile or the one-shot confirmation conjunction has power 0.90.

The reviewer must:

Count the exact number of gate-bearing statistical cells per profile, role, family, depth, contrast ID, and split.

Distinguish per-cell power, gate-family power, profile-eligibility power, probability that the selection map returns a winner, and full confirmation power.

Derive valid bounds under arbitrary dependence and illustrative values under explicitly stated independence assumptions. Do not assume independence merely because base-item identities are disjoint.

Independently check the illustrative calculation implied by the current table: S1 development contains 33 cells at power about 0.953040775, 6 at about 0.938986365, and 4 I4 cells at about 0.921083515; S2 contains 9, 6, and 4 respectively. Under independence these are approximately 0.101 and 0.320 joint pass probabilities. Confirmation analogues are approximately 0.331 and 0.588. These numbers are candidate checks, not prescribed conclusions.

Determine whether the protocol ever labels per-cell 0.90 as overall design power. If so, adjudicate the misstatement.

Decide whether low or unidentified joint power is a blocking feasibility defect, a required claim/label correction, or an accepted operating characteristic. State the rationale.

If an overall power target is scientifically required, return a bounded recommendation or explain why a new amendment must choose one. Do not silently inflate n or weaken gates.

6.5 Tango retirement and descriptive remnants

Search every decision-bearing JSON field, schema constraint, Markdown rule, packet, table, router, test, and handoff for any active paired aggregate-equivalence role.

Confirm there is no margin, critical value, p-value, null, pass/fail, eligibility, selection, confirmation, rescue, ranking, or claim path based on Tango or the four-point discordance grid.

Confirm the false conservativeness statement is withdrawn, not merely reworded.

Confirm historical first-review recalculation bytes remain immutable and are clearly historical.

Determine whether purely descriptive 2×2 summaries can leak into interpretation or selection despite their declared status.

6.6 I4, S4, and OD2

Verify the I4 floor p0 0.80, p1 0.90, n, alpha, four operation-family × depth cells, and conjunction logic.

Decide whether these parameters support the narrow claim that an already independently qualified RP works through a candidate interface.

Verify P3-Q qualification and I4 use physically disjoint items, interfaces, decisions, and authority, with no circular qualification.

The exact-binomial tables list I4 as applicable to S1, S2, S3, and S4, while projected RP_I4_under_candidate_profiles accounts only S1, S2, and S3 and S4 is never selectable. Determine whether S4 I4 is actually applicable, diagnostic-only, or erroneously omitted from accounting. This must receive an explicit cross-artifact adjudication.

Confirm OD2 can remain unresolved without making the generic I4 method internally undefined. List exactly what a later OD2 authority must freeze: checkpoint identity/revision, canonical qualification interface, wrappers, bank isolation, p0, p1, alpha, n, rejection rule, family/depth treatment, stopping rule, and provenance.

Do not select a checkpoint.

6.7 I5 confirmation lifecycle

Verify I5 covers I0, I1a, I1b, I2, I3_J_both, and I4 in every applicable atomic cell for one preselected profile.

Verify confirmation cannot be accessed before separate authority, is physically disjoint, is read once, is spent on error or ambiguity, and cannot be retried, retuned, rescued, or used to select another profile.

Verify confirmation thresholds and exact alpha independently.

Evaluate full-conjunction confirmation power as required by section 6.4.

Confirm a pass supports only the registered interface-calibration claim and creates no mechanistic authority.

6.8 Operation accounting and feasibility

Recompute every work stream from primitive cell counts and units rather than copying the drafting totals:

deterministic I0 fixtures;

target-role development;

external P3-Q;

RP I4 under candidate profiles;

selected-profile one-shot confirmation;

S4 diagnostic generation.

At minimum adjudicate:

20,736 target-role development scored rows/forward passes;

2,048 RP/I4 rows/forward passes for the currently accounted candidate profiles;

17,152 as an explicitly labelled S1 upper bound for one selected-profile confirmation, not a universal exact total;

16,128 S4 generations and the 258,048 generated-token upper bound at 16 tokens per row;

whether S4 can leave forward_passes = null while claiming a usable feasibility projection, and how autoregressive decoding steps map to the repository's operation ontology;

whether external P3-Q quantities are correctly null, rather than zero, while OD2 is unresolved;

whether S3 profile adds zero incremental forward passes and zero incremental scoring rows only under the current jointly single-token answer domain, identical prefix, identical logits, and CPU-only rescoring contract;

whether any document incorrectly says “Study 3 adds zero forward passes” when the intended subject is interface profile S3;

whether future multi-token S3 activation necessarily requires a new scoring contract, cost table, and authority;

whether totals distinguish rendered rows, scored rows, logit reads, model forward passes, generation calls, and generated tokens consistently.

The result is planning arithmetic only and cannot authorize spending or execution.

6.9 Cross-artifact consistency and claim ceiling

Compare authoritative JSON, Markdown, schema, tables, packet, amendment record, receipt, charter, READMEs, status, handoff, tests, methods ledger, and decision log.

Every candidate inconsistency must receive exactly one status:

CONFIRMED_BLOCKING

CONFIRMED_MAJOR

CONFIRMED_MINOR

CONFIRMED_NONBLOCKING

NOT_CONFIRMED

QUALIFIED

Bind evidence paths and exact field names. At minimum cover:

J_both semantics and claim language;

per-cell versus joint power labels;

S4 I4 applicability versus accounting;

S4 forward-pass null versus token/generation bounds;

S3 profile versus Study 3 wording;

sample-frame/binomial assumptions;

all 20 inherited repair claims;

all 22 UR dispositions;

OD2 blocking state;

zero-operation and no-evidence boundaries.

7. Required review outputs

Create exactly these eight new paths and no other new path:

studies/study3/reviews/v0_3_independent_methods_review.md

studies/study3/reviews/v0_3_independent_methods_review.json

studies/study3/reviews/v0_3_independent_methods_review.schema.json

studies/study3/analysis/independent_methods_recalculation_v0_3.py

studies/study3/analysis/independent_methods_recalculation_tables_v0_3.json

tests/test_study3_methods_review_v0_3.py

studies/study3/prompts/study3_v0_3_independent_methods_review_authority.md

studies/study3/methods_review_receipt_v0_3.json

The committed authority must be byte-identical to the downloaded source used to start this round. Do not add a header, footer, wrapper, commentary, normalization, or change to its trailing-newline state.

7.1 Human-readable review

The Markdown review must:

bind starting commit/tree and every core reviewed blob;

document procedural and computational independence;

adjudicate all 20 inherited findings and all 22 UR items exactly once;

answer every section 6 audit target;

assign new findings stable IDs S3MR2-001, S3MR2-002, and so on, with severity BLOCKING, MAJOR, or MINOR;

provide the exact-binomial recalculation table;

provide per-cell, family, profile, selection, and confirmation power tables or justified bounds;

provide the executable multiplicity/selection graph;

provide the independently derived operation table;

state all cross-artifact candidate statuses;

state OD2 and every other operator-decision status;

distinguish reviewed fact, reviewer inference, recommendation, and unresolved operator choice;

return exactly one permitted disposition and its legal successor action;

repeat the claim ceiling, unfrozen state, and all-zero operation boundary.

7.2 Machine-readable review and schema

The JSON must include at least:

schema version, state, reviewed commit/tree, reviewed artifact identities;

source-authority identity and committed-authority identity;

reviewer independence declaration and prohibited-source audit;

disposition and disposition basis;

exactly 20 inherited-finding adjudications;

exactly 22 UR adjudications;

mandatory audit answers;

new findings with stable IDs, severity, evidence paths, fields, and consequences;

cross-artifact candidate adjudications;

estimand and claim-ceiling decisions;

binomial sampling-model decision;

exact-binomial recalculation;

multiplicity and selection graph;

per-cell and joint-power decisions;

Tango-retirement decision;

I4/S4/OD2 decision;

confirmation decision;

projected operation accounting;

unresolved methods items and unresolved operator items kept separate;

zero operation counts and false authority flags;

next legal action.

The schema must reject:

unknown disposition or state;

missing, duplicate, or extra inherited IDs;

missing mandatory audit answer;

finding without severity, evidence path, field, rationale, or consequence;

binding number without unit and authority status;

a claim that per-cell power is profile-level power without derivation;

selected interface or positive reference;

any non-zero operation counter or true execution/freeze flag;

any bank, seed, result, model output, confirmation access, or evidence row;

acceptance as specified with an unresolved blocking/major methods item;

acceptance as specified with any inherited BLOCKING item not VERIFIED_RESOLVED;

misuse of CORRECTLY_RETAINED_AS_BLOCKING_OPERATOR_DECISION outside OD2/UR-22;

a successor action that jumps directly to freeze or execution.

7.3 Independent recalculation table

The committed table must contain:

exact rational inputs, thresholds, null tails, and power for every gate and split;

full admissible-n searches and balancing divisors;

drafting-table comparison after independent derivation;

I3 truth-table and construction enumeration;

cell counts by profile and role;

per-cell, gate-family, profile, selection, and confirmation power calculations or rigorous bounds;

multiplicity decision graph and all 16 selection-map states;

Tango-decision-path absence audit;

operation projections derived from primitive counts;

a prominent PROPOSED_DESIGN_PARAMETERS_NOT_MEASUREMENTS status.

7.4 New committed test

tests/test_study3_methods_review_v0_3.py must:

validate the JSON against the new schema without adding a dependency;

run the independent recalculation in check mode;

prove by AST/static audit that neither drafting statistics nor the first review recalculation is imported, executed, dynamically loaded, or copied through reachable source literals;

bind all reviewed blobs to the starting commit;

enforce exact inherited ID sets and new finding parity;

enforce disposition fail-closed rules;

enforce separation of methods blockers from OD2;

enforce zero counters, false authority flags, no model/RP/interface selection, and no evidence row;

enforce Markdown/JSON parity for disposition, finding IDs, inherited statuses, OD2, state, and next action;

enforce source/committed authority byte identity;

include negative mutations for every fail-closed rule;

accept the unmutated committed review.

Do not modify either pre-existing Study 3 test module to make the new review pass. Record any defect they expose.

8. Disposition rules

Return STUDY3_V0_3_METHODS_REVIEW_ACCEPTED_AS_SPECIFIED only if:

all inherited blocking defects are verified resolved;

all mandatory audit questions are answered;

no blocking or major methods defect remains;

I3's estimand supports the exact claim language used;

the binomial sampling model and later bank requirements are executable without substantive amendment;

multiplicity, selection, and confirmation are mathematically valid and implemented;

per-cell versus joint power is correctly specified and the resulting operating characteristics are acceptable under the registered claim;

operation accounting is complete enough for the next operator decision;

only OD2 remains as the deliberately open non-method operator decision.

OD2 remaining open does not by itself prohibit acceptance as specified, but acceptance does not resolve OD2.

Return STUDY3_V0_3_METHODS_REVIEW_ACCEPTED_WITH_REQUIRED_CHANGES only if every required change is bounded and non-discretionary and does not alter the estimand, p0/p1, sample size, atomic cells, applicability, multiplicity architecture, selection order, confirmation lifecycle, operation ontology, or claim ceiling. List exact edits suitable for a later conformance amendment and subsequent bounded conformance check.

Return STUDY3_V0_3_METHODS_REVIEW_REJECTED_AMENDMENT_REQUIRED if any valid repair requires a substantive choice or redesign, including a changed estimand, new presentation-effect test, changed thresholds/sample sizes, new family-level power target, changed sampling frame, altered applicability, changed multiplicity/selection, unresolved confirmation design, or material operation-accounting redesign; or if a decision-bearing method cannot be independently validated.

Unanswered, contradictory, or unidentifiable methods questions are never acceptance. A human/JSON conflict is a validation failure, not grounds to choose the friendlier text.

Legal successor actions:

accepted as specified: OPERATOR_REVIEW_ADOPTION_AND_OD2_RESOLUTION_ROUND; no freeze or execution;

accepted with required changes: OPERATOR_CONFORMANCE_AMENDMENT_FOR_DRAFT_V0_3_1; then a bounded conformance review; no freeze;

rejected: OPERATOR_AMENDMENT_ROUND_FOR_DRAFT_V0_4; then another independent review; no freeze.

Do not create any successor prompt in this round.

9. Registries and artifact IDs

Register:

decision D42 for the second independent review disposition;

method M-30 for the independent v0.3 recalculation and review method;

artifacts AR-0241 through AR-0250, contiguous and in this exact order:

review Markdown;

review JSON;

review schema;

independent recalculation script;

independent recalculation table;

committed v0.3 review test;

authority copy;

review receipt;

updated studies/study3/README.md;

updated studies/study3/NEXT_THREAD_HANDOFF.md.

Use exact committed-blob bytes and SHA-256 values. Classify every artifact as methods review, review method, validation, routing, or provenance, never scientific result. Do not alter D41, M-29, AR-0222 through AR-0240, any earlier registry row, or EV-0016.

The receipt must bind starting and final commit/tree, reviewed blobs, source/committed authority identity, independent implementation identity, disposition, every new artifact except its own self-hash in its body, path whitelist and observed path set, ACR evidence, protected bytes, evidence-ledger identity, ancestry, zero counters, false authority flags, and remaining legal authority.

10. Exact mutation ceiling

Eight added paths are exactly the section 7 outputs.

The only existing paths that may be modified are:

README.md

studies/README.md

studies/study3/README.md

studies/study3/NEXT_THREAD_HANDOFF.md

reports/current_status.md

docs/decision_log.md

docs/run_log.md

paper/methods_ledger.md

paper/artifact_index.csv

The hard ceiling is exactly 17 changed paths: 8 added plus at most 9 modified. An 18th path is a hard stop.

Routing edits must state only the completed review state, actual disposition, reviewed commit, claim ceiling, and legal next action. They may not rewrite draft-v0.3.

Do not modify any protocol, schema, packet, drafting calculation/table, amendment record/receipt/authority, existing test, source dossier, traceability file, prior review, evidence/limitations/claim ledger, Study 1/2 file, dependency, infrastructure, script, container, runtime, lockfile, or workflow.

11. Commit and ACR validation sequence

Use intentional commits that keep independent calculation/review distinct from registry/publication bookkeeping. Do not squash away that distinction.

All decision evidence must run in clean CPU-only Azure ACR using python:3.11-bookworm or the exact registered CPU validation image, from an exact-commit clean clone. One test path per ACR invocation. Report BOUND_COMMIT, BOUND_TREE, DIRTY=0, Python version, CPU-only/zero-GPU status, exact command, exit code, and complete pass/skip/failure counts.

At minimum run on the final publication candidate:

tests/test_study3_methods_review_v0_3.py;

tests/test_study3_design.py;

tests/test_study3_methods_review.py;

independent_methods_recalculation_v0_3.py --check;

design_statistics.py --check only as regression of immutable reviewed bytes;

Study 2 Stage T and Stage B-D focused tests;

both Phase 1.0D protected-byte modules;

the full repository suite;

a static audit of exact 17-path maximum, authority identity, reviewed-object identity, registry contiguity, evidence ledger, protected bytes, no forbidden import/source use, zero operations, and false authority flags.

The starting full-suite envelope is 3,851 passed, 15 skipped, and exactly 2 registered historical test_parser_v3_seal_job failures. Reconcile every added passing test. No new failure, error, unexplained skip change, or unbound run is acceptable.

If a final run-log or artifact-index commit is added after validation, rerun at least the new review test, both existing Study 3 tests, independent recalculation check, registry/static audit, protected-byte checks, and clean-state check on the actual publication commit. Never claim a commit was validated by a run bound only to its parent.

Any validation helper carrying decision authority must be committed within the eight-path output design or expressed in the committed test/recalculation. Supplemental operator-side checks may be ephemeral only if fully disclosed and no conclusion depends on them.

12. Publication discipline

Before push:

fetch origin;

verify origin/main still equals 2b36f5321d830ea6f70fff2b7bbca3cb93394046;

verify the candidate is its strict descendant;

verify worktree/index/untracked state clean after the final commit;

verify the complete diff is inside the 17-path ceiling;

verify every reviewed and protected byte, registry, counter, and authority flag.

Publish only by non-force fast-forward:

git push origin HEAD:refs/heads/main

Never force-push. If remote main moves, stop; do not merge, rebase, or replay without a new operator decision.

After push verify HEAD equals origin/main, final tree, ancestry, clean state, all new blob identities, artifact contiguity through AR-0250, D42, M-30, unchanged EV-0016, protected rollups, immutable 26-path review object, zero operations, and false authority flags.

13. Required final handoff

Lead with the exact disposition and documentation state. Report:

starting and final commit/tree;

ancestry and fast-forward proof;

exact added and modified paths;

reviewed-object and source/committed authority identities;

independence procedure and recalculation identities;

all 20 inherited-finding and 22 UR adjudications;

every new BLOCKING and MAJOR finding;

exact adjudication of I3 estimand/claim, sampling model, thresholds, multiplicity, joint power, Tango retirement, I4/S4/OD2, I5, S3 reuse, S4 forward accounting, and cross-artifact consistency;

parameter, joint-power, selection, and operation tables;

D42, M-30, and AR-0241 through AR-0250;

all bound ACR runs and full-suite reconciliation;

evidence-ledger and protected-rollup identities;

every operation counter, all zero;

the only legal successor action under the actual disposition.

State explicitly:

Study 1 remains closed.

Study 2 remains closed.

Study 3 remains unfrozen.

No interface or positive reference is selected.

No bank, seed, model operation, gate result, confirmation access, or evidence row exists.

The original research question remains unanswered.

No freeze or execution authority was created.

Stop after publishing and reporting the review. Do not continue into amendment, OD2 resolution, freeze, P3-Q, bank construction, Azure GPU work, development, confirmation, or mechanistic execution.