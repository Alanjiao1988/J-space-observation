GitHub Copilot authority — Study 3 draft-v0.5 bounded operator amendment after third independent rejection

This file is the sole operator authority for one fresh amendment session. Execute it from its original downloaded bytes. Do not execute from rendered, retyped, reformatted, summarized, or copy-pasted text.

## 0. Required endpoint

Starting from the published third-review commit, produce and publish a **draft-v0.5 design candidate** that closes all ten `S3MR3-*` findings and no more than the bounded scope below permits.

Required terminal disposition after successful publication:

`STUDY3_DRAFT_V0_5_BOUNDED_AMENDMENT_PUBLISHED_AWAITING_FOURTH_INDEPENDENT_METHODS_REVIEW`

This is a design-amendment round only. It must not freeze Study 3, authorize execution, select an interface, select or inspect a positive reference, resolve `OD2` or `UR-22`, create a seed or bank, run a tokenizer or model, access confirmation material, write scientific evidence, begin the fourth review, create draft-v0.6, or start a feasibility pilot.

The only legal successor after successful publication is a **fresh-session fourth independent methods review of published draft-v0.5 by a party that did not draft it**.

## 1. Starting state and authority hierarchy

### 1.1 Exact published start

Require all of the following before editing:

- repository: `Alanjiao1988/J-space-observation`;
- branch target: `main`;
- `HEAD == fetched origin/main`;
- commit: `79bcc20244ab55045ba1c5d778d829d4caac3dd3`;
- tree: `3855a579d0174d2c45587cba248e96d42c319664`;
- clean worktree;
- `e865be51da6c7e1a7a4f5b1fcad0efc513bd0f43` is a strict ancestor;
- the 17-path third-review publication remains present;
- `paper/evidence_ledger.csv` is byte-identical and still ends at `EV-0016`;
- every Study 3 operation counter is zero and every authority flag is false.

If any condition fails, stop with `BLOCKED_ON_STUDY3_V0_5_STARTING_STATE_INTEGRITY`. Do not repair, reset, merge, rebase, cherry-pick, or publish around the mismatch.

### 1.2 Authority order

The binding order is:

1. this operator authority;
2. the published third-review findings and disposition at the exact starting commit;
3. the published draft-v0.4 protocol and its still-valid design decisions;
4. earlier review history as immutable provenance.

Where this authority makes an explicit v0.5 choice, that choice supersedes the corresponding rejected v0.4 field only. It does not reopen any resolved finding or authorize redesign elsewhere.

### 1.3 Zero-operation boundary

All empirical and model-facing quantities must remain zero or null as appropriate. In particular, prohibit:

- tokenizer calls or eligibility checks against any checkpoint;
- checkpoint resolution, download, loading, forward pass, logit read, scoring, generation, parser call, or model comparison;
- item-bank materialization, seed generation, random draw, split realization, or confirmation access;
- interface qualification, selection, ranking by observed results, or rescue;
- evidence-ledger additions;
- use of GitHub Actions or any repository workflow for validation;
- local workstation execution of repository tests when the registered ACR CPU environment is available.

Pure deterministic design-time enumeration, schema validation, exact-binomial recalculation, rendering-registry fixture construction, mutation testing, and static audits are permitted. They carry no empirical or scientific evidence authority.

## 2. Required reading and immutable review history

Before drafting, read in full at the starting commit:

- `studies/study3/prompts/study3_v0_4_independent_methods_review_authority.md`;
- `studies/study3/reviews/v0_4_independent_methods_review.md`;
- `studies/study3/reviews/v0_4_independent_methods_review.json`;
- `studies/study3/methods_review_receipt_v0_4.json`;
- `studies/study3/protocol/interface_calibration_protocol_draft.json`;
- its Markdown and schema companions;
- `studies/study3/analysis/design_statistics.py` and `design_statistics_tables.json`;
- `studies/study3/analysis/independent_methods_review_packet_v0_4.md`;
- both earlier independent review records and both operator-amendment records;
- the charter, both Study 3 routing READMEs, handoff, current status, methods ledger, decision log, run log, and artifact index;
- all Study 3 design and historical-review test modules.

Record a pre-drafting derivation commit containing this authority byte-identically and an initial closure matrix for all ten `S3MR3-*` findings. Do not read any drafting output before that derivation commit exists. Preserve proof that the first drafting-output read is a strict descendant of the derivation commit.

The following are immutable scientific-review provenance and may not be edited, deleted, replaced, relabelled, or weakened:

- every first-, second-, and third-review authority, report, JSON, schema, receipt, recalculation program, recalculation table, and review-specific test module;
- every earlier operator-amendment authority and record;
- the historical-harness erratum and its reviewed-commit anchoring semantics;
- every earlier finding, severity, disposition, reviewed-artifact identity, disclosed error, and failed or aborted run;
- every evidence-ledger row through `EV-0016`.

The new amendment may cite these objects and append new registry rows. It must never rewrite an earlier registry row to make history appear cleaner.

## 3. Fixed scientific scope that v0.5 may not reopen

Study 3 remains an **interface-adequacy and label-binding calibration design**, not a test of reasoning, chain-of-thought internalization, causal mechanism transfer, J-space or J-lens validity, or Study 2 Gate A.

Retain all of the following unless an explicit repair below says otherwise:

- S1 and S2 are selectable; S2 remains preferred when eligible;
- S3 remains conditional/integrity-check status under the current single-token domain;
- S4 remains diagnostic-only, never selectable, excluded from every success union, and causally interpretable only as a generation-enabled composite interface;
- exactly three registered rendering IDs overall: `R-base`, `R-sep`, and `R-instr`;
- K5 remains applicable only to label-and-option profiles S1 and S4;
- I3's primary indicator remains `J_joint_correct`;
- `J_joint_correct` is a level: the probability both members of one registered presentation pair are correct under that cell's registered generator distribution;
- it does not identify a presentation effect, invariance, equivalence, absence of a presentation effect, or direction of a presentation effect;
- the complete 16-case outcome lattice remains published, with `b`, `c`, and correctness-state discordance `d` descriptive only and carrying no decision, eligibility, selection, confirmation, rescue, or claim authority;
- `p_joint + d <= 1` remains an identity of the joint law, with no independence claim and no reverse implication from small `d` to correctness;
- the item draws remain iid with replacement within each sampling cell; duplicate tuples are retained; no post-draw uniqueness, difficulty, balance, or response filtering is permitted;
- cross-cell dependence remains handled only by the registered arbitrary-dependence union bound;
- the exact-binomial sample-size search remains over every positive integer, not multiples of 32;
- `OD2`, `UR-22`, and the RP wrapper decisions remain unresolved and must not be advanced;
- no S5 or generation minimal pair is introduced in this amendment.

## 4. Binding operator decisions for draft-v0.5

### 4.1 S3MR3-001 — K6-SEP is not applicable to S2 or S3

Adopt the following substantive decision:

- `K6-SEP` continues to mean the separator **between a displayed option label and its displayed option content**.
- It remains applicable to S1 and S4, which render labelled option lists.
- It is `not_applicable` to S2 and S3, which render neither option labels nor option contents.
- Do not invent a profile-specific replacement separator for S2 or S3.
- Do not render an `R-sep` duplicate of `R-base` for S2 or S3.
- Do not count `not_applicable` as pass, zero effect, robustness evidence, a gate-bearing cell, or a denominator member.
- S2 and S3 each have exactly one genuine I3 contrast under this design: `K6-INSTR`.
- Their per-profile I3 claim ceiling must state joint robust correctness only for that one registered K6 pair, subject to S3's existing conditional status.
- S1 retains seven K5 pairs plus both K6 pairs. S4 retains the same contrast availability for diagnostic description only.

Re-derive every affected applicability list, cell census, gate truth-table row, claim ceiling, fixture count, sampling/evaluation mapping, operation projection, state transition predicate, table row, Markdown explanation, and test. A text-only claim edit is insufficient.

Independently recompute `m_max`, every type-I allocation, every per-cell power target, every minimal sample size and pass count, and every development/confirmation operation projection. Do not assume that a published number remains unchanged. If a number remains unchanged, record the derivation showing why. Never preserve a number merely for continuity.

### 4.2 S3MR3-010 — register a complete deterministic rendering surface

Create a normative, machine-readable draft-v0.5 rendering registry and schema. The protocol must identify them as binding inputs, not illustrative examples.

The registry must be sufficient for an independent implementation to produce every applicable prompt byte without a later wording, punctuation, whitespace, ordering, escaping, or placeholder choice. At minimum register:

- UTF-8 encoding, LF policy, terminal-newline policy, and normalization policy;
- the exact deterministic question-stem template for every registered operation family, depth, and task stratum that can enter a gate-bearing cell;
- placeholder names, types, allowed surface forms, ordering, interpolation, escaping, and rejection rules;
- exact option ordering and exact option-line grammar for S1 and S4;
- exact label alphabets and exact label-to-content separators;
- exact instruction sentences for every applicable `(profile, rendering)` pair;
- exact answer cue and whether any candidate surface includes leading or trailing whitespace;
- exact raw-completion prompt template for S1, S2, and S3;
- exact message content supplied to S4 before any role-native chat wrapper;
- the boundary between registered message content and any role-specific wrapper;
- deterministic tie-break order and the exact candidate surface strings scored at the answer position;
- a table of applicability for every `(profile, rendering, contrast)` combination;
- a cryptographic identity for the registry itself and for every separately referenced normative template asset.

For `K6-SEP`, use these exact ASCII separator literals unless a contradiction with an already-frozen answer-domain surface is proven and disclosed:

- `R-base`: `": "` between label and option content;
- `R-sep`: `" = "` between label and option content.

Every other prompt byte must be identical within a `K6-SEP` pair.

For `K6-INSTR`, register two exact, semantically co-referential instruction strings for each applicable profile. `R-base` and `R-instr` may differ only in that registered instruction field. The answer cue and every other prompt byte must be identical within the pair.

The amendment must include deterministic fixtures that:

- instantiate every applicable profile/rendering/contrast branch without a model;
- prove `K6-SEP` members differ in bytes for S1 and S4;
- prove `K6-SEP` is structurally absent, not duplicated, for S2 and S3;
- prove `K6-INSTR` members differ in bytes for every applicable profile;
- prove each pair differs only in its registered factor;
- reject a byte-identical contrast pair;
- reject any unregistered whitespace, punctuation, placeholder, option order, instruction, cue, or wrapper substitution;
- reject a missing task-template branch;
- cover every generator support branch that can feed a gate-bearing cell.

Tokenizer distinctness is not tested in this round because no checkpoint or tokenizer may be accessed. Instead register a future fail-closed pre-bank rule: after checkpoints and tokenizers are separately authorized and pinned, every gate-bearing pair must be shown to produce distinct token-ID sequences for every role to which the cell applies. Failure makes that role/profile/contrast ineligible; it is never treated as a pass or silently repaired. This future rule does not resolve `OD2` and performs no tokenizer call now.

S4's role-native chat wrapper may remain role-specific, but the pre-wrapper message bytes must be exact. Any unresolved RP wrapper remains explicitly null under `OD2`; it must not be filled in by v0.5.

### 4.3 S3MR3-002 — regenerate confirmation applicability as component-level data

Regenerate `design_statistics_tables.json` only from the amended normative inputs. The confirmation table must represent applicability at component level rather than publishing one family-level list that cannot express differences.

At minimum:

- S4 must appear in no confirmation applicability field;
- I1b and K5 confirmation applicability must be restricted to S1;
- K6-SEP confirmation applicability must exclude S2 and S3;
- K6-INSTR applicability must reflect the amended protocol exactly;
- I2 and I4 applicability must match the gate hierarchy and profile contracts;
- a committed test must bind every component-level applicability field to the normative protocol and rendering registry;
- no table row may imply that a never-selectable or not-applicable profile can reach confirmation.

### 4.4 S3MR3-003 and S3MR3-004 — remove active retired language and enforce the full scope

Bring the charter, repository README, studies README, Study 3 README, handoff, protocol Markdown, review packet, status report, and every active claim-bearing field into one consistent draft-v0.5 vocabulary.

Active text must not describe `J_both` as current, must not claim invariance/equivalence/no presentation effect, must not publish `n = 256` or `n = 128` as active sizes, and must not name the second or third review as a pending future review.

Historical passages may preserve withdrawn language only when enclosed in an unambiguous historical record that states the version, rejection, and non-current status. Do not erase the review history.

Widen committed enforcement so that it actually covers the registered scope across all active reviewed paths and all active protocol claim fields, including the research question, success statements, validation-target interpretations, gate questions, `what_fails`, `what_passes`, claim ceilings, routing documents, and handoff. Use explicit, auditable historical exemptions rather than a hand-picked subset that misses active files. Mutation tests must show that prohibited active language in each covered path is rejected.

### 4.5 S3MR3-005 through S3MR3-009 — exact bounded repairs

Apply these operator choices:

1. Remove I4 from `interface_profiles[S4].applicable_gates`. Do not qualify away the contradiction.
2. Remove `STOP_AWAITING_AUTHORITY` from `gate_truth_table.legal_stop_states`. Do not add it to the registered experimental state machine. The repository's pre-execution governance status is not an experimental stop state.
3. Retain the independently verified smallest unrestricted positive integers and exact-binomial test definitions only if fresh recalculation confirms them after the v0.5 applicability change. Disclose beside the search rule that exact-test power is locally non-monotone in `n`; execution must use the exact registered cell size, not interpret it as “at least n”. Do not replace the registered minimum with an eventual-monotonicity threshold unless fresh arithmetic proves the existing design cannot be executed at exact n.
4. Update active round references so draft-v0.5 awaits a **fourth independent methods review**. The amendment session must not perform or prejudge that review.
5. Restate the end-to-end union-bound conclusion as returning and confirming **an adequate profile**. If multiple selectable profiles are adequate, the frozen priority order selects the highest-priority adequate profile. Do not claim recovery of an externally predesignated profile.

### 4.6 External-validity bridge remains a future prerequisite

The third review did not classify generator-local external validity as a rejection driver, but `UM3-05` remains open. Preserve the claim ceiling:

- a pass applies only to the registered synthetic generator distributions and named interface/checkpoint roles;
- it does not establish adequacy on an unregistered substantive task distribution;
- before the selected instrument is relied upon outside the generator, a new authority must register a bridge or validation design to the target substantive distribution using physically isolated new data;
- descriptive results observed in this future Study 3 run may not be retrospectively upgraded into bridge evidence.

Record this as an unresolved future-methods prerequisite, not as a reason to add a new gate or task bank in v0.5.

## 5. Mandatory closure matrix

Create a machine-readable and human-readable closure matrix for all ten findings. It must include at least: finding ID, severity, exact starting evidence, operator decision, affected normative fields, affected derived fields, committed tests, negative mutations, residual limitation, and closure status.

Required target statuses:

| finding | required v0.5 closure |
| --- | --- |
| S3MR3-001 | `RESOLVED_BY_NOT_APPLICABLE_REREGISTRATION_AND_FULL_REDERIVATION` |
| S3MR3-002 | `RESOLVED_BY_COMPONENT_LEVEL_CONFIRMATION_APPLICABILITY` |
| S3MR3-003 | `RESOLVED_ACTIVE_TEXT_ALIGNED_HISTORY_PRESERVED` |
| S3MR3-004 | `RESOLVED_ENFORCEMENT_SCOPE_MATCHES_REGISTERED_SCOPE` |
| S3MR3-005 | `RESOLVED_S4_I4_REMOVED` |
| S3MR3-006 | `RESOLVED_NON_MACHINE_STATUS_REMOVED_FROM_STOP_STATES` |
| S3MR3-007 | `RESOLVED_NONMONOTONICITY_DISCLOSED_EXACT_N_REQUIRED` |
| S3MR3-008 | `RESOLVED_ROUND_REFERENCES_UPDATED` |
| S3MR3-009 | `RESOLVED_UNION_BOUND_CLAIM_ALIGNED` |
| S3MR3-010 | `RESOLVED_DETERMINISTIC_RENDERING_SURFACE_REGISTERED` |

Do not mark a finding resolved merely because prose says it is resolved. Every status must trace to normative bytes and at least one positive assertion plus a relevant negative mutation.

Also carry forward the inherited first- and second-review closure matrix. Do not reopen a resolved item. For every previously partial item, state whether v0.5 closes the residual defect or why it remains bounded and non-executable. `OD2` and `UR-22` must remain open.

## 6. Artifact and mutation boundary

The following are the only paths that may be added or modified. The final change set may be a strict subset, but no unlisted path is authorized.

### 6.1 Existing paths that may be modified

- `README.md`
- `docs/decision_log.md`
- `docs/run_log.md`
- `paper/artifact_index.csv`
- `paper/methods_ledger.md`
- `reports/current_status.md`
- `studies/README.md`
- `studies/study3/NEXT_THREAD_HANDOFF.md`
- `studies/study3/README.md`
- `studies/study3/RESEARCH_CHARTER_DRAFT.md`
- `studies/study3/analysis/design_statistics.py`
- `studies/study3/analysis/design_statistics_tables.json`
- `studies/study3/analysis/study2_to_study3_design_traceability.md`
- `studies/study3/protocol/interface_calibration_protocol.schema.json`
- `studies/study3/protocol/interface_calibration_protocol_draft.json`
- `studies/study3/protocol/interface_calibration_protocol_draft.md`
- `studies/study3/references/methods_sources.md`
- `studies/study3/references/positive_reference_dossier.md`
- `tests/test_study3_design.py`

### 6.2 New v0.5 paths that may be added

- `studies/study3/analysis/independent_methods_review_packet_v0_5.md`
- `studies/study3/design_receipt_v0_5.json`
- `studies/study3/prompts/study3_v0_5_design_amendment_authority.md`
- `studies/study3/protocol/interface_calibration_rendering_registry_v0_5.json`
- `studies/study3/protocol/interface_calibration_rendering_registry_v0_5.schema.json`
- `studies/study3/reviews/v0_5_operator_amendment.json`
- `studies/study3/reviews/v0_5_operator_amendment.md`
- `studies/study3/reviews/v0_5_operator_amendment.schema.json`
- `tests/test_study3_rendering_registry_v0_5.py`

No deletion, rename, copy, symlink, submodule, executable-bit change, binary artifact, workflow, generated bank, seed file, result file, or evidence row is authorized. No 29th path is authorized. If a necessary repair requires another path, stop and request new authority before touching it.

Commit this authority byte-identically at the authorized prompt path. Report its exact byte count, LF count, CR count, trailing-newline status, and SHA-256.

## 7. Required deterministic validation

### 7.1 Design and rendering assertions

Add positive assertions and non-vacuous negative mutations for every requirement in sections 3–5. At minimum test:

- all ten `S3MR3-*` closure rows and their required evidence links;
- exact K6 applicability and claim ceilings for S1–S4;
- absence of a duplicate `R-sep` branch for S2/S3;
- exact deterministic rendering coverage and factor isolation;
- rejection of byte-identical applicable pairs;
- complete generator-template coverage;
- component-level confirmation applicability and exclusion of S4;
- S4's removal from I4;
- equality of state-machine and legal stop-state sets;
- full active-claim prohibition scope across documents and structured fields;
- explicit historical exemptions and rejection of a mutation that moves retired language into active scope;
- exact-binomial recalculation over every positive integer and disclosure of local non-monotonicity;
- arbitrary-dependence union-bound arithmetic without any independence assumption;
- operation projections derived from primitive applicability and sample counts;
- all counters zero, all authority flags false, all bank/seed/result fields null or empty;
- `OD2`, `UR-22`, and RP wrappers unresolved;
- evidence ledger unchanged through `EV-0016`.

Produce a requirement-to-test map and mutation ledger. No requirement may be “covered” only by a snapshot hash or by testing that prose contains the word resolved.

### 7.2 Independent arithmetic

The drafting implementation may regenerate normative statistics, but acceptance requires a separate deterministic recalculation path that does not import expected numeric values from the protocol or generated table.

Independently derive and compare:

- the amended sampling-cell and evaluation-cell census;
- applicable contrast counts by profile;
- `m_max` and every multiplicity allocation;
- exact-binomial rejection regions, levels, powers, minimal unrestricted n, pass counts, and local post-minimum non-monotonic runs;
- development, diagnostic, confirmation, I0, and unresolved RP operation projections;
- every state-machine transition and reachable terminal state.

Any disagreement is a hard stop. Do not edit expected values merely to make two implementations agree.

### 7.3 Historical-review non-regression

Run every earlier Study 3 design/review/harness test and every immutable recalculation check under its registered historical-input semantics. Earlier review modules must continue to judge their reviewed commits, not live v0.5 bytes.

Do not modify immutable review tests. Do not weaken, skip, xfail, relabel, or absorb any failure. The two registered historical `test_parser_v3_seal_job` failures may remain only with identical node IDs and signatures.

### 7.4 Clean CPU-only ACR envelope

Run validation in the repository's registered Azure Container Registry CPU environment, with `GPU_COUNT=0`, `CUDA_AVAILABLE=False`, and an exact-commit clean clone. Bind every final run to the candidate commit and tree.

Required final envelope:

- all draft-v0.5 design and rendering tests pass;
- `tests/test_study3_design.py` passes;
- all three independent-review modules and the historical v0.3 harness pass unchanged;
- every immutable recalculation check reproduces exactly;
- `design_statistics.py --check` passes;
- Study 2 and protected-byte regressions remain unchanged;
- the full suite has no new failure or skip;
- the only permitted failures are the same two registered historical parser-v3 seal-job failures with identical signatures;
- the pass-count arithmetic reconciles from the published baseline of 4,062 passes plus the exact net-new collected passing tests;
- static publication audit reports the exact actual subset of the authorized path set, with no unlisted path and no forbidden change type.

Do not hard-code an expected final pass total before test collection. Report collection counts, deselections, skips, failures, and reconciliation exactly. Disclose every aborted or failed run and its cause; never relabel it as a pass.

If a registered ACR disappears, use another already registered repository ACR and disclose the substitution. Do not create cloud infrastructure, modify workflows, or use GitHub Actions.

## 8. Commit and publication discipline

Use small commits with truthful messages. The published history must prove:

1. the byte-identical authority and initial closure matrix were committed before drafting output was read;
2. substantive normative choices preceded derived tables and routing claims;
3. final validation was run on the exact final candidate commit/tree;
4. no empirical operation occurred.

Before publication:

- require a clean worktree;
- fetch remote `main`;
- require fetched `origin/main` still equals `79bcc20244ab55045ba1c5d778d829d4caac3dd3`;
- require the candidate is its strict descendant;
- verify the exact changed-path subset, protected bytes, registry contiguity, authority identity, evidence ledger, zero counters, false flags, null seeds/banks/results, and unresolved decisions.

Publish only by non-force fast-forward:

`git push origin HEAD:refs/heads/main`

Then fetch again and verify `HEAD == origin/main`, exact final tree, clean worktree, and all publication invariants. If remote `main` moved, any unlisted path is needed, any immutable review byte changes, any arithmetic differs, or any validation expectation fails, stop without publishing.

## 9. Required final handoff

After successful publication, report:

- the exact terminal disposition heading from section 0;
- starting and final commits and trees;
- non-force fast-forward proof and `HEAD == fetched origin/main`;
- exact changed-path count and added/modified split;
- authority byte identity;
- ordering proof;
- closure status of all ten `S3MR3-*` findings and inherited residual items;
- the exact K6 applicability decision and amended per-profile I3 claim ceilings;
- the rendering registry identity and completeness proof;
- every changed and unchanged independently recalculated number;
- all clean ACR run IDs and exact results;
- full-suite reconciliation and unchanged historical failures;
- all disclosed drafting or validation errors;
- artifact-index tail and unchanged `EV-0016`;
- zero operation counters, false authority flags, empty results/banks/seeds, no selected interface or positive reference;
- `OD2`, `UR-22`, and RP wrappers still unresolved;
- the original research question still unanswered;
- the sole legal successor: a fresh-session fourth independent methods review of the published draft-v0.5.

Do not begin that review or draft its authority in this amendment session.
