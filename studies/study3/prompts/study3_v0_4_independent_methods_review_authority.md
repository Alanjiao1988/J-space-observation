Continue work in the existing repository Alanjiao1988/J-space-observation.

Run this authority only in a fresh Copilot CLI session and a fresh platform-managed worktree based on remote main. The reviewer must not be the session or drafting party that authored draft-v0.4 or repaired its historical-regression harness. Do not coordinate intermediate conclusions with either drafting session. Procedural separation is necessary but not sufficient: every decision-bearing statistical calculation, construction audit, and logical derivation must be independently implemented.

Use the downloaded authority file directly. Do not execute from rendered, retyped, reformatted, or copy-pasted text. Before any repository mutation, preserve the source at a session-neutral path, compute its byte count, LF/CR counts, trailing-newline state, and SHA-256, and later commit those exact bytes verbatim at the required authority path.

This is a bounded, CPU-only, model-free review of the already-published draft-v0.4. It is not an amendment, protocol freeze, preregistration, positive-reference selection, feasibility pilot, bank-construction, seed, tokenizer, model, GPU, development, confirmation, or mechanistic-execution round.

Use UTC for every new timestamp. Do not create another repository. Do not use GitHub Actions. Final decision evidence must come from clean CPU-only Azure ACR jobs bound to exact commits; local work is limited to read-only inspection, document assembly, and non-authoritative debugging that is disclosed.

The required documentation state at the end is exactly:

STUDY3_DRAFT_V0_4_THIRD_INDEPENDENT_METHODS_REVIEW_COMPLETE_AWAITING_OPERATOR_ACTION

Return exactly one disposition, earned by the review:

STUDY3_V0_4_THIRD_METHODS_REVIEW_ACCEPTED_AS_SPECIFIED

STUDY3_V0_4_THIRD_METHODS_REVIEW_ACCEPTED_WITH_REQUIRED_CONFORMANCE_CHANGES

STUDY3_V0_4_THIRD_METHODS_REVIEW_REJECTED_BOUNDED_AMENDMENT_REQUIRED

STUDY3_V0_4_THIRD_METHODS_REVIEW_REJECTED_FUNDAMENTAL_FEASIBILITY_PILOT_REQUIRED

This authority does not prescribe which disposition to return. Successful existing tests, agreement with the drafting implementation, or the fact that draft-v0.4 responds to every second-review finding is not validation. Statistical validity without construct relevance is not acceptance.

Neither the documentation state nor any disposition is a scientific result or execution authority.

1. Authority hierarchy and hard boundaries

This file is the sole operator authority for this review round. Repository documents describe prior state but do not enlarge it.

Study 1 remains closed at INSUFFICIENT_BEHAVIORAL_SUPPORT_FOR_VALIDITY.

Study 2 remains closed at scientific state STUDY2_PROTOCOL_V1_CLOSED_ON_DEVELOPMENT_FEASIBILITY and documentation state STUDY2_PROTOCOL_V1_TERMINAL_DOCUMENTATION_COMPLETE.

Both prior independent Study 3 reviews remain valid immutable rejections. The draft-v0.3 historical-review test-harness repair is a disclosed scope erratum, not an amendment to the second review.

Draft-v0.4 remains an unfrozen design. No interface, winner, positive reference, bank, seed, confirmation split, result, or scientific evidence exists.

The exact 26-path change set from bc98e5c98a2d4e273142c91497b7600ce751bade through e865be51da6c7e1a7a4f5b1fcad0efc513bd0f43 is the reviewed object. Do not repair any reviewed path during this review, including all three existing Study 3 test modules. A reviewer who edits the reviewed object has ceased to perform this independent review.

The reviewer may create only the eight new review outputs and update only the nine routing and registry files whitelisted in section 10.

OD2 remains a blocking operator decision. Assess whether its boundary and the generic P3-Q/I4 method are sufficiently specified, but do not select, prefer, pin, rank, revision-resolve, download, tokenize, load, run, prequalify, or substitute any checkpoint.

paper/evidence_ledger.csv must remain byte-identical, contain 16 data rows, and end at EV-0016. No evidence row is permitted.

Do not modify paper/limitations_ledger.md or paper/claim_evidence_matrix.md. Defects in an unfrozen design are review findings, not limitations of executed measurement.

No Study 1 or Study 2 path, protected rollup input, dependency, lockfile, container definition, runtime source, infrastructure path, workflow, or experiment source may be modified.

Zero empirical operations are permitted: zero downloads, revision resolution by download, tokenizer construction, tokenization, weight load, forward pass, sequence scoring, generation, activation extraction, hooks, lenses, probes, patches, ablations, provider calls, GPU jobs, bank rows, bank accesses, seeds, interface selections, positive-reference selections, model gate evaluations, result rows, evidence rows, confirmation accesses, Phase 1.0D operations, or RQ2/Study 4 operations.

Allowed computation is committed-blob inspection, hashing, schema validation, deterministic design enumeration, exact arithmetic, independent statistical recalculation, static analysis, Git inspection, and CPU-only ACR validation.

Do not run local pytest or any decision-bearing statistical calculation locally. Disclose any local read-only or non-authoritative action precisely.

Branch and worktree names are non-authoritative metadata. Commit, tree, ancestry, clean state, protected bytes, procedural and computational independence, and bound ACR results are authoritative.

No disposition from this round directly authorizes freeze, OD2 resolution, P3-Q, bank construction, seed draw, model execution, development, confirmation, or mechanistic work.

The descriptive outcome-lattice quantities, including any quantities named b, c, or d, have DESCRIPTIVE_ONLY_NO_DECISION_AUTHORITY. This review may assess that choice but may not silently turn a descriptive quantity into a gate.

If a later instruction conflicts with a prohibition here, the prohibition wins.

2. Mandatory starting-state and authority preflight

Fetch remote main before any change and verify from committed blobs:

repository: Alanjiao1988/J-space-observation

expected origin/main and starting commit: e865be51da6c7e1a7a4f5b1fcad0efc513bd0f43

expected starting tree: 86c5a5ec0e475090c14654cff27605f883495a48

immediate parent: 3c229017d85259af2e2e3a6654b66b4383c064a0

published pre-amendment base: bc98e5c98a2d4e273142c91497b7600ce751bade

draft-v0.4 is exactly 5 commits ahead of that base, with the base as merge base and strict ancestor

git diff bc98e5c...e865be51... contains exactly 26 paths: 6 added and 20 modified, with no deletion, rename, copy, or type change

starting state: STUDY3_INTERFACE_CALIBRATION_PROTOCOL_DRAFT_V0_4_COMPLETE_AWAITING_THIRD_INDEPENDENT_METHODS_REVIEW

registry tails: D43, M-31, AR-0269

evidence ledger: 25,241 bytes, SHA-256 3821730c45b7a58d3c582b38ba354eae77558fa4d419a51e9ff4fdf120411ff1, 16 data rows ending EV-0016

frozen, execution_authorized, bank_authorized, seed_authorized, model_operations_authorized, winner_selected, positive_reference_selected, and confirmation_access_authorized are all false

every empirical/model operation counter is zero; no bank, result, seed, selected checkpoint, selected profile, or confirmation access exists

OD2 and UR-22 remain unresolved blocking operator decisions; no positive reference is selected

both Phase 1.0D protected rollups and every Study 1/2 protected blob match their registered identities

HEAD, fetched origin/main, index, worktree, and untracked-path state are clean and content-identical

At minimum verify these committed blobs from Git, never from CRLF-converted working-tree bytes:

Path

Bytes

SHA-256

studies/study3/analysis/independent_methods_review_packet_v0_4.md

14857

2900615281d05dee6d346876b29130c950efbc17486f28e2437a378ade0142dc

studies/study3/design_receipt_v0_4.json

17959

209d4045a082763b48bee8154c78410984476444ce2106bc9d294bab252a4ef2

studies/study3/prompts/study3_v0_4_design_amendment_authority.md

46543

230c57f4bfb874ad724c9448e8cfc1e58b0ff5970159e6741ce61de8104cd173

studies/study3/reviews/v0_4_operator_amendment.md

40052

f93f76dbef397ce9aba8e548254899f2818cafc08265a338c1e6ca7d9e70bb77

studies/study3/reviews/v0_4_operator_amendment.json

49874

451089a70f4fc9b26e6e67399f413854e612a2b6f233c7fb7ec0a565e585eb57

studies/study3/reviews/v0_4_operator_amendment.schema.json

12979

41cd6e6fce0d2e2155f2ca733b47e750b0fae5662373e770946dfd4b5ffe03c9

studies/study3/protocol/interface_calibration_protocol_draft.json

390029

6a89e02555a6e98b24fa6a5570ebab3bdee14eb9ae7346863246f2982a0f20cb

studies/study3/protocol/interface_calibration_protocol_draft.md

113490

1d700b1e0a7a4990568aba954f48c8c42e3f5ac0a11ac7d7c24ab67382998f79

studies/study3/protocol/interface_calibration_protocol.schema.json

121676

0743419b5e3d09b2ee88ef6ef61f29b605c4eeaafc3855426b21823ba2c10854

studies/study3/analysis/design_statistics.py

55893

9348bd615b7d51959b97f3fdab9d1ba74ca55cdd49e37f09ef0bbb43f793469c

studies/study3/analysis/design_statistics_tables.json

37108

96274e8c4eec3eac699a1cd0c53ea78beb29787e447a52de27c5749a69ae6985

studies/study3/analysis/study2_to_study3_design_traceability.md

23076

9bce6a7b92b06beb981be0068dede3a3a9ba277b86bd0655bc5282e2970d72ab

studies/study3/references/methods_sources.md

23814

99f9854dc167da6745a76563c187cb310dba258ce48c37a5a126cae4d4540319

studies/study3/references/positive_reference_dossier.md

13712

578b35fbfdbc2e3874fca2ea5b5b90ee233690a261b0063f2e27456abe7fae2e

tests/test_study3_design.py

162410

596755be5341cbd794f1df1c698ba8a13ed98384b3b846dd3010da0ab855d862

tests/test_study3_methods_review.py

60781

331d2a7644ee3256d7a145fa8ba83d0b02dcfd1faa1ed8989b726c1c656509ba

tests/test_study3_methods_review_v0_3.py

44821

a1676d31ed32225c8e8a3dba40b4ee6f01d5d3490619eeb35d385efb4ea48c61

Also verify:

the supplemental harness authority is recorded verbatim in section 8 of the v0.4 amendment record at 5,938 bytes and SHA-256 165acb770db3f25c0403a7d376a81b569fff11d623610a802e135633ec9c503b;

the v0.3 recalculation script and table remain 58,317 bytes / cb74c035... and 66,631 bytes / ea11c345... respectively;

the repaired harness still collects the same 35 node IDs as the reviewed historical module, anchors mutable historical inputs to REVIEWED_COMMIT = 2b36f5321d830ea6f70fff2b7bbca3cb93394046, and does not amend any v0.3 finding or disposition;

AR-0246 retains the pre-erratum harness identity while AR-0269 records the corrected current identity; do not rewrite either row in preflight.

Verify the source authority for this round before touching the repository. Record its observed absolute path, bytes, LF/CR counts, trailing-newline state, and SHA-256. Later verify the committed authority blob is byte-identical to that source.

If remote main moved, any required blob differs, the worktree is dirty, the source authority cannot be verified, the protected state differs, or the 26-path review object cannot be reconstructed exactly, stop at:

BLOCKED_ON_STUDY3_V0_4_THIRD_METHODS_REVIEW_STARTING_STATE_INTEGRITY

Do not reset, merge, rebase, cherry-pick, force-push, repair, or replay around a discrepancy.

On success record:

STUDY3_V0_4_THIRD_METHODS_REVIEW_STARTING_STATE_ACCEPTED_CONTENT_IDENTITY_BRANCH_METADATA_NONAUTHORITATIVE

3. Required reading and immutable review object

Read in full before writing a disposition:

root README.md, studies/README.md, and reports/current_status.md;

studies/study3/README.md, RESEARCH_CHARTER_DRAFT.md, and NEXT_THREAD_HANDOFF.md;

all three current protocol files;

analysis/independent_methods_review_packet_v0_4.md;

analysis/design_statistics.py and analysis/design_statistics_tables.json only at the point permitted by section 4;

analysis/study2_to_study3_design_traceability.md;

references/methods_sources.md and references/positive_reference_dossier.md;

all three reviews/v0_4_operator_amendment.* files, design_receipt_v0_4.json, the original v0.4 amendment authority, and the embedded supplemental harness authority;

all three existing Study 3 test modules;

the complete first and second independent reviews, receipts, authorities, independent recalculations, committed tables, and exact review packets they reviewed;

committed history at 2b36f532..., bc98e5c..., 3c229017..., and e865be51... needed to audit the harness erratum;

current decision, run, methods, artifact, evidence, limitations, and claim-evidence registries;

Study 2 terminal handoff, Stage B-D interpretation erratum, and post-hoc interface diagnostic only to verify provenance and non-reuse boundaries.

The 26 draft-v0.4 paths and every prior review artifact are immutable during this review. Read them; do not edit them. Study 1/2 measurements and post-hoc diagnostics are not pilot data for choosing Study 3 parameters.

4. Independence protocol

Create a new implementation at:

studies/study3/analysis/independent_methods_recalculation_v0_4.py

It must not import, execute, dynamically load, copy functions or constants from, or derive control flow from any of:

studies/study3/analysis/design_statistics.py

studies/study3/analysis/independent_methods_recalculation.py

studies/study3/analysis/independent_methods_recalculation_v0_3.py

either prior independent recalculation table

Requirements:

Extract registered inputs from the authoritative protocol JSON and derive formulas and algorithms independently from those inputs and English-language primary statistical sources.

Use independently named and structured functions.

Commit the parameter extraction, formulas, and executable derivation before opening the drafting implementation or drafting output table. Record the ordering and the commit identities. The review packet may be read first, but it is a claim object, not a numeric oracle.

Provide canonical emit and --check modes, fail-closed parameter validation, and no network, model, tokenizer, bank, seed, result, prior-evidence, or confirmation access.

Use integer and exact-rational arithmetic for every binding quantity. Decimal renderings are comparison outputs, never policy inputs.

Independently enumerate the full 16-case I3 outcome lattice, all registered generator supports and exact weights, K5/K6 construction laws, sampling/evaluation cells, selection map, state machine, and operation projections.

Independently derive exact-binomial rejection regions, null tails, power, minimal sample sizes, all union-bound steps, and the end-to-end guarantee under arbitrary dependence.

Include at least one closed-form identity, exhaustive small-case enumeration, or published-example check for every statistical family. Agreement only with drafting bytes is not validation.

Only after the independent implementation and initial outputs are committed may the reviewer open design_statistics.py and design_statistics_tables.json, compare field by field, and classify every difference.

Treat every number as a proposed design parameter, never an observation or model result.

Use primary sources at minimum for the exact binomial model and interval/test duality, intersection-union logic, and multiplicity bounds. Prior repository sources may locate the primary literature, but the reviewer must check whether the protocol's actual assumptions satisfy the method; source citation alone is not validation.

5. Mandatory inherited-repair and non-regression audit

The review JSON must contain exactly one independent adjudication for each second-review finding S3MR2-001 through S3MR2-010.

Allowed inherited-finding statuses:

VERIFIED_RESOLVED

PARTIALLY_RESOLVED

NOT_RESOLVED

For each finding, cite exact artifact paths and field names, state whether the repair is substantive or cosmetic, explain whether it closes the original defect without creating a new one, and distinguish the drafting party's proposed resolution from the reviewer's evidence. Do not accept PROPOSED_RESOLVED_SUBJECT_TO_THIRD_INDEPENDENT_METHODS_REVIEW as proof.

Also produce a non-regression matrix for S3MR-001 through S3MR-020 and UR-01 through UR-22. This matrix verifies that draft-v0.4 did not reopen a previously resolved issue; it does not rewrite either prior review. UR-22/OD2 must remain explicitly unresolved as an operator decision unless the reviewed draft itself unlawfully claims otherwise.

Audit the historical-review harness erratum independently. A passing repaired harness is not self-validating. Verify its commit anchoring, snapshot isolation, input-coverage assertion, unchanged historical node-ID set, unchanged v0.3 recalculation bytes/results, and non-vacuity. Classify any mismatch as a new review finding; do not repair it.

Any inherited blocking defect that is not actually resolved prohibits acceptance as specified.

6. Mandatory new audit targets

Treat every question below as a candidate issue to adjudicate independently. The authority does not prescribe a favorable or unfavorable answer.

6.1 I3 estimand, claim ceiling, and scientific purpose

Re-derive J_joint_correct for every ordered pair in the registered correct / wrong_a / wrong_b / invalid lattice. Confirm exactly which cases score 1.

Express the joint distribution as q11, q10, q01, and q00, where the first two indices are the correctness states of baseline and transformed variants. Derive p_joint = q11 and the correctness-state discordance d = q10 + q01, including the identity p_joint + d <= 1 and its one-way implications. Do not misstate these parameters as independent.

Verify that J_joint_correct is a level, not a contrast: it does not identify direction or magnitude of a presentation effect, different wrong answers when both variants are wrong, invalid-versus-wrong movement, or general answer invariance.

Search every active claim, gate question, what_fails clause, validation target, success statement, charter objective, handoff, and downstream routing field for residual or implied claims of invariance, equivalence, stability across presentations, no presentation effect, or semantic equivalents.

Verify that all quantities derived from discordance or the broader outcome lattice, including registered b, c, and d, are descriptive only, have no null, alpha, p-value, pass/fail, ranking, rescue, eligibility, selection, confirmation, or claim authority, and cannot leak into later decisions.

Do not require a d gate merely because d is available at zero extra forwards. If the reviewer concludes that a presentation-sensitivity gate is necessary for the scientific purpose, classify that as a new substantive estimand/design choice. Do not invent delta0, d1, or a power claim without independent substantive justification and exact recalculation.

Separately adjudicate two questions that must not be collapsed:

Is the narrowed J_joint_correct design internally valid for its exact registered generator-local claim?

Is that narrowed claim still sufficient to serve Study 3's interface-calibration purpose and the planned downstream mechanistic study?

A design may be statistically correct yet fail the second question. If the instrument no longer measures a construct needed by the stated research program, classify the defect according to section 8 rather than accepting it because the wording was narrowed.

State the strongest permitted claim for S1, S2, S3, and non-selectable S4. Do not expand beyond cell-wise joint robust correctness on the registered generator distribution.

6.2 Sampling frame, generator completeness, and external validity

For every one of the 17 development and 17 confirmation sampling cells:

Reconstruct the full generator tuple, support, exact rational weights, validity predicate, independent unit, with-replacement rule, and output-to-Bernoulli mapping.

Verify that every draw ordinal independently draws the complete registered generator tuple. Detect any implementation or specification that instead draws a template once and generates a batch beneath it; such template-level clustering would not be iid Bernoulli without additional treatment.

Verify all exact weights sum to one, including the 32 K5 nuisance states at exactly 1/32, and that duplicate draws are retained rather than deduplicated.

Verify the registered rejection probability of zero follows from deterministic pre-model support validity rather than from an untested assertion.

Distinguish a sampling cell from an evaluation cell. Verify item-stream reuse across profiles/roles preserves marginal iid Bernoulli validity while inducing cross-cell dependence that is permitted only because all binding joint bounds allow arbitrary dependence.

Verify physical and identity separation between development and confirmation namespaces, between target and positive-reference roles, and wherever the protocol claims disjointness.

Determine whether the future generator implementation, seed authority, and bank authority can instantiate the registered distribution without a substantive missing choice. No generator blob, seed, or bank exists now; null must not be treated as zero or as already frozen.

State the external-validity ceiling explicitly. Passing supports only the registered synthetic generator distributions unless a separately justified bridge to real tasks exists. Generator-local validity must not be silently generalized to natural task distributions, benchmark universes, or unrestricted model behavior.

Decide whether that generator-local ceiling is acceptable for the next scientific stage or is a material/fundamental purpose defect.

6.3 Exact-binomial components and arbitrary-dependence power

For I1a, I1b, I2, I3, and I4, separately for development and confirmation:

Recompute p0, p1, exact alpha, n, pass count, exact null tail, and exact power from exact rationals.

Search every unrestricted positive integer n up to the registered ceiling. The retired multiple-of-32 restriction must not survive.

Verify development minimal sizes 413, 214, and 448; development pass counts 389, 129, and 383; confirmation pass counts 388, 127, and 381; and the claimed minimality/non-degeneracy. These are drafting claims to test, not values to assume.

Reconstruct the exact gate-bearing cell counts for every profile. Verify m_max = 43 is taken only over selectable profiles and that S4's diagnostic cells never enter selection or confirmation power.

Independently derive the per-stage profile false-negative budget 19/400, per-cell budget 19/17200, per-cell power target 17181/17200, profile-stage floor 381/400, panel-selection error bound 1/200, and end-to-end floor 9/10.

Verify every binding inequality under arbitrary dependence. Do not use independence, pairwise positive correlation, or an independence product to strengthen a binding claim. Any independence calculation may appear only as clearly labelled sensitivity analysis.

Verify the registered least-favourable configuration is sufficient for the end-to-end guarantee, including higher-priority inadequate profiles, selection order, one-shot confirmation, and the selected profile's confirmation-generating distribution.

Verify the indifference regions, I0 failure, distribution shift, invalid sampling frame, and protocol deviations are excluded from the guarantee rather than hidden inside it.

Distinguish per-cell power, profile-stage power, probability of correct selection, and full development-plus-confirmation power in every artifact.

6.4 Type-I multiplicity, applicability, selection, and confirmation

Write formal null and alternative sets for each atomic cell, the conjunction within one profile, the union over the three selectable profiles, development selection, and the one selected confirmation profile.

Verify intersection-union logic matches the exact profile claim and that per-component development alpha 1/600 bounds a falsely qualified profile without an extra within-profile correction.

Verify the fixed three-profile union bound gives 1/200 and is never data-dependently reduced.

Reconstruct the full selection map, including inapplicability, tie, no-eligible-profile, error, and ambiguity cases. Verify S3 cannot be selected outside its registered single-token applicability condition.

Verify confirmation applicability is the intersection of the selected profile with component applicability: S4 never appears; I1b and K5 apply only to S1.

Verify confirmation is physically disjoint, separately authorized, read once, spent on error or ambiguity, and cannot be retried, retuned, rescued, or used to select another profile.

Verify no descriptive lattice statistic, S4 result, label diagnostic, pooled statistic, or confidence interval can rescue a failed gate.

6.5 State machine, I0, I4, and OD2

Reconstruct the state machine independently and prove it is total and deterministic: every registered event has exactly one next state and every terminal state is reachable.

Verify I0 failure, error, or ambiguity maps only to STOP_INSTRUMENT_DEFECT and makes no interface claim.

Reconstruct I0 fixture accounting: 232 clusters, 232 cluster-derived base items, 464 cluster rows, 38 non-cluster rows, and 502 rows total. Resolve units explicitly.

Verify I4's p0 = 4/5, p1 = 9/10, four operation-family-by-depth cells, applicability, conjunction, and disjointness from external P3-Q.

Verify unresolved OD2 does not make the generic I4 method internally undefined. List every field a later OD2 authority must freeze without selecting a checkpoint now.

Verify the binding ordering constraint P3Q >= 19/20 > I4 p1 = 9/10 > I4 p0 = 4/5 is mathematically and procedurally sufficient for the generic design, and that no document treats it as completed qualification.

6.6 Operation ontology, feasibility, and the missing generation axis

Recompute every work stream from primitive counts and units rather than copying totals:

deterministic I0 fixtures;

target-role development;

external P3-Q;

RP I4 under candidate profiles;

selected-profile one-shot confirmation;

S4 diagnostic generation.

At minimum adjudicate:

target-role development 33,543 rendered rows and sequence-level evaluations;

RP/I4 3,584 rows/evaluations for selectable candidate profiles;

selected-profile one-shot confirmation upper projection 27,856;

S4 26,064 generation calls, 26,064 prefill evaluations, at most 390,960 incremental decode evaluations, and at most 417,024 sequence-level evaluations under the 16-token bound;

the distinction between a sequence-level evaluation, prefill evaluation, incremental decode evaluation, generation call, generated token, and runtime batched model call;

external P3-Q quantities are null, not zero, while OD2 is unresolved;

no grand total is legal while a cost-bearing stream is null.

Then assess headroom scope:

S4 is a non-selectable diagnostic composite that simultaneously changes chat template, free generation, output length, decoding, and parser. If S4 succeeds while S1-S3 fail, the strongest permitted interpretation is that a generation-enabled composite interface shows headroom absent from the restricted interfaces; it does not identify generation as the cause.

Determine whether excluding every generation-enabled profile from the selectable set leaves Study 3 unable to answer a purpose-critical feasibility question. Do not assume that diagnostic-only S4 is sufficient merely because it has a cost table.

A minimal-pair generation profile sometimes described as S5 would be a new design, not an edit this review may make. If such an axis is necessary to make the study scientifically useful, classify that as a substantive or fundamental design finding. Do not create S5 here.

6.7 Historical harness erratum and provenance integrity

Diff the v0.3 historical-review module before and after the erratum. Verify the semantic change is limited to anchoring historical inputs and snapshot isolation, with the same 35 collected node IDs and no weakened assertion, skip, xfail, exception swallowing, or changed expected scientific value.

Independently run non-vacuity probes in clean CPU-only ACR: pristine reviewed snapshot passes; substitution of live v0.4 mutable input fails; perturbation of the committed historical table fails.

Verify every artifact that says an object was immutable, not edited, historical, current, or registered uses those words consistently with the actual Git diff and commit anchor.

Reconcile counters and flags with actual repository mutations. Empirical/model counters must remain zero, but provenance counters or statements must not deny an authorized historical-harness edit that actually occurred.

Verify AR-0246 and AR-0269 preserve history without creating two contradictory claims about the current blob.

Verify all current registered bytes and hashes against e865be51..., all historical bytes against their stated historical commit, all path counts against Git, and all dates/timestamps against the declared time basis. Do not repair discrepancies in this review.

6.8 Cross-artifact consistency and construct-validity verdict

Compare authoritative JSON, Markdown, schema, tables, packet, amendment records, receipts, charter, READMEs, status, handoff, tests, methods ledger, decision log, run log, and artifact index.

Every candidate inconsistency must receive exactly one status:

CONFIRMED_BLOCKING

CONFIRMED_MAJOR

CONFIRMED_MINOR

CONFIRMED_NONBLOCKING

NOT_CONFIRMED

QUALIFIED

Bind evidence paths and exact field names. At minimum cover:

J_joint_correct semantics and every active claim;

descriptive b/c/d non-leakage and the p_joint + d <= 1 relationship;

generator completeness, iid-with-replacement semantics, template clustering risk, and generator-local external validity;

per-cell versus profile/end-to-end power labels and arbitrary-dependence proof;

all profile/cell applicability and the selection map;

S4 diagnostic-only authority, its composite causal ambiguity, and selectable-interface headroom;

I0, state-machine, I4, P3-Q, and OD2 boundaries;

operation ontology and all null-versus-zero fields;

all ten inherited repair claims and the earlier-review non-regression matrix;

historical harness provenance, artifact identities, path counts, counters, registry history, and chronology;

zero-operation and no-evidence boundaries.

Return two explicit verdicts:

METHOD_INTERNAL_VALIDITY_VERDICT

STUDY3_PURPOSE_AND_CONSTRUCT_RELEVANCE_VERDICT

The second verdict cannot be inferred automatically from the first.

7. Required review outputs

Create exactly these eight new paths and no other new path:

studies/study3/reviews/v0_4_independent_methods_review.md

studies/study3/reviews/v0_4_independent_methods_review.json

studies/study3/reviews/v0_4_independent_methods_review.schema.json

studies/study3/analysis/independent_methods_recalculation_v0_4.py

studies/study3/analysis/independent_methods_recalculation_tables_v0_4.json

tests/test_study3_methods_review_v0_4.py

studies/study3/prompts/study3_v0_4_independent_methods_review_authority.md

studies/study3/methods_review_receipt_v0_4.json

The committed authority must be byte-identical to the downloaded source used to start this round. Do not add a header, footer, wrapper, commentary, normalization, or change to its trailing-newline state.

7.1 Human-readable review

The Markdown review must:

bind starting commit/tree and every core reviewed blob;

document procedural and computational independence and the ordering of independent derivation before drafting-output inspection;

adjudicate S3MR2-001 through S3MR2-010 exactly once and provide the earlier-review non-regression matrix;

answer every section 6 target;

assign new findings stable IDs S3MR3-001, S3MR3-002, and so on, with severity BLOCKING, MAJOR, or MINOR and a fundamental boolean;

provide exact-binomial, sampling-support, cell-count, union-bound, selection/state-machine, and operation tables;

provide both explicit verdicts required by section 6.8;

state OD2 and every other operator-decision status;

distinguish reviewed fact, reviewer inference, recommendation, and unresolved operator choice;

return exactly one permitted disposition and its legal successor action;

repeat the exact claim ceiling, unfrozen state, and all-zero empirical/model operation boundary.

7.2 Machine-readable review and schema

The JSON must include at least:

schema version, state, reviewed commit/tree, reviewed path set, and artifact identities;

source-authority and committed-authority identities;

reviewer independence declaration, prohibited-source audit, and ordering proof;

disposition, disposition basis, and rejection class where applicable;

exactly ten inherited-finding adjudications and the two earlier-review non-regression sets;

every mandatory audit answer;

new findings with stable IDs, severity, fundamental flag, evidence paths, fields, rationale, consequence, and successor implication;

cross-artifact candidate adjudications;

both construct verdicts;

I3 estimand/claim and descriptive-lattice decision;

sampling-frame, generator-completeness, and external-validity decisions;

exact-binomial recalculation and minimal-n searches;

multiplicity, selection, state-machine, and arbitrary-dependence power derivations;

S4/headroom/generation-axis decision;

I4/P3-Q/OD2 and confirmation decisions;

historical-harness and provenance audit;

projected operation accounting;

unresolved methods items and unresolved operator items kept separate;

zero empirical/model operation counts and false authority flags;

next legal action.

The schema must reject:

unknown disposition or state;

missing, duplicate, or extra S3MR2 IDs;

missing mandatory audit answer or either construct verdict;

finding without severity, fundamental flag, evidence, rationale, consequence, or successor implication;

binding number without unit and authority status;

a claim that J_joint_correct estimates a presentation effect;

any decision role for descriptive b, c, or d;

a binding independence assumption or unproved overall power claim;

selected interface or positive reference;

any non-zero empirical/model operation counter or true execution/freeze flag;

any bank, seed, result, model output, confirmation access, or evidence row;

acceptance as specified with any unresolved BLOCKING/MAJOR methods item or an inadequate construct-relevance verdict;

accepted-with-conformance when a required change alters an estimand, threshold, sample size, generator distribution, applicability, multiplicity, selection, confirmation, operation ontology, claim ceiling, or headroom axis;

bounded-amendment rejection when any finding marked fundamental drives the rejection;

fundamental-pilot rejection with no fundamental finding;

a successor action inconsistent with section 8.

7.3 Independent recalculation table

The committed table must contain:

exact rational inputs, thresholds, null tails, and power for every gate and split;

unrestricted positive-integer n searches;

full generator supports, exact weight sums, units, and validity predicates for all 34 sampling cells;

the 16-case I3 lattice, q parameterization, and descriptive-only relationships;

drafting-table comparison only after independent derivation;

cell counts by profile, role, family, depth, contrast, and split;

every arbitrary-dependence union-bound step and the least-favourable configuration;

selection graph and state machine;

operation projections derived from primitive counts;

historical-harness non-vacuity results;

a prominent PROPOSED_DESIGN_PARAMETERS_NOT_MEASUREMENTS status.

7.4 New committed test

tests/test_study3_methods_review_v0_4.py must:

validate the JSON against the new schema without adding a dependency;

run the new independent recalculation in check mode;

prove by AST/static audit that no drafting or prior-review recalculation is imported, executed, dynamically loaded, or copied through reachable source literals;

bind all 26 reviewed paths and core prior-review blobs to the starting commit or stated historical commit;

enforce exact inherited-ID sets, non-regression sets, new finding parity, construct verdicts, and disposition fail-closed rules;

enforce descriptive-only b/c/d, generator-local claim ceiling, arbitrary-dependence binding logic, and no unauthorized S4/S5 decision path;

enforce separation of methods blockers from OD2;

enforce zero empirical/model counters, false authority flags, no model/RP/interface selection, and no evidence row;

enforce Markdown/JSON parity for disposition, finding IDs, inherited statuses, both verdicts, OD2, state, and next action;

enforce source/committed authority byte identity;

include negative mutations for every fail-closed rule;

accept the unmutated committed review.

Do not modify any pre-existing Study 3 test to make the new review pass. Record any defect it exposes.

8. Disposition rules and the round limit

Return STUDY3_V0_4_THIRD_METHODS_REVIEW_ACCEPTED_AS_SPECIFIED only if:

every inherited blocking/major defect is verified resolved;

every mandatory audit question is answered;

no blocking or major methods defect remains;

both internal validity and purpose/construct relevance are adequate;

the generator-local claim ceiling is explicit and acceptable for the next stage;

the sampling/generator model is complete enough to instantiate later without a substantive design choice;

multiplicity, selection, state machine, confirmation, and arbitrary-dependence power are valid;

operation accounting is complete enough for the next operator decision;

only OD2 remains as the deliberately open non-method operator decision.

OD2 remaining open does not by itself prohibit acceptance, but acceptance does not resolve OD2.

Return STUDY3_V0_4_THIRD_METHODS_REVIEW_ACCEPTED_WITH_REQUIRED_CONFORMANCE_CHANGES only if every required change is bounded and non-discretionary and does not alter any estimand, p0/p1, sample size, atomic cell, generator distribution, applicability, multiplicity architecture, selection order, confirmation lifecycle, operation ontology, claim ceiling, or selectable/headroom axis. Exact edits must be suitable for a later v0.4.1 conformance amendment and bounded conformance check.

Return STUDY3_V0_4_THIRD_METHODS_REVIEW_REJECTED_BOUNDED_AMENDMENT_REQUIRED only if acceptance requires a substantive but localized design choice that does not invalidate Study 3's basic purpose or feasibility strategy. Examples may include a correctable threshold, sample-size, applicability, or lifecycle defect whose repair does not require redefining what Study 3 is trying to measure. State why the defect is not fundamental.

Return STUDY3_V0_4_THIRD_METHODS_REVIEW_REJECTED_FUNDAMENTAL_FEASIBILITY_PILOT_REQUIRED if any rejection-driving defect is fundamental, including but not limited to:

the narrowed valid estimand no longer serves the purpose for which Study 3 exists;

a presentation-sensitivity or generation-axis construct is necessary but absent;

generator-local results cannot support the intended next scientific decision and no defensible bridge is registered;

the sampling frame or model-task-interface cells are not feasibly instantiable without first learning whether any cell approaches the registered success floor;

the design would require another wholesale estimand, interface, task, difficulty, model, or feasibility redesign rather than a bounded repair.

This is the round-limit rule: a fundamental third-review rejection must not automatically become draft-v0.5. Its only legal successor is an operator-authored, publicly registered, zero-claim-authority feasibility-pilot design. The pilot must be physically isolated from later development/confirmation data and must not use pilot effect sizes to post hoc justify confirmatory thresholds. The reviewer does not design or start that pilot here.

Unanswered, contradictory, or unidentifiable methods questions are never acceptance. A human/JSON conflict is a validation failure, not grounds to choose the friendlier text. Review cost alone cannot substitute for construct judgment.

Legal successor actions:

accepted as specified: OPERATOR_REVIEW_ADOPTION_AND_OD2_RESOLUTION_ROUND; no freeze or execution;

accepted with conformance changes: OPERATOR_CONFORMANCE_AMENDMENT_FOR_DRAFT_V0_4_1, then a bounded conformance review; no freeze;

bounded amendment rejection: OPERATOR_BOUNDED_AMENDMENT_ROUND_FOR_DRAFT_V0_5, then another independent review; no freeze;

fundamental rejection: OPERATOR_AUTHORITY_FOR_DISCLOSED_ZERO_CLAIM_FEASIBILITY_PILOT_DESIGN; no pilot execution in this review session.

Do not create any successor prompt in this round.

9. Registries and artifact IDs

Register:

decision D44 for the third independent review disposition;

method M-32 for the independent v0.4 recalculation and review method;

artifacts AR-0270 through AR-0279, contiguous and in this exact order:

review Markdown;

review JSON;

review schema;

independent recalculation script;

independent recalculation table;

committed v0.4 review test;

authority copy;

review receipt;

updated studies/study3/README.md;

updated studies/study3/NEXT_THREAD_HANDOFF.md.

Use exact committed-blob bytes and SHA-256 values. Classify every artifact as methods review, review method, validation, routing, or provenance, never scientific result. Do not alter D43, M-31, AR-0001 through AR-0269, any earlier registry row, or EV-0016.

The receipt must bind starting and final commit/tree, the exact 26-path reviewed object, source/committed authority identity, independent implementation identity and ordering, disposition, every new artifact except its own self-hash in its body, path whitelist and observed path set, ACR evidence, protected bytes, evidence-ledger identity, ancestry, zero empirical/model counters, false authority flags, and remaining legal authority.

10. Exact mutation ceiling

The eight added paths are exactly the section 7 outputs.

The only existing paths that may be modified are:

README.md

studies/README.md

studies/study3/README.md

studies/study3/NEXT_THREAD_HANDOFF.md

reports/current_status.md

docs/decision_log.md

docs/run_log.md

paper/methods_ledger.md

paper/artifact_index.csv

The hard ceiling is exactly 17 changed paths: 8 added plus at most 9 modified. An 18th path is a hard stop.

Routing edits may state only the completed review state, actual disposition, reviewed commit, claim ceiling, principal findings, and legal next action. They may not rewrite draft-v0.4.

Do not modify any protocol, schema, packet, drafting calculation/table, amendment record/receipt/authority, existing test, source dossier, traceability file, prior review, evidence/limitations/claim ledger, Study 1/2 file, dependency, infrastructure, script, container, runtime, lockfile, or workflow.

11. Commit and ACR validation sequence

Use intentional commits that preserve the ordering of independent parameter extraction/derivation before drafting-output inspection and keep review logic distinct from registry/publication bookkeeping. Do not squash away that distinction.

All decision evidence must run in clean CPU-only Azure ACR using python:3.11-bookworm or the exact registered CPU validation image, from an exact-commit clean clone. One test path per ACR invocation. Report BOUND_COMMIT, BOUND_TREE, DIRTY=0, Python version, GPU_COUNT=0, CUDA_AVAILABLE=False, exact command, exit code, and complete pass/skip/failure counts.

At minimum run on the final publication candidate:

tests/test_study3_methods_review_v0_4.py;

tests/test_study3_design.py and require 197 passed;

tests/test_study3_methods_review.py and require 78 passed;

tests/test_study3_methods_review_v0_3.py and require 35 passed;

independent_methods_recalculation_v0_4.py --check;

design_statistics.py --check only as a regression of immutable reviewed bytes and require DESIGN_STATISTICS_CHECK_OK sections=19;

Study 2 Stage T and Stage B-D focused tests;

both Phase 1.0D protected-byte modules;

the full repository suite;

a static audit of the exact 17-path maximum, source/committed authority identity, reviewed-object identity, registry contiguity, evidence ledger, protected bytes, independence/prohibited-source rules, historical-harness invariants, zero empirical/model operations, and false authority flags.

The starting full-suite envelope is 3,974 passed, 15 skipped, and exactly 2 registered historical test_parser_v3_seal_job failures with unchanged node IDs and signatures. The final pass count must reconcile as 3,974 plus the number of newly collected passing tests. There may be no new failure, error, unexplained skip change, disappearance or signature change of a historical failure, or unbound run.

If a final run-log or artifact-index commit is added after validation, rerun at least the new review test, all three existing Study 3 tests, the new independent recalculation check, registry/static audit, protected-byte checks, and clean-state check on the actual publication commit. Never claim a commit was validated by a run bound only to its parent.

Any validation helper carrying decision authority must be committed within the eight-output design or expressed in the committed test/recalculation. Supplemental operator-side checks may be ephemeral only if fully disclosed and no conclusion depends solely on them.

12. Publication discipline

Before push:

fetch origin;

verify origin/main still equals e865be51da6c7e1a7a4f5b1fcad0efc513bd0f43;

verify the candidate is its strict descendant;

verify worktree/index/untracked state clean after the final commit;

verify the complete diff is inside the 17-path ceiling;

verify every reviewed and protected byte, registry, counter, flag, and authority identity.

Publish only by non-force fast-forward:

git push origin HEAD:refs/heads/main

Never force-push. If remote main moves, stop; do not merge, rebase, or replay without a new operator decision.

After push verify HEAD equals fetched origin/main, final tree, ancestry, clean state, all new blob identities, artifact contiguity through AR-0279, D44, M-32, unchanged EV-0016, protected rollups, immutable 26-path reviewed object, zero empirical/model operations, and false authority flags.

13. Required final handoff

Lead with the exact disposition and documentation state. Report:

starting and final commit/tree;

ancestry and fast-forward proof;

exact added and modified paths;

reviewed-object and source/committed authority identities;

independence procedure, ordering proof, and recalculation identities;

all ten inherited-finding adjudications and both earlier-review non-regression sets;

every new BLOCKING and MAJOR finding, including its fundamental flag;

both internal-validity and purpose/construct-relevance verdicts;

exact adjudication of I3 estimand/claim, descriptive b/c/d, sampling/generator model, external-validity ceiling, thresholds, multiplicity, arbitrary-dependence power, selection, state machine, confirmation, I4/P3-Q/OD2, S4/headroom/generation axis, operation accounting, historical harness, and cross-artifact consistency;

parameter, generator-support, power, selection/state-machine, and operation tables;

D44, M-32, and AR-0270 through AR-0279;

all bound ACR runs and full-suite reconciliation;

evidence-ledger and protected-rollup identities;

every empirical/model operation counter, all zero;

the only legal successor action under the actual disposition.

State explicitly:

Study 1 remains closed.

Study 2 remains closed.

Study 3 remains unfrozen.

No interface or positive reference is selected.

No bank, seed, model operation, gate result, confirmation access, or evidence row exists.

The original research question remains unanswered.

No freeze, execution, amendment, or pilot authority was created by this review.

Stop after publishing and reporting the review. Do not continue into amendment, OD2 resolution, freeze, P3-Q, bank construction, Azure GPU work, development, confirmation, feasibility-pilot execution, or mechanistic execution.