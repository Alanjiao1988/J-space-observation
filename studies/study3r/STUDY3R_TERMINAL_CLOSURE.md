# Study 3R terminal closure

> **Final lifecycle state:** `STUDY3R_TERMINAL_CLOSURE_COMPLETE_RESEARCH_QUESTION_UNANSWERED`
>
> The authoritative machine-readable router is [`STATUS.json`](STATUS.json). This report is its
> human-readable companion. Study 3R has **no active protocol**, no frozen candidate and no
> execution authority of any kind. `paper/evidence_ledger.csv` still ends at `EV-0016`.

This is a terminal-governance and archival record. It does not repair, amend, reactivate or
succeed Study 3R, and it authorizes no protocol authoring and no model execution.

## 1. Authority and starting state

| item | value |
| --- | --- |
| authority | [`prompts/study3r_terminal_closure_authority.md`](prompts/study3r_terminal_closure_authority.md) |
| authority bytes | `9236` |
| authority SHA-256 | `5daf943cc1a236715e39c29013addfb3e1b34da54b98ffc4f385ea4ace5f3a99` |
| authority Git blob | `4ffb72895562be28dbb2ad8d77b6c1cb7050502f` |
| authority commit | `f3935293d29dac6df0277179ebcdf9f5778d304b` (published alone, first) |
| authority parent | `08c01ff4753b98ad0f43843fc49c93fac68c89da` |
| starting commit | `08c01ff4753b98ad0f43843fc49c93fac68c89da` |
| starting tree | `0dbf9ab33c19606c12c84a985dfabb93131bc0aa` |
| review disposition | `STUDY3R_PROTOCOL_V1_REJECTED_TERMINAL_NO_EXECUTION` |

Verified before writing: clean worktree; `HEAD == fetched origin/main`; the exact starting tree;
`08c01ff…` is exactly three linear commits after `da1ea31…`; the review authority `9952263…` was
committed alone first; all twelve review files are additive; no candidate, historical, protected or
paper path changed; zero merges, rebases, force-pushes or rewrites; all execution flags false.

## 2. Binding operator decision

1. The independent focused review is accepted as the governing methods disposition.
2. Study 3R protocol v1 is rejected and terminal.
3. The four BLOCKING findings are decision-bearing and cannot be converted into limitations.
4. The candidate is not frozen, selected, executable or amendable.
5. No protocol v1.1, v2, repair commit or second authoring session is authorized.
6. No Study 3S, Study 4, Study 3M or other successor is authorized by this session.
7. No model, tokenizer, bank, seed, interface, RP-B, GPU or cloud operation is authorized.
8. The research question remains unanswered.
9. The repository records a methods-development failure, not a negative scientific result about
   J-space or reasoning.
10. Any future restart requires a fresh explicit project-level operator decision issued outside this
    terminal Study 3R authority.

The decision is recorded without qualification.

## 3. Validated components

The independent review reproduced the following **protocol-development** properties. They are
validations of the development artifacts only. **None of them is scientific evidence**, and none of
them supports any claim about a model, an interface or J-space.

| reproduced | value |
| --- | --- |
| immutable checkpoint revisions | all `4` |
| tokenizer/config file hashes | all `16` |
| chat-template digests | all `4` |
| registered wrapper surfaces | both (`W1_RAW_DIRECT`, `W2_ROLE_CANONICAL`) |
| tokenizer-equivalence strata over the adversarial reconstruction set | `1` stratum over a grid of `30` |
| candidate atomic-cell census | `m_max = 58` |
| per-cell error budget | `alpha_per_cell = 1/1160` |
| sample sizes, integer boundaries, exact sizes and exact powers | every registered pair agrees, and every `n` and boundary is minimal |
| candidate manifest entries | all `27` reproduce |
| candidate-registered mutations | all `24` killed |

## 4. Terminal defects

Severity counts: **4 BLOCKING, 5 MAJOR, 2 MINOR**. Nothing below was repaired, and nothing below was
downgraded to a limitation.

### BLOCKING

1. **F-01 — No registered depth-2/depth-3 allocation rule for any mixed bank.**
   Affects task population, atomic-cell content, execution reproducibility and the registered
   depth-3 competence claim.
2. **F-02 — The pooled depth cell lets depth-3 performance be masked to chance or to zero.**
   Affects the pass/fail decision of `G01_COT_CEILING`, `G07_RPB_DEVELOPMENT`,
   `G08_RPB_CONFIRMATION` and the primary headline gate `G09_RT_E0_QUALIFICATION`.
3. **F-03 — Prequalification gates are globally conjunctive, so one candidate's failure ends the
   study.** Affects state transition, candidate-selection result and reachability of the primary
   endpoint.
4. **F-04 — The generated-CoT route freezes no decoding contract beyond `do_sample` and `k`.**
   Affects the rendered model input, execution reproducibility and the `G01_COT_CEILING` pass/fail
   decision that preconditions every later state.

### MAJOR

5. **F-05 — `d0_discriminant_position` is a single fixture constant, not a per-item rule.**
6. **F-06 — `W2_ROLE_CANONICAL` injects a forced reasoning closure absent from the authoritative
   Markdown.**
7. **F-07 — Registered worst-case token bounds are fixture-length point estimates.**
8. **F-08 — Item-disjointness and bank realization have no registered orchestration.**
9. **F-09 — Seven coordinated decision-bearing mutations survive the semantic validator.**

### MINOR

10. **F-10 — The governance policing test widened its own scope predicates.**
11. **F-11 — The `.gitattributes` file is neither a manifest entry nor a declared exclusion.**

### Surviving coordinated mutations

The seven survivors recorded by F-09 are retained unrepaired:

* `adv_cot_parser_regex_unanchored`
* `adv_d2_d3_ceiling_mix_drops_depth_three`
* `adv_d2_d3_family_mix_drops_depth_three`
* `adv_d3_family_depth_relabelled`
* `adv_forced_reasoning_closure_changed`
* `adv_forced_reasoning_closure_removed`
* `adv_surfaces_closure_emptied_while_rendered_bytes_unchanged`

## 5. Claim boundary

This closure makes:

* **no** claim about RT competence;
* **no** claim about any RP-B candidate;
* **no** claim that the interface is valid;
* **no** claim that the interface is invalid;
* **no** claim about the existence of J-space;
* **no** claim about the absence of J-space;
* **no** claim about reasoning internalization;
* **no** scientific negative result of any kind.

The **only** conclusion supported by this record is:

> Study 3R protocol v1 is not a valid executable instrument.

## 6. `NONAUTHORITATIVE_FUTURE_PREREQUISITES_NOT_A_SUCCESSOR_DESIGN`

Listed for archival clarity only. This section is **not** a protocol, **not** a successor design,
contains **no** sample-size recommendation and **no** successor name, and confers **no** execution
authority. It authorizes nothing. Any separately authorized future study would, at minimum, have to
satisfy:

1. separate D2 and D3 populations and gates;
2. explicit allocation and bank orchestration;
3. candidate-local prequalification transitions;
4. a complete CoT decoding/runtime contract;
5. per-item D0 discriminant-position derivation;
6. explicit forced-reasoning-closure naming;
7. semantic tests that kill all seven surviving coordinated mutations.

## 7. Preserved bytes

No Study 3R protocol, schema, Markdown, pointer, registry, state machine, task generator,
statistical calculator, tokenizer artifact, manifest, candidate test, authoring authority or
disclosure changed. No independent-review authority, calculation, test, report, schema or receipt
changed. `.gitattributes` and every Study 3, Study 2, Study 1 and paper path are untouched.

`protocol/study3r_protocol_current.json` is retained unchanged. It is a **rejected-candidate-internal
pointer** describing the historical candidate state at the moment it was reviewed. It is **not** the
current lifecycle authority and prospective readers must not resolve it. The current lifecycle
authority is [`STATUS.json`](STATUS.json).

This closure adds six paths and modifies one:

| change | path |
| --- | --- |
| added | `studies/study3r/STATUS.json` |
| added | `studies/study3r/STATUS.schema.json` |
| added | `studies/study3r/STUDY3R_TERMINAL_CLOSURE.md` |
| added | `studies/study3r/study3r_terminal_closure.json` |
| added | `studies/study3r/study3r_terminal_closure.schema.json` |
| added | `studies/study3r/closure/test_study3r_terminal_closure.py` |
| modified | `studies/study3r/README.md` |

## 8. Validation

| run | result |
| --- | --- |
| registered baseline | `8 failed, 5,120 passed, 16 skipped` |
| full repository suite (`testpaths = tests`) | `8 failed, 5,120 passed, 16 skipped`, 0 collection errors |
| new failure node IDs | none |
| standing failures edited or suppressed | none |
| `tests/test_study3r_protocol_v1.py` + `tests/test_study3r_operator_governance.py` | 163 passed, 0 failed |
| `studies/study3r/closure/test_study3r_terminal_closure.py` | all closure tests pass |
| JSON schema validation | every new artifact validates against its own restrictive schema |

The eight standing failure node IDs are unchanged:

* `tests/test_parser_v3_seal_job.py::test_seal_writes_twelve_objects_with_the_set_manifest_last`
* `tests/test_parser_v3_seal_job.py::test_seal_refuses_a_non_empty_parent_prefix`
* `tests/test_phase1_0d_build_provenance.py::test_the_bundle_digest_ignores_the_checkout_line_endings`
* `tests/test_phase1_0d_generation_launcher_rp_compat.py::test_shim_has_valid_bash_syntax_and_frozen_launcher_remains_in_baseline`
* `tests/test_phase1_0d_protected_bytes.py::test_line_endings_do_not_change_the_rollup`
* `tests/test_phase1_0d_review_image.py::test_v2_refuses_a_rehashed_record_with_moved_metadata`
* `tests/test_study3_p0_feasibility_pilot.py::test_every_committed_p0_source_file_is_lf_only`
* `tests/test_study3_v0_7_focused_review.py::test_the_review_changed_no_reviewed_or_historical_path`

### Inherited scope expiry

One independent-review test was **already failing at the inherited starting head** `f3935293…`,
which the predecessor session published:

```
studies/study3r/reviews/test_study3r_protocol_v1_single_focused_review.py::test_every_review_path_is_additive_and_inside_the_reviews_directory
```

It compares `git diff --name-status <reviewed> HEAD` against the review's own path set, so it expires
the moment any authorized commit is added after the review head — the same scope expiry that already
retired `tests/test_study3_v0_7_focused_review.py::test_the_review_changed_no_reviewed_or_historical_path`.
It was verified to **pass at `08c01ff…`** in a detached worktree and to **fail at `f3935293…`**, so it
was not introduced by this closure.

The module is an independent-review artifact that section 3 of this authority forbids editing, so the
failure is **recorded rather than repaired or suppressed**. Its substantive guarantee — that no
candidate, review, historical or protected byte moved — is re-established at the closure head by
`studies/study3r/closure/test_study3r_terminal_closure.py::test_no_candidate_review_or_protected_byte_moved`,
which compares every such blob against `08c01ff…`.

### A note on the README banner

Section 6 requires a terminal banner, while the candidate test
`tests/test_study3r_protocol_v1.py::test_the_study3r_readme_routes_to_the_authored_candidate`
requires the third README line to carry the retired authored-state token — and section 3 forbids
editing that candidate test. The two are **reconciled rather than traded off**: the banner line leads
with `STUDY3R_TERMINAL_CLOSURE_COMPLETE_RESEARCH_QUESTION_UNANSWERED` and names the authored state
only to mark it superseded, retired and no longer a routing instruction. Both the candidate test and
the closure tests pass unmodified.

## 9. Lifecycle router

[`STATUS.json`](STATUS.json) is the authoritative Study 3R lifecycle router. It records
`active_protocol = null` and sets every one of `frozen`, `execution_authorized`,
`formal_execution_authorized`, `repair_authorized`, `amendment_authorized`,
`second_authoring_session_authorized`, `successor_study_authorized`, `model_execution_authorized`
and `research_question_answered` to `false`. Its schema constrains every decision-bearing value with
`const`, `enum` and `required`, and closes every object with `additionalProperties = false`.

## 10. Counters

Every scientific and execution counter for Study 3R is zero: model constructions, weight files
acquired, forward passes, prefill operations, generations, logit reads, adapter or activation loads,
activation patches, execution seeds drawn, scientific banks realized, RP-B selections, scoring
operations, cloud or GPU jobs, evidence-ledger rows written, candidate repairs or amendments and
successor authorizations.

## 11. Next action

Study 3R is closed. There is no active protocol, no successor and no answer to the research question.

> Any future restart requires a fresh explicit project-level operator decision issued outside this
> terminal Study 3R authority.

`STUDY3R_TERMINAL_CLOSURE_COMPLETE_RESEARCH_QUESTION_UNANSWERED`
