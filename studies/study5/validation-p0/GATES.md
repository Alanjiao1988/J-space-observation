# P-0 gate results, rendered from the committed artifacts

Generated by `tools/render_gates.py` from `out/od017_conformance.json`
and `out/non_vacuity_demonstration.json`. Nothing here is retyped, so
this document cannot drift from the artifacts of record.

---

## OD-011 — non-vacuity demonstration

Null source: **synthetic ensemble**, seed `20260829`, 95 clusters, sigma [0.05, 0.15].

Proven before any real curve was judged: **True**.

| case | must return | returned | layers above ceiling | result |
|---|---|---|---|---|
| `all_zero` | `NOT_CAUSALLY_USED` | `NOT_CAUSALLY_USED` | [] | PASS |
| `pure_noise` | `NOT_CAUSALLY_USED` | `NOT_CAUSALLY_USED` | [] | PASS |
| `strong_localised_effect` | `CAUSALLY_USED` | `CAUSALLY_USED` | [12, 13, 14, 15, 16] | PASS |
| `the_null_itself` | `NOT_CAUSALLY_USED` | `NOT_CAUSALLY_USED` | [] | PASS |

All passed: **True**.

> the rule does not manufacture a positive from a null, from noise or from an empty curve, and it is still able to return a positive when one is present; both directions are required, because a rule that can only say no is as uninformative as one that can only say yes

---

## OD-017 — registered text against live implementation

**23 entries, 0 divergences, verdict `PASS`.**

Method: each module is imported and its live values are compared against P0_PREREGISTRATION.json; comments and hand-written agreement tables are not accepted as evidence

Directionality precedent: changing the IMPLEMENTATION to match the REGISTERED TEXT is a bug fix; changing the REGISTERED TEXT to match the DATA is p-hacking. The difference is the direction, not the outcome

| # | registered id | verdict | registered value | observed value |
|---|---|---|---|---|
| 1 | `1_sampling.source` | **CONFORMS** | `"multihop"` | `"multihop"` |
| 2 | `1_sampling.max_units` | **CONFORMS** | `200` | `200` |
| 3 | `1_sampling.seed` | **CONFORMS** | `20260829` | `20260829` |
| 4 | `1_sampling.admissibility_rules` | **CONFORMS** | `["both members tokenise to the same length under the registered encodi` | `["both members tokenise to the same length under the registered encodi` |
| 5 | `2_decision_rule.decisive_site` | **CONFORMS** | `"BRIDGE"` | `"BRIDGE"` |
| 6 | `2_decision_rule.confidence_level` | **CONFORMS** | `[0.95, 2.5, 97.5]` | `[0.95, 2.5, 97.5]` |
| 7 | `2_decision_rule.bootstrap` | **CONFORMS** | `[10000, 20260829]` | `[10000, 20260829]` |
| 8 | `2_decision_rule.verdict_condition` | **CONFORMS** | `["CAUSALLY_USED", "NOT_CAUSALLY_USED"]` | `["CAUSALLY_USED", "NOT_CAUSALLY_USED"]` |
| 9 | `2_decision_rule.unit_exclusion` | **CONFORMS** | `0.0` | `0.0` |
| 10 | `3_null.replicates` | **CONFORMS** | `5` | `5` |
| 11 | `3_null.ceiling_rule` | **CONFORMS** | `"PREFIX"` | `"PREFIX"` |
| 12 | `3_null.sites_measured_for_nulls` | **CONFORMS** | `["CUE", "BRIDGE", "READOUT"]` | `["CUE", "BRIDGE", "READOUT"]` |
| 13 | `3_null.integrity_tolerance` | **CONFORMS** | `0.0001` | `0.0001` |
| 14 | `4_harness.gate` | **CONFORMS** | `["CUE", -1, 0.9]` | `["CUE", -1, 0.9]` |
| 15 | `7_gates.OD-011.required_cases` | **CONFORMS** | `{"all_zero": "NOT_CAUSALLY_USED", "pure_noise": "NOT_CAUSALLY_USED", "` | `{"all_zero": "NOT_CAUSALLY_USED", "pure_noise": "NOT_CAUSALLY_USED", "` |
| 16 | `layers.depth_grid` | **CONFORMS** | `-1` | `-1` |
| 17 | `transcription.readout_rule` | **CONFORMS** | `"token_before_target"` | `"token_before_target"` |
| 18 | `transcription.surface_forms` | **CONFORMS** | `[["Brazil", " Brazil", "brazil", " brazil"], ["gold", " gold", "Gold",` | `[["Brazil", " Brazil", "brazil", " brazil"], ["gold", " gold", "Gold",` |
| 19 | `independence.jlens_absent_from_the_measurement` | **CONFORMS** | `false` | `false` |
| 20 | `independence.no_p0_tool_imports_jlens` | **CONFORMS** | `[]` | `[]` |
| 21 | `independence.jlens_absent_at_runtime` | **CONFORMS** | `false` | `false` |
| 22 | `layers.observed_depth` | **CONFORMS** | `28` | `28` |
| 23 | `1_sampling.frame_reached_the_tool_unchanged` | **CONFORMS** | `20260829` | `20260829` |

### What each entry checks

**1. `1_sampling.source`** — CONFORMS

- registered: the frame is drawn from the pre-existing multihop evaluation set
- implemented by: `build_pairs.EVAL_SLUG`
- what the code does: the tool opens lens-eval-<EVAL_SLUG>.json and reads no other set

**2. `1_sampling.max_units`** — CONFORMS

- registered: max_units = 200
- implemented by: `passed to build_pairs.main as --max-units and compared against len(units)`
- what the code does: the cap is applied by dropping whole clusters, never by splitting one
- note: the live check that the value reaching the tool equals this is the frame report's frame.max_units field, audited below when present

**3. `1_sampling.seed`** — CONFORMS

- registered: seed = 20260829
- implemented by: `build_pairs.main --seed, and decide_p0.BOOTSTRAP_SEED`
- what the code does: the same integer seeds the frame and the bootstrap

**4. `1_sampling.admissibility_rules`** — CONFORMS

- registered: the ten registered admissibility rules
- implemented by: `build_pairs.compatible and build_pairs.sites_for`
- what the code does: compatible() enforces the four content rules; sites_for() enforces equal length, at least one difference, the last difference strictly before the readout, non-empty BRIDGE and non-empty PREFIX
- note: the registration text and the tool's own emitted list are the same object by construction; the behavioural claim is carried by the excerpt and by tests/test_p0_pairs.py, which asserts each rule rejects a case built to violate it

**5. `2_decision_rule.decisive_site`** — CONFORMS

- registered: the verdict is read from BRIDGE and from no other site
- implemented by: `decide_p0.DECISIVE_SITE, used in decide_p0.decide`
- what the code does: decide() looks up summary[real_key][DECISIVE_SITE] and reads the layer set from that series alone; CUE and READOUT never enter the verdict expression

**6. `2_decision_rule.confidence_level`** — CONFORMS

- registered: two-sided 95 percent
- implemented by: `decide_p0.CONFIDENCE, LOWER_PERCENTILE, UPPER_PERCENTILE`
- what the code does: the interval is taken as the 2.5th and 97.5th percentiles of the bootstrap distribution

**7. `2_decision_rule.bootstrap`** — CONFORMS

- registered: 10000 resamples, seed 20260829, cluster resampling
- implemented by: `decide_p0.BOOTSTRAP_RESAMPLES, BOOTSTRAP_SEED, cluster_bootstrap`
- what the code does: cluster_bootstrap draws len(clusters) clusters with replacement and averages over all observations in the drawn clusters, so both directions of a pair move together

**8. `2_decision_rule.verdict_condition`** — CONFORMS

- registered: there exists a layer of BRIDGE whose 2.5th bootstrap percentile is strictly greater than the ceiling
- implemented by: `decide_p0.decide, the `passing` comprehension`
- what the code does: a layer is retained when stats['lcb'] > ceiling, strictly; the verdict is CAUSALLY_USED when that list is non-empty and NOT_CAUSALLY_USED otherwise

**9. `2_decision_rule.unit_exclusion`** — CONFORMS

- registered: units whose denominator is not strictly positive are dropped
- implemented by: `patch_effect.MIN_DENOMINATOR and the guard in patch_effect.main`
- what the code does: a unit is skipped and appended to `dropped` when ld_donor - ld_recipient <= MIN_DENOMINATOR

**10. `3_null.replicates`** — CONFORMS

- registered: five replicates per null construction
- implemented by: `build_pairs.NULL_REPLICATES, consumed by patch_effect.main`
- what the code does: the frame assigns NULL_REPLICATES third items per unit, and the measurement emits NULL_C_<r> and NULL_R_<r> for r in range(replicates)

**11. `3_null.ceiling_rule`** — CONFORMS

- registered: the maximum, over every null construction, every replicate, every site other than PREFIX, and every layer, of the upper 95 percent cluster-bootstrap bound
- implemented by: `decide_p0.decide, the ceiling loop`
- what the code does: the loop maximises stats['ucb'] over every key in null_keys, every site except INTEGRITY_SITE, and every layer, producing one scalar
- note: the registered exclusion of PREFIX from the ceiling is realised by the `if site == INTEGRITY_SITE: continue` guard

**12. `3_null.sites_measured_for_nulls`** — CONFORMS

- registered: PREFIX is measured for the real construction only
- implemented by: `patch_effect.NULL_SITES`
- what the code does: the null job builder iterates NULL_SITES, which excludes PREFIX; the real job builder iterates unit['sites'], which includes it

**13. `3_null.integrity_tolerance`** — CONFORMS

- registered: the PREFIX mean must be within 0.0001
- implemented by: `decide_p0.INTEGRITY_TOLERANCE, applied in decide_p0.check_gates`
- what the code does: check_gates takes the maximum absolute mean over all PREFIX layers and fails the gate when it exceeds the tolerance

**14. `4_harness.gate`** — CONFORMS

- registered: the 2.5th bootstrap percentile of CUE at layer -1 must be at least 0.90
- implemented by: `decide_p0.HARNESS_GATE_SITE, HARNESS_GATE_LAYER, HARNESS_GATE_MIN_LCB`
- what the code does: check_gates reads summary[real][CUE]['-1'] and fails when its lcb is below the registered floor

**15. `7_gates.OD-011.required_cases`** — CONFORMS

- registered: {'all_zero': 'NOT_CAUSALLY_USED', 'pure_noise': 'NOT_CAUSALLY_USED', 'strong_localised_effect': 'CAUSALLY_USED', 'the_null_itself': 'NOT_CAUSALLY_USED'}
- implemented by: `non_vacuity_p0.REQUIRED`
- what the code does: the demonstration iterates REQUIRED and fails unless every case's verdict equals its required value

**16. `layers.depth_grid`** — CONFORMS

- registered: layer -1 is the embedding output and 0..27 are decoder block outputs
- implemented by: `patch_effect.EMBEDDING_LAYER and Harness.layers`
- what the code does: Harness.layers is [EMBEDDING_LAYER] + list(range(n_layers)), where n_layers is read from the loaded model rather than assumed

**17. `transcription.readout_rule`** — CONFORMS

- registered: P-0 transcribes EQ2's multihop readout rule rather than importing it, so that jlens stays out of the ground-truth path
- implemented by: `build_pairs.READOUT_RULE against rank_profile.READOUT_RULE['multihop']`
- what the code does: both name the same rule; build_pairs.readout_position returns len(tokens) - 1, and rank_profile.readout_position returns the same index for this rule
- note: a transcription is exactly what drifts silently, which is why it is compared against the live EQ2 value here rather than trusted

**18. `transcription.surface_forms`** — CONFORMS

- registered: P-0's surface-form expansion matches EQ2's for a set with no synonym rule
- implemented by: `build_pairs.surface_forms against rank_profile.synonym_forms('multihop', .)`
- what the code does: both produce the bare, lowercase and capitalised variants, each with and without a leading space, in the same order

**19. `independence.jlens_absent_from_the_measurement`** — CONFORMS

- registered: tools/patch_effect.py never imports jlens
- implemented by: `guard_p0.imports_jlens applied to patch_effect.py`
- what the code does: the check matches an import STATEMENT, not the bare word, so a file that merely names the library is not reported; patch_effect.py additionally records sys.modules membership at the end of each run
- note: a substring rule would fire on this auditor's own source, so the check is delegated to the guard's statement-level matcher

**20. `independence.no_p0_tool_imports_jlens`** — CONFORMS

- registered: no P-0 tool imports jlens
- implemented by: `guard_p0.scan_tools, tools_importing_jlens`
- what the code does: every file in tools/ is parsed for an import statement, the guard itself included

**21. `independence.jlens_absent_at_runtime`** — CONFORMS

- registered: no part of the instrument under test was loaded during the measurement
- implemented by: `patch_effect.instrument_under_test_is_loaded, written into the report`
- what the code does: the committed report records whether the library was present in sys.modules when the run finished

**22. `layers.observed_depth`** — CONFORMS

- registered: 28 transformer layers plus the embedding output
- implemented by: `the loaded model's model.model.layers length`
- what the code does: read from the model at run time, not assumed

**23. `1_sampling.frame_reached_the_tool_unchanged`** — CONFORMS

- registered: the frame the measurement consumed is the frame that was committed
- implemented by: `patch_effect writes units_file_sha256 into its report`
- what the code does: the digest is computed from the file the run actually opened

