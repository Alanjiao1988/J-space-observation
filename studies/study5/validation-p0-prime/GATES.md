# P-0' gate results, rendered from the committed artifacts

Generated by `tools/render_gates_p0prime.py` from the JSON artifacts of record. Nothing here is retyped, so this document cannot drift from them.

In the case table, *worst |mean|* is the pooled mean itself, so a case required to return 1 shows 1 and a case required to be positive shows its magnitude. *Interval contains 0* is required only of the two must-be-zero cases.

---

## OD-011 as revised — the four synthetic cases

Estimand under test: **the one the directive prescribed**, `(L_patch - L_clean) / (L_full - L_clean)`.

How *approximately zero* is formalised: the construction's 95 percent cluster-bootstrap interval must contain zero at every layer; no numeric tolerance is invented, because a tolerance chosen after the failure it is meant to judge would not be evidence

| case | must return | worst \|mean\| | interval contains 0 | result |
|---|---|---|---|---|
| `case_1_no_op` | `EXACTLY_ZERO` | 0.000000 | True | PASS |
| `case_2_random_vector` | `APPROXIMATELY_ZERO` | 0.515487 | False | **FAIL** |
| `case_3_full_donor` | `ONE` | 1.000000 | False | PASS |
| `case_4_carries_the_intermediate` | `SIGNIFICANTLY_POSITIVE` | 0.600000 | False | PASS |

All passed: **False**.

### Why case 2 fails, in closed form

> L_patch = 0, the expected gap of a state carrying no information about which answer token is which
>
> `effect = -L_clean / (L_full - L_clean)`
>
> the numerator is positive and the denominator is positive, so a patch that destroys the state produces a POSITIVE effect, not zero

Swept over 20000 plausible clean-run pairs, using no P-0 data: min 0.0635, mean 0.4997, max 0.9352, **fraction at or below zero 0.0000**.

---

## No-op integrity after the three-part batch repair

dtype `bfloat16`, 190 units, batch 48, tolerance 1e-04 normalised.

| family | worst \|mean\| normalised | worst single unit | worst \|mean\| logits |
|---|---|---|---|
| `EMBED_NOOP` | 0.000e+00 | 0.000e+00 | 0.000e+00 |
| `PREFIX_DONOR` | 0.000e+00 | 0.000e+00 | 0.000e+00 |
| `SELF_PATCH` | 0.000e+00 | 0.000e+00 | 0.000e+00 |

**Verdict PASS** — bit-exact, not merely within tolerance.

Baseline shift against a batch-of-one forward: 0.476257 logits.

---

## Registered inclusion

| | |
|---|---|
| units measured in P-0 | 184 |
| correct on both sides | **18 units, 9 clusters** |
| after the denominator floor | 18 units |
| registered floor | 30 |
| meets the floor | **False** |

---

## OD-017 conformance

**18 entries, 1 divergence(s), verdict `FAIL`.**

> entry 0.estimand_is_a_replacement is EXPECTED to diverge; it is the audit reporting that the prescribed replacement is the estimand P-0 already used, which is the finding that halted the phase

| # | registered id | verdict | registered | observed |
|---|---|---|---|---|
| 1 | `0.estimand_is_a_replacement` | **DIVERGES** | `"a different estimand from P-0's"` | `"the same estimand as P-0's"` |
| 2 | `1.estimand_is_held` | **CONFORMS** | `0` | `0` |
| 3 | `5.OD-011.must_return_positive_case_present` | **CONFORMS** | `true` | `true` |
| 4 | `5.OD-011.four_cases` | **CONFORMS** | `["case_1_no_op", "case_2_random_vector", "case_3_full_dono` | `["case_1_no_op", "case_2_random_vector", "case_3_full_dono` |
| 5 | `5.OD-011.no_threshold_from_P0_data` | **CONFORMS** | `false` | `false` |
| 6 | `2.n_floor` | **CONFORMS** | `30` | `30` |
| 7 | `2.n_floor_disclosure` | **CONFORMS** | `"no"` | `"no"` |
| 8 | `2.denominator_floor` | **CONFORMS** | `1.0` | `1.0` |
| 9 | `2.correct_only_measured_on_clean_runs` | **CONFORMS** | `true` | `true` |
| 10 | `3.no_op_tolerance` | **CONFORMS** | `0.0001` | `0.0001` |
| 11 | `3.repair_is_three_parts` | **CONFORMS** | `true` | `true` |
| 12 | `8.no_tool_imports_the_instrument_under_test` | **CONFORMS** | `[]` | `[]` |
| 13 | `8.no_tool_references_the_target` | **CONFORMS** | `[]` | `[]` |
| 14 | `8.P0_artifacts_untouched` | **CONFORMS** | `true` | `true` |
| 15 | `layers.depth_grid_unchanged` | **CONFORMS** | `-1` | `-1` |
| 16 | `3.no_op_observed` | **CONFORMS** | `true` | `true` |
| 17 | `8.instrument_absent_at_runtime` | **CONFORMS** | `false` | `false` |
| 18 | `2.count_computed_after_the_push` | **CONFORMS** | `true` | `true` |

### What each entry checks

**1. `0.estimand_is_a_replacement`** — DIVERGES

- registered: the estimand is REPLACED with a logit-difference recovery ratio
- implemented by: `non_vacuity_p0prime.estimand_logit_difference_recovery against the expression in patch_effect.main`
- what the code does: P-0 computed (gap - ld_recipient) / (ld_donor - ld_recipient), where gap, ld_recipient and ld_donor are the logit differences in the patched, clean-recipient and clean-donor runs; the prescribed formula is the same expression under different names
- why this entry exists: an audit that only compared each tool to its own registration could not have noticed that a prescribed replacement was not one

**2. `1.estimand_is_held`** — CONFORMS

- registered: the estimand is HELD and not operative
- implemented by: `P0PRIME_PREREGISTRATION.json section 1`
- what the code does: no measurement tool in this namespace applies an effect estimand; verify_baseline computes only no-op deviations, whose numerator is exactly zero under any recovery ratio of this shape

**3. `5.OD-011.must_return_positive_case_present`** — CONFORMS

- registered: a non-vacuity demonstration must include a case that MUST RETURN POSITIVE
- implemented by: `non_vacuity_p0prime.REQUIRED`
- what the code does: the demonstration iterates REQUIRED and fails unless every case's verdict equals its requirement; two of the four requirements are positive-valued

**4. `5.OD-011.four_cases`** — CONFORMS

- registered: four cases: no-op, random vector, full donor, carries the intermediate
- implemented by: `non_vacuity_p0prime.REQUIRED`
- what the code does: the four keys are exactly these, in this order

**5. `5.OD-011.no_threshold_from_P0_data`** — CONFORMS

- registered: the four cases run on synthetic or constructed objects and may not take a threshold from P-0's existing data
- implemented by: `non_vacuity_p0prime, module constants and build_world`
- what the code does: the synthetic world is built from a registered seed and fixed constants; no P-0 artifact is opened by the module

**6. `2.n_floor`** — CONFORMS

- registered: the floor is 30 correct-both units
- implemented by: `count_inclusion reads the value from the pushed registration`
- what the code does: the tool does not hard-code the floor; it applies the value in the committed registration file

**7. `2.n_floor_disclosure`** — CONFORMS

- registered: the directive requires disclosing whether the pair count was known when the floor was set
- implemented by: `P0PRIME_PREREGISTRATION.json section 2, n_floor.disclosure_required_by_the_directive`
- what the code does: the registration records that only the two separate marginals were known, that the joint count had never been computed, and that it was deliberately left uncomputed until the registration was pushed

**8. `2.denominator_floor`** — CONFORMS

- registered: the denominator floor is 1.0 logits
- implemented by: `count_inclusion reads the value from the pushed registration`
- what the code does: units with L_full - L_clean below the floor are excluded; the floor is evaluated on clean runs only

**9. `2.correct_only_measured_on_clean_runs`** — CONFORMS

- registered: correct-only is evaluated on CLEAN runs, so it is independent of every patching result
- implemented by: `count_inclusion, the correct_both comprehension`
- what the code does: it reads donor_top1_is_donor_target and recipient_top1_is_recipient_target, both recorded from clean forwards

**10. `3.no_op_tolerance`** — CONFORMS

- registered: the no-op families must be within 1e-4, on the normalised scale
- implemented by: `verify_baseline.NOOP_TOLERANCE and the verdict expression`
- what the code does: the verdict is taken on worst_abs_mean_normalised; raw logits are reported beside it but do not decide
- note: an earlier revision of this tool compared RAW LOGITS against this tolerance, which is a units error of exactly the shape this audit exists to catch; it was corrected before any verdict was recorded

**11. `3.repair_is_three_parts`** — CONFORMS

- registered: the baseline is measured inside the same batch
- implemented by: `verify_baseline.main job 0, capture_at_width, and the padding step`
- what the code does: job 0 is a self-patch inside the batch; the cache is captured at the same width as the consuming run; every chunk is padded to full width

**12. `8.no_tool_imports_the_instrument_under_test`** — CONFORMS

- registered: no P-0' tool imports the library EQ2 was testing
- implemented by: `guard_p0.imports_jlens applied to every file in this namespace's tools`
- what the code does: the check matches an import statement rather than the bare word

**13. `8.no_tool_references_the_target`** — CONFORMS

- registered: no P-0' tool references T
- implemented by: `static scan for the target's repository id and pinned revision`
- what the code does: neither marker appears in any tool in this namespace

**14. `8.P0_artifacts_untouched`** — CONFORMS

- registered: P-0 is closed and may not be reopened by this phase
- implemented by: `this namespace writes only under validation-p0-prime/`
- what the code does: the only P-0 path any P-0' tool opens is read-only: the merged report and the shared harness module

**15. `layers.depth_grid_unchanged`** — CONFORMS

- registered: the depth grid is the embedding output plus the decoder blocks
- implemented by: `patch_effect.EMBEDDING_LAYER, reused unchanged`
- what the code does: Harness.layers is [EMBEDDING_LAYER] + range(n_layers), read from the loaded model

**16. `3.no_op_observed`** — CONFORMS

- registered: the no-op families must be within 1e-4 normalised
- implemented by: `verify_baseline, as run on the real frame`
- what the code does: the committed report records the worst normalised mean over all three families

**17. `8.instrument_absent_at_runtime`** — CONFORMS

- registered: the instrument under test is not loaded during any run
- implemented by: `patch_effect.instrument_under_test_is_loaded, written into the report`
- what the code does: the committed report records sys.modules membership at the end

**18. `2.count_computed_after_the_push`** — CONFORMS

- registered: the count is computed only after the registration was pushed
- implemented by: `count_inclusion, the flag it writes`
- what the code does: the tool records that it ran against a committed registration

