You are continuing the research repository Alanjiao1988/J-space-observation as the principal research-software executor. Execute this authority to completion; do not merely propose a plan. Preserve the distinction between protocol design, mechanical tokenization qualification, behavioral evidence, mechanistic evidence, and scientific claims.

This is a new, additive operator authority for Study 2 Stage T only. It authorizes pinned config/tokenizer identity resolution, tokenization and alignment checks, deterministic mechanics-only pair selection, Stage T artifact sealing, validation, documentation, and fast-forward publication to main. It does not alter any frozen Stage P scientific byte and does not authorize B-D, Gate A behavior, B-C, M-D, M-C, model weights, forward passes, generation, activations, probes, patching, ablation, or J-lens execution.

1. Exact takeover state

The only acceptable starting repository state is:

repository: Alanjiao1988/J-space-observation;

branch: main;

HEAD == origin/main == c2e2383e96ba3d94f3dcf9b9b57db36e1f08dcd1;

tree: 533fb62db4db096f4f6d09eeb858a391936a28c9;

worktree: clean;

Stage P registered origin remains ancestor: commit 191d4a3596ab64b26f54effb6ccaf6005f229139, tree 9d1c68d895435928a10ac2b0f44d277b370000c1;

validated Stage P freeze remains ancestor: commit d5e8e19c025410fda7c9eb430f507a201a18c9cd, tree b044133055c697cad8828664254143f3b83f68d5;

lifecycle: NONTERMINAL_CHECKPOINT_STUDY2_PROTOCOL_FROZEN_AWAITING_TOKENIZER_GATE_AND_EXECUTION.

Before editing anything, fetch remote state and verify every identity above. Rehash the complete Stage P handoff package and all protected anchors it enumerates. At minimum verify:

studies/study2/STAGE_P_FINAL_HANDOFF.md: 16,380 bytes, SHA-256 4801d1b52622ade4d6badd9e1b4c37bb3038e7060c39e40faf7c2512e2a8a2e9;

Stage P authority: 53,018 bytes, SHA-256 1408c5ae4d09a097c70b0e984150c4947e527ca12b5614905a98b65685ed0b37;

Gate A amendment: 5,836 bytes, SHA-256 e7f015a71e0491aa26f66780e94ad7fd8201b3d1b9411298d92848781310c3c1;

frozen protocol JSON: 39,357 bytes, SHA-256 2f115e057249fb59e34ef34de2eb71ff042a449bb4ef1637ebec3181aedd7ad5;

frozen protocol schema: 54,502 bytes, SHA-256 d9f834282038f840707ea694bb5dd5422e87a3ca661eb987b2b9ce631d23b134;

frozen protocol Markdown: 21,151 bytes, SHA-256 4d58d8c569728d96999135c2bc5c547980a3c0d86b5c5b2c8ca0b87e57edf054;

frozen methods review: 9,741 bytes, SHA-256 e84607443798d4953a2b59ab7b47e82bd9bf89fe832a3d86de7ec5ab3fd7b139;

freeze decision: 5,025 bytes, SHA-256 aa0151be87a43719ef8056b45d532281178ca5ea55480d46b0f7d484c7caff4d;

task-bank manifest: 32,337 bytes, SHA-256 7d07db2b508136229f06a727a3deb787106e2b389bb1207ab2c2d1099b21458f;

development bank: 384 rows / 752,708 bytes / SHA-256 7dd19884cc2cb4685863cc9df768347f7cfd52c348e5117ec574b52d3b0cf1d6;

behavioral-confirmation bank: 1,536 rows / 3,068,780 bytes / SHA-256 cbd20d061ee5bdc8f8484b79005ad7faa018add9ef028da16cd885f2c89ea3a9;

mechanistic-development candidates: 1,024 pair units / 8,008,776 bytes / SHA-256 397c752162e41ff1bc83ecf4cf58b768baa6400c9e6d20dc092f317238c1ef66;

mechanistic-confirmation candidates: 1,024 pair units / 8,102,984 bytes / SHA-256 61dfaed3b8a56be4d27083bdca5307ea326ecfaeaa26f2d43dd3c8deafd77df6;

Study 1 terminal commit/tree remain 6409d2c6d665187e4459d94d490a20d7b085e8af / bc8b80cb0e66f9426dcdedd52b624c892caa3fc9;

Phase 1.0D protected rollups remain 436ed331c7dd53fa6387d6b52447bc72edf166bbb3640b7f7723a8766bdf51dd for 152 files and ef5a417c572f7da94a562411b752d74b48da2e28aa3aa1491db9bc34dfbde82a for 36 files, each with zero differences;

paper/evidence_ledger.csv still ends at EV-0016.

If the remote head moved, the worktree is dirty, an ancestry or byte identity fails, or a protected file has changed, do not merge, rebase, reset, repair, or continue. Stop as BLOCKED_ON_STUDY2_STARTING_STATE_INTEGRITY and report the exact discrepancy.

2. Binding authority and scientific boundary

Read in full and treat as controlling:

studies/study2/STAGE_P_FINAL_HANDOFF.md;

studies/study2/RESEARCH_CHARTER.md;

studies/study2/protocol/reasoning_internalization_protocol.json;

studies/study2/protocol/reasoning_internalization_protocol.schema.json;

studies/study2/protocol/reasoning_internalization_protocol.md;

studies/study2/decisions/reasoning_internalization_protocol_freeze.md;

studies/study2/prompts/stage_p_gate_a_operator_amendment.md;

studies/study2/data/task_bank_manifest.json;

relevant global decision/run logs, methods/limitations ledgers, artifact index, and claim–evidence matrix.

The frozen Stage P protocol, schema, Markdown, review, Gate A rule, task banks, seeds, thresholds, options, prompts, pair definitions, selection rule, models, revisions, scientific classifications, and protected historical bytes are immutable. Do not “clean up,” reformat, regenerate, move, or rewrite them. If Stage T reveals an ambiguity that cannot be implemented without changing a frozen scientific choice, stop as BLOCKED_ON_STUDY2_PREREGISTRATION_INTEGRITY; do not choose a favorable interpretation.

The single formal methods-review allowance is already SPENT_VERIFIED. Engineering tests, static checks, and independent byte verification are required, but do not perform, label, or claim a second methods review and do not create a consolidated protocol correction.

All Study 2 scientific claims remain unsupported / preregistered. Stage T is mechanical qualification, not empirical evidence. Do not add a scientific evidence row. paper/evidence_ledger.csv must remain byte-identical and stop at EV-0016.

3. Execution environment and repository rules

All executable work must run through Azure ACR Tasks/QuickRuns:

no local tests, builds, generators, tokenizer construction, Hugging Face resolution, tokenization, selector execution, or scientific computation;

the laptop/local shell may only inspect and edit text, perform Git/SHA operations, submit ACR work, and retrieve/read ACR-produced results;

use the existing Azure AAD/managed-identity and ACR route; do not introduce PATs, admin passwords, SAS, shared keys, embedded credentials, public storage, GHCR, or a second registry path;

GitHub is Git transport only: no GitHub Actions/workflows, PRs, issues, releases, artifacts, Packages, Codespaces, or GHCR;

pushes must be non-force fast-forwards to main; no rebase, commit amendment, history rewrite, or tag movement;

keep the worktree clean at every published checkpoint and verify HEAD == origin/main after each push.

Stage T does not need a GPU. ACR CPU QuickRuns are appropriate. GPU jobs, weight downloads, weight loads, model forwards, generations, provider calls, lens operations, activations, probes, patching, and ablations must all remain exactly zero. GPU cost is not a later design constraint, but that authorization does not broaden Stage T.

4. Fixed identities and permitted downloads

Resolve only the configuration and tokenizer assets for these exact identities:

Role

Model ID

Immutable revision

target

deepseek-ai/DeepSeek-R1-Distill-Qwen-1.5B

ad9f0ae0864d7fbcd1cd905e3c6c5b069cc8b562

lineage base

Qwen/Qwen2.5-Math-1.5B

4a83ca6e4526a4f2da3aa259ec36c259f66b2ab2

instruction control

Qwen/Qwen2.5-Math-1.5B-Instruct

aafeb0fc6f22cbf0eaeed126eff8be45b0360a35

For every role, record the requested revision, resolved immutable revision, model/config identity, tokenizer class, config/tokenizer filenames, byte counts, SHA-256 hashes, special-token map, BOS behavior, dependency-lock hash, Python/platform identity, ACR source commit/tree, base image digest, and ACR run ID in a non-scientific receipt.

Acquisition must use an explicit allowlist containing only files required by AutoConfig and AutoTokenizer. After acquisition, construct from the resolved local snapshot with trust_remote_code=false and local/offline resolution. Fail closed if any model-weight or adapter-weight file is downloaded or present in the staged snapshot, including *.safetensors, pytorch_model*.bin, *.gguf, *.pt, *.pth, weight shards, or weight indexes. Do not instantiate any AutoModel* class and do not import or call a model-forward path.

Do not commit Hugging Face caches or raw snapshot blobs to Git. Commit only the closed receipts, hashes, deterministic tokenization outputs, selected frozen rows, manifests, source, tests, and documentation needed to reproduce Stage T.

An unavailable or mismatched pinned identity, unresolved revision, forbidden remote-code requirement, or config/tokenizer identity inconsistency closes this round as BLOCKED_ON_STUDY2_MODEL_IDENTITY. Do not substitute a newer revision, alias, mirror checkpoint, tokenizer, alphabet, or chat template.

5. Freeze the Stage T implementation before real tokenizer access

Use two explicit subphases.

T0 — static implementation and seal

Before constructing any of the three real tokenizers:

Implement the Stage T runner, strict closed schemas, independent validator, fake-tokenizer fixtures, focused tests, full-suite integration, ACR task definitions, deterministic output writer, and failure receipts.

Bind all inputs to the frozen hashes above. The runner must refuse any source, bank, prompt, pair, protocol, or dependency drift.

Make the canonical core outputs deterministic: UTF-8, LF, canonical JSON, sorted keys, no NaN/Infinity, no timestamps/run IDs/cache paths in hash-compared core files, exact row counts, duplicate/missing/unknown-field rejection, and manifest written last.

Test adversarially with fake tokenizers: option splitting, shared option IDs, prefix retokenization, decode drift, unexpected BOS, prompt mutation, answer-position mismatch, unresolved/partial wrong-position spans, unequal pair lengths, selector contamination by outcome fields, fewer than 128 eligible pairs, duplicate rows, partial output, non-last manifest, and weight-file contamination.

Commit and fast-forward-push the T0 implementation. Run focused and full tests in ACR only. The full suite may retain exactly the two disclosed historical parser-seal failures and no others. Report the new passed/skipped/failed counts and delta from Stage P’s 3,537 passed / 15 skipped / 2 historical failures.

Seal the exact T0 source commit/tree plus source/schema/task hashes in a pre-execution receipt before the first real tokenizer is constructed.

After the first real pinned tokenizer has been constructed, do not modify any tokenizer rule, eligibility rule, option surface, BOS rule, anchor rule, sort key, selected count, output schema, or gate/blocker mapping in this authority. Only exact-source operational retries allowed by the reliability contract may occur. A discovered semantic implementation defect requires a stop and new operator authority; it may not be patched after observing Stage T mechanics.

T1 — real pinned identity/tokenizer gate

Run the sealed T0 implementation in ACR against the exact three identities. For a claimed PASS, require an independent second clean ACR execution from the same source commit, with an independently populated tokenizer cache and a different fixed PYTHONHASHSEED. Deterministic core artifact hashes and selected-pair hashes must match byte-for-byte. ACR/run metadata may differ only in separate attempt receipts and may not enter the deterministic core hash.

Use immutable shard manifests and idempotent attempt IDs. Perform capacity/network preflight. Retries must be finite and restricted to closed operational reason codes. Checkpoints are create-only; resume must not duplicate contributions. Reject duplicate, missing, out-of-shard, or mixed-attempt rows. A failed shard cannot shrink a denominator. Merge only a complete pack and write its manifest last.

6. Exact prompt and option-token gate

Read the existing frozen prompt bytes; never rerender or regenerate them. Cover every relevant prompt arm in both behavioral banks and every primary/control NT prompt in both mechanistic candidate banks. Verify each stored prompt hash before tokenization.

For each of the three pinned tokenizers:

Use the identical raw UTF-8 prompt bytes across models. Do not call apply_chat_template; do not add system/user/assistant wrappers, <think>, an empty-think prefill, an answer placeholder, or generated text.

Freeze the tokenizer’s actual special-token/BOS behavior. The only model-specific prompt-transport difference allowed is tokenizer-required BOS behavior. Do not manually add a model-specific natural-language prefix.

For each literal continuation surface  A,  B,  C, and  D, prove that appending it to every exact prompt adds exactly one token, preserves the complete prompt-token prefix, produces four pairwise-distinct option token IDs, and round-trips byte-exactly to the prompt plus that literal surface when the registered BOS/special-token behavior is accounted for.

Record the exact token IDs, decoded option surfaces, special-token/BOS behavior, full input token IDs or their closed row-level representation, input length, input-ID SHA-256, prompt SHA-256, and answer_position_index for every model/prompt/arm row. The answer position is the final input position whose logits would predict the option continuation; do not run those logits.

Use exact, cleanup-disabled decoding and reject normalization, whitespace loss, suffix retokenization, multi-token options, duplicated option IDs, prompt round-trip drift, truncation, or implicit chat templating.

If any required prompt/option row fails for any tokenizer, seal the complete finite diagnostic pack and stop as BLOCKED_ON_STUDY2_COMMON_OPTION_TOKENIZATION. Do not replace the alphabet or remove failed prompts.

7. Exact mechanistic token support and pair selection

For every candidate pair in both mechanistic roles, examine the primary donor, primary recipient, no-op recipient/donor, same-intermediate donor, same-answer/different-intermediate donor, and deterministic random donor using each of the three pinned tokenizers.

A candidate is eligible only if, for every pinned tokenizer:

every required prompt hash and raw UTF-8 byte string matches the frozen row;

donor, recipient, and all controls have exactly equal input length under that tokenizer;

their answer positions are aligned under that tokenizer;

the recipient Start: state-symbol byte span from the frozen wrong_position_anchor resolves unambiguously to an exact token boundary and one frozen token index;

no prompt is truncated and no unregistered special/prompt token is inserted beyond the frozen BOS behavior.

Eligibility is the conjunction across all three tokenizers. Cross-model absolute indices may differ only as implied by the recorded tokenizer-required BOS behavior; within each model, all pair objects must align exactly. Record all per-model lengths, answer indices, wrong-position indices, token IDs/hashes, eligibility booleans, and closed reason codes.

Then, without logits or any outcome field:

split candidates into the eight fixed cells: role (mechanistic_development, mechanistic_candidate_confirmation) × family (permutation_chain, affine_mod10) × depth (2, 3);

sort each cell in ascending pair_semantic_id exactly as frozen;

select the first 128 jointly eligible candidates in each cell;

do not backfill after inference, optimize balance, consult correctness, or substitute a candidate for any favorable property;

preserve selected pair rows byte-exactly from their frozen source bank and place any Stage T annotations in a separate closed table;

verify and report selected-set counts, templates, recipient labels, state/operator tables, hashes, and source row positions. If the deterministic selected set conflicts with a registered invariant, stop on preregistration integrity rather than changing selection.

Exactly 512 pairs must be selected for mechanistic development and 512 for mechanistic confirmation. Fewer than 128 eligible candidates in any one cell stops before any weight load as BLOCKED_ON_STUDY2_MECHANISTIC_TOKEN_SUPPORT. No post-inspection replacement or same-version task generation is allowed.

Also record target-only single-token support for the registered digit/intermediate concept surfaces needed by the future secondary J-lens axis. This diagnostic may make that future secondary axis ineligible or not estimable, but it must not block, select, rescue, promote, or demote the lens-independent Stage T result. Do not open A600, B600, or M1200.

8. Required code and artifact contract

Choose repository paths consistent with existing conventions, but the final implementation must include at least:

one Stage T implementation module under src/jspace_observation/;

one execution entry point and one independent validation entry point under scripts/;

focused fake-tokenizer and integration tests under tests/;

ACR-only task definitions under infra/azure/acr_tasks/;

a closed Stage T JSON Schema or equivalent exact schema contract;

deterministic model/config/tokenizer identity receipts;

a row-level prompt-tokenization pack;

a row-level mechanistic eligibility pack;

byte-exact selected mechanistic-development and mechanistic-confirmation JSONL files;

immutable shard/attempt receipts and complete ACR run ledger;

one deterministic Stage T core manifest written last;

one Stage T decision document and final handoff;

updates to Study 2 README, global decision/run logs, methods/limitations ledgers, claim–evidence matrix, and artifact index without changing scientific support status.

Store authoritative Stage T outputs in the Git repository. Retrieve exact ACR-produced bytes from the ACR output image/layers or the repository’s established ACR artifact route; do not locally regenerate canonical outputs. Local verification may hash and inspect retrieved bytes only.

The deterministic core manifest must bind at least:

Stage T authority identity and T0 sealed source commit/tree;

frozen protocol, bank, handoff, and protected-anchor hashes;

three model/config/tokenizer identities and resolved revisions;

tokenizer/config file hashes and explicit proof that weight files are absent;

dependency lock and base image identity;

all row-pack hashes, byte counts, row counts, schemas, and primary keys;

option-token gate results for every model and required prompt;

candidate eligibility counts and reason counts per model/cell;

the ordered selected pair IDs and selected JSONL hashes for all eight cells;

digit/J-lens eligibility diagnostics as secondary/non-rescuing;

independent-repeat equality;

operation counts and the exact terminal Stage T state.

Do not place mutable timestamps or infrastructure IDs in deterministic core files; put them in separate attempt receipts bound by hash from the final handoff.

9. Terminal-state precedence

Use this fail-closed precedence:

starting commit/tree/ancestry/worktree/protected-byte failure → BLOCKED_ON_STUDY2_STARTING_STATE_INTEGRITY;

frozen protocol ambiguity, schema contradiction, selector/invariant contradiction, or required scientific-byte change → BLOCKED_ON_STUDY2_PREREGISTRATION_INTEGRITY;

pinned config/tokenizer identity unavailable or mismatched → BLOCKED_ON_STUDY2_MODEL_IDENTITY;

required common option continuation gate failure → BLOCKED_ON_STUDY2_COMMON_OPTION_TOKENIZATION;

fewer than 128 eligible mechanistic pairs in any fixed cell → BLOCKED_ON_STUDY2_MECHANISTIC_TOKEN_SUPPORT;

exhausted infrastructure/retry contract → BLOCKED_ON_STUDY2_EXECUTION with exactly one applicable registered reason from NONFINITE_OUTPUT, MISSING_ROW, HASH_MISMATCH, SOURCE_IMAGE_MISMATCH, ARTIFACT_WRITE_FAILURE, RUNTIME_EXCEPTION, or CAPACITY_UNAVAILABLE.

Do not convert an operational blocker into a scientific negative. Do not create a partial scientific classification.

Only if every identity, option, alignment, count, deterministic-repeat, artifact, and validation gate passes, set the nonterminal lifecycle to exactly:

NONTERMINAL_CHECKPOINT_STUDY2_STAGE_T_TOKENIZER_GATE_SEALED_AWAITING_BD_AUTHORITY

That state authorizes nothing further. Do not start B-D, do not run the development bank through any model, do not calculate Gate A, and do not open any behavioral-confirmation output object in this round.

10. Final validation, publication, and operator response

Before closure:

run focused Stage T tests in ACR;

run the full repository suite in ACR and admit only the same two historical parser-seal failures;

run an independent frozen Stage T package validator in ACR over the final candidate commit;

recheck every Stage P and historical protected hash and both Phase 1.0D rollups;

verify paper/evidence_ledger.csv is byte-identical and ends at EV-0016;

verify model-weight downloads, weight loads, forwards, generations, provider calls, GPU jobs, lens operations, activation operations, probes, patching, ablations, Phase 1.0D, RQ2/S4, Gate A, and scientific evidence rows are all zero;

commit the decision/handoff and push only by fast-forward;

verify final HEAD == origin/main, record final tree, and leave the worktree clean.

The final response must lead with the exact terminal state and include:

final HEAD, tree, ancestry, and clean/fast-forward confirmation;

every commit created in order;

Stage T authority/source/schema/core-manifest/decision/handoff hashes and byte counts;

exact model/config/tokenizer resolved identities and snapshot-file hash summaries;

option token IDs and BOS behavior per model;

prompt/tokenization row counts and all failure counts;

eligible and selected pair counts for each of the eight cells, plus selected file hashes;

target-only digit/J-lens token-support status clearly marked secondary and non-rescuing;

every authoritative ACR run ID, source commit/tree, image identity, status, and focused/full/validator result;

deterministic repeat comparison;

protected-state and EV-0016 recheck;

complete operation counts;

a precise statement of what remains unmeasured and that B-D/Gate A still require separate operator authority.

Do not describe Stage T as evidence that the target can reason, that distillation created a mechanism, or that J-space is validated. A successful Stage T establishes only that the frozen interface and fixed mechanistic pairs are mechanically executable under the three pinned tokenizers.