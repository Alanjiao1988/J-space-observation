# Study 2 Stage P Gate A operator amendment

## 1. Authority and scope

The operator delegated the choice among the three paths recorded in
`studies/study2/protocol/stage_p_operator_amendment_required.md` and then
instructed execution to continue. Gate A, family-composition qualification, is
selected.

This is an additive authority. It does not edit, replace, or weaken
`studies/study2/prompts/stage_p_protocol_design_prompt.md`, whose identity
remains 53,018 bytes and SHA-256
`1408c5ae4d09a097c70b0e984150c4947e527ca12b5614905a98b65685ed0b37`.
Every original rule remains in force except the narrow B-D feasibility
amendment below.

Amendment starting candidate:

- commit: `def1e743ba68744adff208eb8bcede18e7fa0b32`;
- tree: `a699c84077068348e9023030cefdb0247f04f2f0`;
- canonical lifecycle: `CANDIDATE_AWAITING_REVIEW`;
- formal Stage P review allowance: `UNSPENT`;
- Study 2 tokenizer/model/lens/activation/probe/patching/GPU operations: zero.

This amendment authorizes Stage P to incorporate and review the future Gate A
contract. It does not authorize Stage T, B-D, B-C, model loading, tokenizer
construction, or any empirical execution now.

## 2. Exact amended stage boundary

The stage order remains P -> T -> B-D -> B-C -> M-D -> M-C. B-D gains one
fixed, disposable feasibility decision after implementation validation and
before any B-C object is opened.

Gate A is not permission to revise the protocol. It can only open B-C or close
the current protocol version.

## 3. Gate A inputs

After a later Stage T authority has sealed all model and tokenizer identities,
common option-token support, input lengths, answer positions, and mechanically
eligible pair hashes:

1. Run the complete fixed development bank for target, lineage base, and
   instruction control on the same tasks and registered arms.
2. Keep the behavioral-confirmation bank unopened until the Gate A decision and
   its manifest are sealed.
3. Use only target NT rows at depths 2 and 3 for the gate decision.
4. Within each family, pool the fixed 64 depth-2 and 64 depth-3 rows, giving
   exactly 128 rows per family.
5. Do not select a depth, template, item, task, model, or favorable subgroup.
   Control outcomes are reported but cannot affect Gate A.

## 4. Exact Gate A statistic

For each family independently, let `X` be the target's number of restricted-
option-correct NT rows among the fixed 128 compositional development rows.

Use the exact one-sided binomial upper tail under chance:

`p_exact = sum_{j=X}^{128} C(128,j) * 0.25^j * 0.75^(128-j)`.

The family passes only when all are true:

- exactly 128 registered rows are present;
- every row is finite and execution integrity is complete;
- all registered option, template, state, and operator balance invariants hold;
- `p_exact <= 0.025`.

The exact integer form is `X >= 43`; `X=42` does not pass. The overall Gate A
passes only when both families pass. No multiplicity fallback, pooled rescue,
depth fallback, or control-model rescue is allowed.

## 5. Gate outcomes

If Gate A passes:

- seal the complete development pack and Gate A decision manifest-last;
- preserve all development rows;
- B-C remains unopened until a separate authority explicitly opens it;
- no task, seed, threshold, arm, model, pair, or scientific rule changes.

If Gate A fails for either family:

- set the non-scientific operational closure state to
  `STUDY2_PROTOCOL_V1_CLOSED_ON_DEVELOPMENT_FEASIBILITY`;
- do not open B-C or any mechanistic stage;
- do not backfill, replace, pool away, or reinterpret the failed family;
- any further attempt requires a new protocol version, new operator authority,
  and new task-bank seeds.

Gate A is a protocol/interface feasibility decision, not a Study 2 scientific
result. It receives no scientific evidence row and cannot be reported as
support for or against internal reasoning, distillation association, or a
J-space.

## 6. Common-support and reliability rules

Stage T pair eligibility remains based only on common mechanical support from
all three fixed tokenizers and frozen hashes. Every model still receives the
same tasks, target-defined cells, pairs, and target-defined window. Correctness
cannot define an intersection, and poor control performance cannot remove a
row.

Every later execution authority must require:

- an immutable shard manifest;
- idempotent attempt IDs;
- capacity preflight;
- finite, reason-code-bound retries;
- create-only checkpoints and verified resume;
- failed-shard isolation without denominator shrinkage;
- duplicate, missing, and out-of-shard row rejection;
- complete merge with manifest written last;
- strict separation of infrastructure failure from scientific negatives.

GPU cost after Stage T is not a primary design constraint. No later authority
may reduce samples, registered repetitions, or critical controls to save GPU
cost.

## 7. Advisory boundaries

Any Claude Study 1 failure analysis remains
`ADVISORY_POST_HOC_METHODS_INPUT`: no EV identifier, no Study 1 state change,
and no unverified numerical fact.

The protected four-scale J-lens engineering trajectory remains
`EXPLORATORY_METHODS_OBSERVATION_AWAITING_CONTROLLED_VALIDATION`. It makes no
publication or novelty claim, carries no causal attribution to
`max_seq_len=128`, and cannot select or alter a primary task, cell, pair, layer,
window, or causal metric.

## 8. Stage P completion

Stage P may now update the candidate package, regenerate its model-free banks,
run ACR validation, consume the single formal methods review, apply at most one
consolidated correction, and freeze if every original and amended integrity
gate passes.

The successful Stage P endpoint remains:

`NONTERMINAL_CHECKPOINT_STUDY2_PROTOCOL_FROZEN_AWAITING_TOKENIZER_GATE_AND_EXECUTION`

Stage T remains separately authorized and must not start in this round.
