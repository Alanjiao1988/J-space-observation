[project]
name = "jspace-observation"
version = "0.1.0"
description = "J-lens based hidden workspace observation for R1 distilled small models"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
