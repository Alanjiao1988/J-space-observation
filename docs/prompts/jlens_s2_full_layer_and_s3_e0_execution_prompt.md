You are operating the public repositoryAlanjiao1988/J-space-observation as the sole implementation and Azureexecution agent. Treat this document as the complete operator authority forthis round.

The round has two strictly ordered goals:

produce and byte-seal the exact full-layer S2 identities A600, B600, andM1200 required by the already frozen J-lens S3 validity protocol; then

only after all three identities are sealed, execute the frozen S3 Stage E0once and seal its eligibility/split manifest.

Do not execute S3 E1/E2, any validity readout, intervention, patching, or RQ2.Do not resume or redesign Phase 1.0D semantic review.

1. Controlling interpretation

The exact starting Git authority is:

origin/main = 72336f822a8ffdbd2e0caf40f4a62c68cce68156
tree        = d0592ae0b0edb62b4f082c0a12a9bcafe5693ee5

The simultaneous project states are:

NONTERMINAL_CHECKPOINT_JLENS_S3_VALIDITY_PROTOCOL_FROZEN_AWAITING_S2_LENSES_AND_EXECUTION
BLOCKED_ON_SEMANTIC_REVIEW_TRANSPORT_CAPACITY

The S3 protocol is already reviewed and frozen. Its methods-review allowanceis spent. This round may execute frozen requirements; it may not reinterpret,repair, relax, or replace them.

The following conclusions are controlling:

Stage E0 is not an expendable feasibility probe. It is the formal,create-only S3 eligibility/split stage.

The frozen S3 protocol requires the S2 full-layer lens identities before S3execution. Therefore no benchmark tokenizer operation or clean next-tokenpass may occur until A600, B600, and M1200 are byte-sealed.

A600 and B600 are exactly independent 600-sequence fits. M1200 isexactly their official n_prompts-weighted merge. Do not substituteA800/B800, A1000/B1000, a selected replicate, or any later refit.

Matrix convergence is an engineering diagnostic. It must be measured andreported, but no Frobenius or cosine threshold may block Stage E0 when theregistered transport, finiteness, save/load, merge, identity, and artifactintegrity requirements pass.

A Stage E0 floor failure is a valid frozen scientific result:INSUFFICIENT_BEHAVIORAL_SUPPORT_FOR_VALIDITY. It is not an execution defectand cannot be repaired with backfill, replacement items, prompt changes, achat template, CoT, synonym growth, or a second E0 run.

2. Exact frozen anchors

Before adding or changing any file, independently hash and verify all of thesecommitted bytes:

Artifact

Required SHA-256

docs/jlens_s3_validity_protocol.json

bb07dc3be90539e88ff8ada8adee879da747ec5b0b0409499b9809f259df4625

docs/jlens_s3_validity_protocol.schema.json

5d6e2fc33771b427130bd1dbe94c79cdf6d5827288b96929352c0caa793acbf1

docs/jlens_s3_validity_protocol.md

d2e851013037a5efa96d7ae06c3d7c9d63466b299d255c75b6c665debf862bff

docs/jlens_s3_validity_protocol_review.md

3ea426e74006098ebedf28e6f71f45e0bc38cc040df652ab1632eb288b07cb6a

validator source bundle

7e837b0cfdb0c9a12eb1b6c9067751c7cd4262cc18c5a6f17f4a6505f25b7410

vendored third-party manifest

9af58768b200488ba28e3522c08624d8273487f6662f0dce5177a04a5f66fffc

S3 design authority

5d39859bc3d75143f3fdcb469de1d199ad7f831d474509b605569cdc9c1814b8

docs/decisions/jlens_s3_validity_protocol_freeze.md

d7d9623e3668b5469b426ba45671f267b631599e44f598f710f6c16564a96b48

Also verify and preserve:

Phase 1.0D protected artifact diff: 0
recovery authority SHA-256: dc350039f118cb5931dab08fd65e24ed169757c472898b7dbe8d27eb3ce2f92b
capacity certificate SHA-256: 20e486e05a5f076b720ca12db3459b5a1c2c42e95684977dfdcff19d6da055d3
capacity manifest SHA-256: 23016ad15430b1720e4b37033a3638bf45e817ac00513292d138d26e0ed0a834
paper/evidence_ledger.csv final row before this round: EV-0014

Verify that D25–D30 remain append-only history and that .dockerignore is therestored frozen byte sequence. If any starting SHA, rollup, ancestor, or treefact differs, stop before any tokenizer/model/GPU operation asBLOCKED_ON_S2_STARTING_STATE_INTEGRITY. Report expected and observed values;do not repair the mismatch under this authority.

3. Operating boundary

3.1 Azure executes; the laptop only edits and orchestrates

The laptop may inspect tracked public files, edit, hash, use lightweight Git,upload source context, submit Azure work, and read bounded results. It must notrun pytest, imports-as-tests, builds, dataset scans, tokenizer loads, modeldownloads, model inference, J-lens fitting/application, activation work, orsustained analysis.

Run tests and deterministic CPU validation in ACR Tasks/QuickRuns. Run everytokenizer/model/J-lens operation in Azure. T4 is authorized. A larger Azure GPUis authorized only if the prospectively defined smoke rule below establishesthat T4 cannot complete the exact registered configuration safely. Cost mustbe measured and reported, but cost is not an acceptance gate.

3.2 GitHub is transport and durable audit history only

Allowed: inspect, clone/fetch, create small tracked source/data/protocol/auditfiles, commit, and fast-forward push to main.

Forbidden: GitHub Actions/workflows, pull requests, issues, releases, GitHubPackages/GHCR, Codespaces, amend, rebase of published commits, force-push, orhistory rewriting. Do not change .github/workflows/**.

Use small reproducible checkpoints. Before every push, fetch origin/main andrequire the local branch to be its fast-forward descendant. Preserve unrelateduser changes. End with a clean worktree.

3.3 Protected bytes and resources

Do not edit any frozen S3 file or its source-bundle component. Do not edit oldPhase 1.0D protocols, prompts, rubrics, addenda, protected manifests, source,images, decisions, or terminal archives. Append new S2/E0 decisions and ledgerentries only.

Do not delete or mutate existing Azure accounts, deployments, identities,roles, Jobs, executions, locks, images, or Blob objects. Use distinct S2/E0resource names and prefixes. Retain every execution and immutable image createdin this round. No Phase 1.0D provider call, recovery Job, capacity mutation, orsemantic-review call is authorized.

4. Stage order and irreversible boundary

Apply this order exactly:

S2-G0  starting-state verification
S2-P0  exact S2 protocol and artifact-schema freeze
S2-P1  deterministic corpus acquisition, role assignment, and byte freeze
S2-I0  implementation and ACR-only deterministic tests
S2-T0  bounded all-layer timing/memory/numerical smoke on smoke-only rows
S2-F0  production arm A/B shard fits
S2-M0  cumulative merges, A600/B600, and M1200
S2-V0  independent artifact verification and byte seals
S3-E0  one formal frozen eligibility/split execution
S3-V0  independent E0 pack verification and terminal decision

No stage may consume a later-stage signal. In particular:

no official S3 benchmark item may be tokenized or sent through the targetmodel before S2-V0 has sealed all three full-layer identities;

no A/B prompt, shard, checkpoint, layer, dim_batch, or resource may beselected using any S3 benchmark behavior;

no lens may be loaded or applied during E0;

no E1/E2 output may be computed or opened in this round.

Once the first production S2 shard processes a registered A/B sequence, thecorpus roles, model/tokenizer identities, source/target layers, estimator,sequence length, skip rule, shard membership, checkpoint definitions, andmerge semantics are immutable. Only byte-identical deterministic resume of aregistered shard is allowed after that boundary.

Once E0 acquires its create-only execution lock, it is the sole formal E0 run.No retry is allowed after any complete E0 model result exists. An executionfailure before an all-or-nothing manifest is sealed is an operational blocker,not a scientific classification, and needs new operator authority.

5. Exact S2 scientific and implementation identities

Freeze these identities in a canonical JSON protocol and a closed JSON schemabefore S2-T0:

target model: deepseek-ai/DeepSeek-R1-Distill-Qwen-1.5B
target revision: ad9f0ae0864d7fbcd1cd905e3c6c5b069cc8b562
model parameter dtype: torch.float16
evaluation mode: true
use_cache: false
trust_remote_code: false

J-lens repository: https://github.com/anthropics/jacobian-lens.git
J-lens commit: 581d398613e5602a5af361e1c34d3a92ea82ba8e
HF adapter force_bos: true
compile: false

source layers: 0..26 inclusive
target layer: 27
max_seq_len: 128
skip_first: 16
aggregation: official per-prompt mean and cross-prompt arithmetic mean
official target-position semantics: current-and-future positions as implemented at the pinned commit

arm A: exactly 600 unique registered sequences
arm B: exactly 600 different unique registered sequences
heldout: exactly 200 different unique registered sequences
primary lens: official n_prompts-weighted merge A600 + B600 = M1200

Use the pinned upstream implementation's property that one retained graph andthe same backward passes return gradients for every registered source layer.Do not fit layers separately and do not extrapolate three-layer timing as a27-fold or nine-fold multiplier. Measure the real full-layer path in S2-T0.

dim_batch is an engineering parameter, not a scientific selection signal.Before the smoke, freeze a deterministic selection procedure over candidates[8, 4, 2, 1] in descending order:

run each candidate in a fresh process on the same smoke-only sequence(s);

require shapes for all layers 0..26, finite float32 matrices, no OOM, exactregistered identities, and peak reserved memory no greater than 92% of thedevice's reported total memory;

compare each successful candidate with the dim_batch=1 reference on thesame smoke row and report per-layer max-absolute, relative-Frobenius, andcosine differences;

require every layer to have max-absolute difference no greater than 1e-5,relative-Frobenius difference no greater than 1e-5, and flattened cosineat least 0.999999 against the dim_batch=1 reference;

choose the largest candidate that passes every frozen engineering check.

If dim_batch=1 itself cannot complete the exact full-layer 128-token path onT4, record the failure and repeat the same frozen candidate procedure on anavailable larger Azure GPU. Do not reduce sequence length, skip layers, changethe target layer, change dtype, or switch model revision. If no authorizedAzure GPU can execute the exact path, stop asBLOCKED_ON_S2_RUNTIME_COMPATIBILITY before production fitting.

6. Deterministic public corpus

Use the public dataset already used by the pinned upstream example:

dataset: Salesforce/wikitext
configuration: wikitext-103-raw-v1
split: train

Resolve an immutable Hugging Face dataset commit/revision and its actuallicense bytes before loading the target tokenizer. Record the dataset commit,file identities, row identities, library versions, and content hashes. Do notuse a floating main revision in any production artifact.

Follow the upstream example's source-row eligibility baselinelen(text.strip()) >= 600, plus all of these prospectively fixed rules:

source text is the exact dataset string; do not paraphrase, clean, translate,concatenate, or generate it;

after the pinned adapter's force_bos=true behavior, the exact untruncatedtokenization must contain at least 128 tokens;

the actual fit sequence is exactly the first 128 token IDs that the pinnedadapter supplies to the model;

reject duplicate 128-token ID sequences;

reject any candidate with an exact registered prompt-byte overlap againstthe Phase 1 bank, parser/evaluator fixtures, RQ2 material, or the threevendored official S3 benchmark files, using one frozen symmetricsubstring/equality rule defined before scanning;

do not inspect model logits, losses, activations, Jacobians, or S3 behaviorwhile selecting corpus rows.

Freeze one literal assignment seed before scanning. For every eligible uniquerow compute a role key from the seed, immutable dataset row identity, exact rawtext hash, and exact 128-token-ID hash. Sort ascending by the hexadecimal keywith immutable row ID as the collision-only tie-break. Assign withoutreplacement:

first 600   -> A
next 600    -> B
next 200    -> heldout
next 2      -> smoke-only

If 1,402 eligible unique rows cannot be formed, stop before fitting asBLOCKED_ON_S2_CORPUS_FREEZE. Do not relax a filter or substitute a datasetunder this authority.

Track exact corpus bytes in a clearly licensed repository directory, includingraw row identity, role, exact source text or a losslessly reproducible sourcereference, exact input token IDs, token count, raw-text SHA-256, token-IDSHA-256, role key, and exclusion audit. Include the dataset's attribution andshare-alike/license notice. If repository licensing prevents tracking exacttext, stop for operator direction; do not silently retain only an unverifiableremote pointer.

The two smoke-only rows are excluded from A, B, heldout, every official S3role, and all paper statistics except runtime/memory compatibility.

7. Exact shards and cumulative checkpoints

Each A/B sequence must be fitted exactly once in the successful productionpath. Define the scientific cumulative checkpoints for both arms as:

n = 64, 128, 256, 600

Their fixed contiguous role-order increments are:

1..64       size 64
65..128     size 64
129..256    size 128
257..600    size 344

The final 344-sequence increment may be divided into smaller operationalsubshards only by a deterministic planner frozen before production. Derive amaximum subshard size from the observed smoke wall time, the exact Azurecontainer timeout, and a predeclared safety factor. The planner may reduce Jobwall-time risk; it may not change role order, cumulative checkpoints, totalweight, sequence inclusion, or the estimator.

Parallel Azure execution of independent registered subshards is allowed whenGPU quota permits. Never duplicate a successful subshard to estimate variance.An infrastructure-failed subshard may resume only from its own exact checkpointunder the same image digest, corpus bytes, configuration, and shard identity.Preserve every failed attempt and disclose whether any sequence was recomputed.

At each cumulative checkpoint, use the pinned officialJacobianLens.merge() semantics. Independently recompute then_prompts-weighted float32 matrix mean and compare it with the officialmerge. Do this for A64/B64, A128/B128, A256/B256, A600/B600, and M1200.

8. Canonical artifacts and operational gates

Create a distinct immutable S2 image and provenance bundle. The final imagemust be addressed by digest and have tag/manifest write and delete disabled.

Use create-only Azure Blob prefixes for:

corpus acquisition and role manifest;

smoke attempts and selected engineering configuration;

every A/B subshard and resumable checkpoint;

cumulative A64/A128/A256/A600 and B64/B128/B256/B600 lenses;

canonical M1200 lens;

heldout apply diagnostics;

independent verification and final S2 manifest;

later, a distinct Stage E0 lock/output/manifest prefix.

Do not put large lens binaries into ordinary Git history. Commit exact publicmanifests, hashes, schemas, summaries, bounded logs, provenance, and retrievalcoordinates so every large Blob artifact is independently identifiable andreconstructible. Keep all small experiment/audit artifacts in the repository.

The canonical A600, B600, and M1200 files must use the project's existinglossless float32 J-lens serialization path, not the upstream default fp16 saveas the sole identity. For each canonical lens require:

n_prompts exactly 600, 600, and 1200 respectively;

source layers exactly [0, 1, ..., 26];

target layer provenance exactly 27;

every matrix shape equals the pinned model's (d_model, d_model);

every element is finite;

save/load max-absolute difference exactly 0 for the canonical lossless path;

independent official-merge recomputation passes the frozen max-absolute andrelative-Frobenius engineering tolerances;

Blob create-only readback bytes, size, and SHA-256 match the repo manifest;

source model, tokenizer, upstream code, corpus, shard, container, and Azureexecution identities are complete.

These are operational gates. Failure produces no S3 scientific result andmust stop before E0 as BLOCKED_ON_S2_ARTIFACT_INTEGRITY.

9. Required S2 measurements

For every arm checkpoint pair at n=64, 128, 256, and 600, report every layer0..26 and a registered across-layer summary for:

A versus B relative Frobenius distance;

A versus B flattened cosine;

finite rate;

official merge versus independent weighted recomputation;

lossless save/load identity;

fitted prompt count and sequence identities;

wall time, seconds per prompt, GPU type, peak allocated/reserved memory, andestimated/actual Azure cost.

Fit and report the exponent and residuals for the already registered scalinghypothesis without forcing exponent 1/2. Also report the prediction based onC approximately 1.7, clearly labeled as a prior diagnostic rather than anacceptance criterion.

Run heldout engineering apply diagnostics on all 200 registered heldoutsequences for A600, B600, and M1200 using a metric contract frozen before thefirst heldout lens application. Reuse the project's existing technicaltop-k/rank/logit stability definitions where compatible. These metrics areengineering diagnostics only. They cannot select a replicate, alter M1200,block Stage E0, or support semantic/J-space claims.

Regardless of the measured 600-vs-600 Frobenius/cosine result, proceed to E0when and only when every operational gate in section 8 passes.

10. S3 Stage E0 — exact frozen execution

S3-E0 requires a new, create-only lock that binds:

the canonical frozen S3 protocol/schema hashes;

all vendored upstream benchmark hashes;

target model/tokenizer revision and parameter dtype;

exact A600/B600/M1200 byte hashes and S2 manifest hash;

E0 executing image digest and source bundle;

exact row order and expected item counts 93/55/90;

the frozen E0 output schemas and manifest destination;

zero pre-lock benchmark tokenizer/model operations in this round.

Only after independent readback verifies that lock may the sole E0 executionstart.

Execute exactly $.stages.E0 from the frozen protocol:

pinned tokenizer mechanical eligibility;

exactly one clean next-token forward pass per official item;

exact raw official prompt bytes;

no chat template and no generated CoT;

greedy top-1 only;

clean-behavior eligibility;

frozen distribution-local hash split;

create-only e0_item, e0_surface, and eligibility/split manifest;

manifest written last after an all-or-nothing independent verification.

Do not load, fit, merge, apply, inspect, or rank any J-lens during E0. Thesealed A600/B600/M1200 hashes are prerequisites only. Do not compute logit-lensreadout, activation output, intervention, ablation, patching, development E1,or confirmation E2.

Implement the exact frozen normalization, complete-surface, tokenizer-boundary,prompt-leakage, target-overlap, single-token, over-length, control-position,clean-top-1, exclusion-count, and split rules. Do not use the informal offlinecounts that motivated this round. In particular, do not treat a substringinside an alphanumeric word as token-bounded, and do not exclude an item merelybecause one of several registered candidate surfaces was removed.

Compute the four frozen floor booleans from sealed rows:

multihop confirmation >= 20
order_ops confirmation >= 20
pooled readout confirmation >= 50
causal_swap confirmation >= 30

Remember that 15 eligible items per distribution are assigned to developmentbefore the confirmation remainder. The pooled readout floor is not redundant:it couples multihop and order_ops. Do not replace the frozen counts with asingle informal accuracy threshold.

If any floor fails, seal the complete E0 pack and return exactly:

INSUFFICIENT_BEHAVIORAL_SUPPORT_FOR_VALIDITY

No backfill, replacement batch, alternative prompt mode, surface growth,threshold change, or second E0 is authorized.

If every floor passes, seal the complete E0 pack and return:

NONTERMINAL_CHECKPOINT_JLENS_S3_E0_SEALED_AWAITING_E1_E2_EXECUTION

This state does not validate the lens and does not authorize E1/E2 or RQ2.

If E0 suffers an operational failure before a complete all-or-nothing pack issealed, preserve partial bytes under the registered partial prefix, write noscientific classification, and return:

BLOCKED_ON_JLENS_S3_E0_EXECUTION_INTEGRITY

11. Implementation and tests

Prefer extending existing project modules and using the vendored upstreamsource rather than duplicating fitting or S3 normalization logic. Keep S2production code separate from frozen S3 protocol bytes.

Before any production GPU fit, add deterministic tests for at least:

exact starting hash/rollup verifier;

immutable dataset revision and licensed corpus manifest;

deterministic role-key sorting and exact 600/600/200/2 disjoint counts;

exact token-ID uniqueness and exclusion-audit reconstruction;

no overlap with Phase 1, parser/evaluator, RQ2, or official S3 roles;

full source-layer set 0..26 and target 27;

all-layer jacobian_for_prompt smoke shapes and finite matrices;

dim_batch selection-rule determinism;

operational subshard planner determinism;

exactly-once successful sequence accounting across shards/resume;

cumulative official merge and independent weighted recomputation;

exact A600/B600/M1200 identities and lossless save/load;

create-only Blob/manifest-last behavior and independent readback;

E0 precondition rejects missing or unsealed S2 identities;

E0 lock rejects any pre-lock benchmark tokenizer/model operation;

all frozen E0 normalization, surface, token-boundary, eligibility, split,count, pooled-floor, no-backfill, and manifest schemas;

E0 cannot import or invoke a lens/readout/intervention path;

E0 floor-fail, floor-pass, and operational-blocker terminal branches;

protected Phase 1.0D and frozen S3 byte regression checks.

Use synthetic/fake tests for branch coverage. ACR tests must not accidentallyload the real model, tokenizer, dataset, or GPU unless they are the explicitlyregistered smoke/production Jobs.

Run focused suites and the full repository suite in ACR. The authority baselineis:

3427 passed / 15 skipped / 2 disclosed historical parser-seal failures

Report the exact delta. No new failure is acceptable. Do not edit the twohistorical parser-seal tests or frozen inputs to manufacture a green suite.

12. Commit and evidence policy

Recommended fast-forward checkpoints are:

commit this controlling prompt verbatim;

add/freeze S2 protocol, schema, corpus-source contract, and tests;

freeze exact corpus bytes/roles and pre-production image provenance;

record bounded smoke and selected engineering configuration;

record production shard and cumulative-lens manifests;

seal A600/B600/M1200 and S2 verification;

add/freeze E0 source/image/lock provenance before launch;

record the complete E0 terminal pack and final verification;

append final ledgers and handoff.

Do not commit claims ahead of the corresponding sealed evidence.

If S2 completes, append one new engineering-only evidence row after EV-0014for the full-layer scaling run. It may report measured convergence disagreementor agreement, transport, and artifact facts. It may not promote CL-02, CL-07,hidden reasoning, internal workspace, or J-space claims.

If E0 completes, append a distinct evidence row only for the selected-casebehavioral eligibility/floor result. State explicitly that E0 contains no lensoutput and is not evidence of J-lens validity or model reasoning. If E0 isoperationally blocked without a complete pack, do not add a scientific resultrow.

Append new decision/method/limitation/artifact/run-log entries. Never edit olddecision conclusions. At minimum preserve these limitations:

WikiText is a public Wikipedia-derived proxy for a pretraining-like corpus,not the target model's known training distribution;

two A/B fits provide one independent difference trajectory, not a samplingdistribution;

matrix convergence and heldout apply stability are engineering evidence;

E0 estimates only eligibility on the selected official public items;

public benchmark labels are not private or researcher-blind;

no E0 or future S3 result changes the independent Phase 1.0D capacity block;

no result in this round establishes hidden reasoning, an internal workspace,or a J-space.

13. Zero-operation assertions outside scope

At final handoff report exact counts and require zero for:

Phase 1.0D recovery/provider calls, Jobs, locks, labels, and decisions;

changes to any Phase 1.0D deployment, quota, concurrency, retry client, orreviewer instrument;

changes to frozen S3 protocol/schema/Markdown/review/freeze/source-bundlebytes;

S3 lens readout, logit-lens readout, activation extraction for E1/E2,intervention, coordinate swap, ablation, patching, bootstrap, or validityclassification other than the E0 floor result;

RQ2/S4 runs;

GitHub Actions/workflows and destructive cleanup.

Do not claim zero tokenizer/model/J-lens operations globally: this roundexplicitly authorizes S2 fitting and, after its seal, one E0 model pass perofficial item. Report those actual counts precisely by stage.

14. Allowed terminal states

Return exactly one primary terminal state:

INSUFFICIENT_BEHAVIORAL_SUPPORT_FOR_VALIDITY
NONTERMINAL_CHECKPOINT_JLENS_S3_E0_SEALED_AWAITING_E1_E2_EXECUTION
BLOCKED_ON_S2_STARTING_STATE_INTEGRITY
BLOCKED_ON_S2_CORPUS_FREEZE
BLOCKED_ON_S2_RUNTIME_COMPATIBILITY
BLOCKED_ON_S2_GPU_CAPACITY
BLOCKED_ON_S2_EXECUTION_INTEGRITY
BLOCKED_ON_S2_ARTIFACT_INTEGRITY
BLOCKED_ON_JLENS_S3_E0_EXECUTION_INTEGRITY

Do not return JLENS_NOT_VALIDATED, JLENS_PARTIALLY_VALIDATED, orJLENS_VALIDATED_FOR_RQ2_PILOT; all require a later complete E1/E2 execution.Do not return a Phase 1.0D headroom result.

15. Final handoff

The final response must include:

final origin/main commit and tree, ancestor proof, fast-forward push proof,clean-worktree proof, and the ordered commit list;

every frozen S3 and Phase 1.0D anchor re-hash result;

S2 protocol/schema/corpus/image/source-bundle hashes;

immutable WikiText dataset revision, license, selection seed, exclusioncounts, and exact A/B/heldout/smoke role counts;

every smoke attempt, GPU identity, dim_batch result, memory, timing, chosenconfiguration, and predicted versus actual production GPU-hours/cost;

every shard/attempt/resume identity and whether any sequence was recomputed;

A64/A128/A256/A600, B64/B128/B256/B600, and M1200 object hashes, sizes,shapes, source layers, prompt counts, merge/save-load checks, and manifests;

all per-layer convergence diagnostics and fitted scaling-model results,explicitly marked non-gating;

heldout engineering diagnostics;

S2 independent verification receipt and three canonical lens byte seals;

proof that no benchmark tokenization/model call preceded the S2 seal;

E0 lock, execution, exact tokenizer/model call counts, item/exclusion counts,per-distribution eligible/development/confirmation counts, all four floorbooleans, E0 manifest hash, and independent readback;

confirmation that E0 loaded/applied no lens and produced no E1/E2 output;

final focused and full ACR results and exact baseline delta;

all new Azure resources retained and every old resource unchanged;

new evidence/method/decision/limitation/artifact ledger identifiers;

the exact primary terminal state and the smallest next independentlyauthorized gate;

a precise statement of what the round establishes and does not establish.

Do not summarize away a failed shard, retry, partial prefix, numericaldisagreement, floor failure, or limitation. Preserve and report it.