Take over the repository:

https://github.com/Alanjiao1988/J-space-observation.git

Default branch:

main

Verified remote baseline reported by the preceding round:

origin/main = a99d1e8341af85b8db32cb97a46ed5095c3f7978

Execute:

Cloud-native private boundary, independent parser-v3-v2 construction and sealing, post-seal preregistration, and one-shot parser-v3 evaluation

0. Operator intent and supersession

This prompt replaces the earlier infrastructure-only Phase 1.2H-R2 prompt.The operator explicitly authorizes a much larger staged advance. Do not stopmerely because the private-review boundary has qualified. If every precedinggate is green, continue in the same authorized round through:

provisioning and runtime qualification of the private semantic-review andegress-controlled worker boundary;

independent private repair/construction of exactly one eligibleparser-v3-v2 set;

complete blind review, arbitration, replacement, private audit, andcreate-only sealing;

authenticated seal-time listing, one-time final contract compilation, andcontent-free receipt publication;

post-seal preregistration and immutable single-launch evaluation readiness;

Stage P prediction generation and create-only prediction sealing;

Stage E label opening and the one formal parser-v3 evaluation;

content-free result publication and terminal project-state closure.

No new operator confirmation is required between these internal gates. Theauthorization is conditional: each gate must be mechanically established fromactual evidence before the next gate starts. A blocked or invalid result is anhonest outcome and must stop further progression.

This expanded authorization supersedes only the earlier phase/cost limits thatsaid to stop after synthetic boundary qualification or before formalevaluation. It does not supersede any scientific, privacy, independence,immutability, one-shot, private-access, parser-isolation, protected-artifact,or zero-tolerance principle already registered by the repository.

The operator's laptop has repeatedly overheated and restarted. Treat thecloud-first execution rules below as hard requirements. Copilot CLI on thelaptop is an editor, Git client, Azure control-plane submitter, and boundedcontent-free result reader only. The laptop is never the compute host.

1. Target outcomes and terminal states

Use exactly one terminal state in the final result.

PARSER_V3_VALIDATED_READY_FOR_JSPACE_EXPERIMENT

Use only if all of the following occurred exactly once and passed:

the qualified private boundary was bound to the actual deployed worker,image digest, identities, routes/firewall rules, endpoints, storage scopes,model deployments, and executions;

the public construction protocol was frozen before the first privatesemantic read;

exactly one 120-case parser-v3-v2 set passed all admission, quota,ontology, review, arbitration, overlap, provenance, facts, and audit gates;

create-only sealing, authenticated listing, remote byte verification,listing witness, and one-time final contract compilation all passed;

post-seal preregistration froze the exact set, policy, contract, parser,scorer, image, commands, identities, retry rules, and status machine;

the public synthetic rehearsal passed without reading any real v2 label;

Stage P ran once, read inputs but not labels, produced one immutable completeprediction stream, and sealed it before any scoring-label access;

Stage E ran once, opened labels only after the prediction seal, and producedthe unique formal result;

every binding gate in the FINAL Phase 1.2G policy passed, including exactconformance for all 120 mandatory cases;

the formal evaluation ordinal became exactly 1 and cannot be reset;

no comparator influenced status and no post-result tuning occurred;

all cloud checks and final audits have no unresolved BLOCKER or MAJOR.

This status validates parser v3 only for the registered finite conformancesuite. It does not itself establish deployment accuracy, J-space, hiddenreasoning, invisible CoT, or an internal workspace.

PARSER_V3_EVALUATED_NOT_ACCEPTED

Use if the one formal Stage P → prediction seal → Stage E evaluation completedvalidly but any binding acceptance criterion failed. Preserve the result asfinal. Do not repair parser v3, reinterpret a label, replace a case, change athreshold, rerun Stage P/E, or construct another set in this round.

FORMAL_EVALUATION_INVALID

Use if Stage P or Stage E began but the registered one-shot state machine,identity separation, exact inputs, prediction seal, scorer, image, command,completeness, or provenance was violated. Preserve all evidence; do not retryunless the preregistered infrastructure-only retry rule unambiguously permits aretry before any prediction-bearing terminal object or label access occurred.

READY_FOR_SINGLE_LAUNCH_EVALUATION

Use only if the set is sealed and post-seal preregistration/readiness fullypassed, but the formal launch could not begin because of an external Azureavailability/quota/control-plane condition. No Stage P execution, prediction,or label opening may have occurred. This is a technical blocker, not a reasonto change frozen artifacts.

SEALED_READY_FOR_PREREGISTRATION

Use only if set construction and sealing passed but preregistration/readinesscould not be completed without changing a post-seal invariant. No parser run,prediction, or scoring-label read may have occurred.

BLOCKED_ON_PRIVATE_REVIEW_BOUNDARY

Use if no qualifying private model backend plus egress-controlled worker can bedeployed and proven on public synthetic material.

BLOCKED_ON_MODEL_AVAILABILITY_OR_QUOTA

Use if neither a qualifying regional managed-model deployment nor the frozenprivate self-hosted T4/A100 alternative can be provisioned with sufficient livequota. Do not fall back to an unrelated, public, Global, or Data Zone endpoint.

BLOCKED_ON_EGRESS_CONTROL

Use if the actual worker cannot remain healthy while all unapproveddestinations are denied, or if any negative runtime canary succeeds. A NATGateway, route table, NSG, or firewall resource existing is not by itself apassing result.

BLOCKED_ON_REVIEW_DATA_GOVERNANCE

Use if the exact service-side processing, abuse-monitoring, storage/human-reviewsemantics, application logging, or private-output lifecycle cannot meet thefrozen governance rule honestly. Do not invent a zero-retention entitlement.

BLOCKED_ON_CLOUD_EXECUTION

Use if ACR remote build/test, private Container Apps/GPU execution,restart-safe receipt recovery, or exact commit/image/run binding cannot be madereliable without heavy local compute or a forbidden GitHub feature.

BLOCKED_ON_PRIVATE_SOURCE_ACCESS

Use if the exact authoritative retired-v1 source cannot be accessed read-onlyfrom the qualified private boundary. Never substitute an unverified localcopy.

BLOCKED_ON_INDEPENDENCE

Use if blind review, role separation, storage isolation, reviewer/arbiterseparation, or private audit cannot be established honestly.

BLOCKED_ON_SET_REPAIR

Use if the frozen rules cannot produce exactly 120 eligible, adjudicable,mandatory conformance cases with every quota filled and zero unresolved case.

BLOCKED_ON_SEALING

Use if the accepted private set exists but create-only sealing, remoteverification, listing witness, or one-time final contract creation fails.

BLOCKED_ON_PROTOCOL_INTEGRITY

Use for policy/protected-byte drift, private leakage, a runtime/assessor objectmismatch, irreconcilable ledger contradiction, an unresolved audit blocker, oranother scientific-integrity failure before formal evaluation starts.

Do not add a cost terminal state. Cost must be measured and reported, but costalone is not a reason to stop this authorized round.

2. Four non-negotiable operating rules

2.1 Azure executes; the laptop orchestrates

Permitted on the laptop:

edit public repository text, source, IaC, schemas, and prompts;

git fetch, git status, git log, targeted git diff, git add,git commit, and non-force git push;

lightweight tracked-file enumeration with git ls-files;

az control-plane submission and bounded status/result queries;

read closed, content-free receipts and short bounded content-free logs;

create one tracked-only git archive in a dedicated temporary directory ifthe remote ACR Git context is unavailable, upload it, and delete itimmediately after Azure accepts it.

Forbidden on the laptop:

any pytest invocation, including a focused single test;

compileall, lint, type checking, schema validation, generators, scanners,coverage, benchmarks, mutation tests, or repository-wide scans;

Docker build/run, package installation, compilation, model loading, modeldownload, inference, CUDA checks, or GPU work;

private Blob listing, hashing, streaming, decoding, migration, review,scoring, or result extraction;

local subagent/auditor workloads;

any sustained command expected to use more than two CPU cores or run longerthan two minutes.

All command-line compute runs through Azure Container Registry Tasks, AzureContainer Apps Jobs, or another explicitly qualified Azure in-VNet job definedand committed by this round. If a local command unexpectedly crosses theboundary, terminate it, record it, and move it to Azure.

2.2 Minimize local files safely

At the start, enumerate tracked files and inspect only the names/status ofuntracked and ignored files. Do not open ignored/private material.

After the final remote commit is verified:

retain only the repository, .git, tracked project files, required CLI/Gitconfiguration, and any pre-existing private/untracked item whose necessitycannot be disproved safely;

delete exact, verified, regenerable files created by this round, includingtracked-only upload archives, downloaded bounded logs after receipt commit,__pycache__, .pytest_cache, .mypy_cache, .ruff_cache, .coverage,htmlcov, build, and dist;

resolve each deletion target inside the repository or a round-specifictemporary directory, reject symlinks, produce a dry-run list, then deleteonly exact paths;

never run git clean -fd, git clean -fdx, wildcard recursive deletion,broad profile cleanup, or a command that could touch the workspace root oruser home;

preserve every pre-existing locked_inputs.jsonl, locked_labels.jsonl,curator file, .env, credential, Azure CLI profile, and unknown ignoredartifact unless the operator separately names an exact deletion target.

The final report must list removed paths and retained categories withoutprinting protected/private filenames beneath private prefixes.

2.3 Handoff-first repository discipline

Treat the task as a takeover by an agent with no hidden local context.

Use the full repository URL, explicit branch, starting SHA, Azuresubscription, resource group, region, and resource names in committedrunbooks.

Save this received prompt verbatim atdocs/prompts/phase1_2h_r2_to_1_2j_cloud_execution_prompt.md.

Commit every public program, IaC definition, task definition, schema,synthetic fixture, decision record, runbook, content-free receipt, audit,report, generator, and current-state artifact required to reproduce orunderstand the work.

A live Azure object must be reproducible from committed definitions. Shellhistory or an uncommitted portal change is not a deliverable.

Private prompts, responses, inputs, labels, case records, prediction streams,facts, contracts, manifests, listing witnesses, and case-bearing auditpackets remain in private Azure storage. Git contains only the registeredcontent-free hashes, counts, schema IDs, resource IDs, receipts, and aggregateresults. This is the necessary exception to “all experiment content is savedin GitHub.”

2.4 GitHub is only the Git repository

The only permitted GitHub functions are Git transport and reading therepository tree at an exact commit.

Do not use or invoke:

GitHub Actions, workflows, workflow_dispatch, checks, or status APIs;

pull requests, issues, discussions, projects, releases, Pages, Packages,GHCR, artifacts, or caches;

Codespaces, GitHub-hosted runners, or self-hosted GitHub runners;

GitHub Apps/API automation or gh operations for workflow, check, PR,issue, release, artifact, or API activity.

Do not add, edit, delete, enable, or trigger .github/workflows/*. Existingworkflow files are historical only. ACR may clone the repository by ordinaryGit transport at an exact SHA; that is allowed.

3. Starting state to verify

Fetch and verify without reset, force push, amend, or history rewrite:

repository: https://github.com/Alanjiao1988/J-space-observation.git;

branch: main;

expected remote SHA:a99d1e8341af85b8db32cb97a46ed5095c3f7978;

Round 5 terminal status: BLOCKED_ON_PRIVATE_REVIEW_BOUNDARY;

Round 5 result commit was pushed non-force and is the expected start;

final Round 5 Azure validation: ACR run cm1r;

final result: 2235 passed / 15 skipped / 2 failed;

parent same-image comparison: 2218 passed / 15 skipped / 2 failed;

the two failures are the disclosed sealed-input environmental failures intests/test_parser_v3_seal_job.py;

protected registry count is derived as 12, not hand-written;

current private-review assessment remains 0 PASS, 5 FAIL, 8 NOT_ASSESSABLE;

authoritative byte-only source receipt covers 12 objects / 396,613 bytes;

existing ledger records 14 byte-only integrity verifications, 12 data-planecontent reads, 0 data-plane writes, and 9 Azure resource changes for R1;

private semantic input reads, private semantic label reads, parser runs,predictions, formal evaluations, and v2 sets remain zero;

parser-v3-v1 is SEALED / UNSPENT / UNSCORABLE /
RETIRED_AS_INELIGIBLE, byte-unchanged at the starting state;

parser-v3-v2 does not exist;

Phase 1.2G policy is FINAL, while parser v3 remains unvalidated and formalevaluation ordinal remains 0.

Verify live Azure facts narrowly:

subscription name: MCAPS-Hybrid-REQ-125620-2025-alanjiao;

subscription ID: 943bacdf-8b6e-4e3a-8126-a149f623d32e;

resource group: rg-jspace-observation-sea;

region: southeastasia;

VNet: vnet-jspace-observation-sea;

existing ACA subnet: snet-aca-jspace-sea-v2 (10.80.4.0/23);

private-endpoint subnet: snet-pe-jspace-sea (10.80.2.0/27);

Blob private endpoint: pe-stjspacefiles-blob-sea, recorded IP10.80.2.4;

storage account: stjspacefiles0709085305, public network disabled;

authoritative source container: jspace-results;

authoritative source prefix:phase1-evaluator-validation/parser-v3-v1/20260725T160340Z;

Blob private DNS zone: privatelink.blob.core.windows.net;

existing ACA environment: cae-jspace-observation-sea-vnet2;

existing ACR: acrjspaceobssea0708231738.azurecr.io;

existing R1 read identity: id-jspace-p12h-r1-read-sea;

existing serverless GPU profile previously used by the project:gpu-t4 / Consumption-GPU-NC8as-T4;

unrelated account to leave untouched:aj-gpt56-25-943b-southeastasia in gpt56-sol-2025-rg.

If origin/main advanced, inspect and reconcile every intervening commit. Stoponly for an overlapping conflict that cannot be isolated safely. Preserve dirtyuser work.

Read all repository instructions and all current Phase 1.2D through Round 5protocols, reports, policies, ledgers, handoffs, run logs, decision logs,methods/limitations ledgers, generated current-state artifacts, protectedregistries, sealing tooling, and evaluation state-machine tooling beforeediting.

4. Resource and cost authorization

This prompt authorizes creation or modification of project-owned resources insubscription 943bacdf-8b6e-4e3a-8126-a149f623d32e, resource grouprg-jspace-observation-sea, primarily in southeastasia, where required bythe staged design.

Cost is no longer a blocking acceptance criterion. Do not select an inferiormodel, weaker network boundary, undersized runner, or fragmented proceduremerely to remain under the old USD 25/month or USD 10/run caps. Those caps arewithdrawn for this prompt.

Authorized when technically necessary and available:

a project-owned Azure OpenAI/Models-sold-by-Azure resource with public accessand local/key authentication disabled;

multiple pinned regional model deployments/capacity sufficient for reviewerA, reviewer B, arbiter, and private-set audit;

private endpoints, private DNS zones/links, and an ACR private endpoint ifrequired for a closed worker boundary;

a dedicated internal Container Apps workload-profiles environment andnon-overlapping delegated subnet;

Consumption CPU, Dedicated CPU, Flex, serverlessConsumption-GPU-NC8as-T4, and—only if model memory/runtime evidence requiresit and live quota exists—A100 workload profiles;

Azure Firewall plus route table/UDR and a dedicated firewall subnet whennecessary to prove destination-controlled egress;

NSGs, UAMIs, least-privilege custom roles/RBAC, private storage containers,manual jobs, ACR Tasks, and content-free monitoring;

enough parallel cloud capacity to run public validation and isolated privateroles without using the laptop.

T4 use is explicitly authorized. Reuse the existing T4-capable environment oradd the profile to the new qualifying environment only if live compatibility,quota, VNet, and isolation checks pass. T4 is not mandatory for CPU-boundparsing or deterministic tooling; choose GPU when it materially benefits modelreview, target inference, image qualification, or frozen evaluation execution.

Do not stop solely because Azure Firewall, stronger model capacity, T4, or A100costs more than the previous design. Still query current prices and reportestimated and actual incremental cost, token usage, GPU-seconds, build minutes,and retained monthly cost. Scale jobs to zero and stop executions when idle.Cost transparency remains required; cost minimization is subordinate toscientific correctness, privacy, reliability, and forward progress.

Do not create or modify unrelated resources. Tag every new resource with:

project=J-space-observation
phase=1.2H-R2-to-1.2J
owner=alanjiao
repo=Alanjiao1988/J-space-observation
lifecycle=retain-until-parser-v3-evaluation-closed

Add exact freeze/evaluation commit tags after those commits exist.

5. Scientific and privacy invariants

The following remain binding throughout the round:

Set admission and mandatory correct handling are the same predicate.

The FINAL Phase 1.2G policy semantics, ontology, quotas, gates, status logic,comparator role, and zero-tolerance limits do not change.

Parser v3, parser v2, and the legacy parser may not run on retired-v1 orcandidate-v2 material during construction, review, arbitration,replacement, audit, or sealing.

Candidate selection may never use parser behavior, performance, predicteddifficulty, or a hidden parser proxy.

Every final case is adjudicable, mandatory, independently reviewed, andaccepted with zero unresolved semantic uncertainty.

Private source, labels, cases, reviews, predictions, facts, contracts, andmanifests never enter Git, Copilot, ChatGPT, GitHub services, public logs,public telemetry, or local storage.

No scoring label may be accessible to Stage P or any prediction-producingidentity, image, process, or storage lane.

Stage E cannot begin until a complete prediction stream is sealedcreate-only and its authenticated listing/receipt is frozen.

Formal evaluation happens at most once. Failure is final for parser v3 onthis suite.

No post-seal semantic repair and no post-result parser/set/policy tuning ispermitted in this round.

Protected files and registered private source bytes remain unchanged.

A passing policy/set/tooling audit is not a parser validation result.

The round must not:

alter parser/scorer behavior, ontology, normalization licenses, gatesemantics, thresholds, accepted coverage, comparator policy, orPASS/FAIL/INVALID logic;

coerce an old label, preserve a case to hit an expected reuse count, orrelax a rule to fill a quota;

use the unrelated Azure OpenAI account/resource group;

use storage keys, Azure OpenAI keys, SAS, connection strings, client secrets,ambient CLI credentials, or DefaultAzureCredential fallback in privatejobs;

enable public network access on private Blob, reviewer backend, workerenvironment, private ACR path, or evaluation lane;

emit private fields through stdout, stderr, exceptions, metrics, tracing,monitoring, environment dumps, receipts, or model logs;

use stateful Responses/Assistants/Threads, Files API, vector stores, storedcompletions, or batch APIs for private review unless a separately frozengovernance record proves the feature and storage lifecycle; prefer statelesssynchronous inference;

claim zero service-side retention/human access unless actual entitlementevidence proves modified abuse monitoring;

treat LLM reviewer agreement as human ground truth or statisticalindependence;

draw a J-space or hidden-reasoning conclusion from parser validation.

6. Transactional phase plan

The following phases are sequential. Never skip a gate. Each phase writes anappend-only content-free checkpoint, commits all public work, validates inAzure, and binds the exact input commit/tree/image/resource state.

Phase A — public protocol and tooling freeze

Complete before any new private semantic read.

Run the exact-starting-commit baseline entirely in ACR Tasks.

Record protected raw-byte hashes and policy full-file/semantic hashes.

Implement/finalize all boundary, review, migration, construction,replacement, sealing, preregistration, Stage P, prediction-seal, Stage E,result, ledger, and cleanup tooling using public synthetic fixtures only.

Freeze the complete role/access matrix, admission rule, reviewer schemas,replacement-batch limit, deterministic selection/tie-break, overlap rules,seal layout, retry/abandonment rules, preregistration schema, and evaluationstate machine.

Audit the real executable entrypoints and call graph. Do not repeat Round5's defect by validating a named definition, binding, or schema that thelive job does not actually execute.

Run public synthetic end-to-end rehearsal through construction, fake seal,preregistration, Stage P, prediction sealing, and Stage E using syntheticpublic cases and a synthetic parser only. Real parser v3 must remain isolatedfrom any private/candidate data.

Obtain the required pre-private public audits and final-state re-reviews.

Commit and push the exact public freeze before private semantics are read.

Suggested commit:

Freeze cloud-native parser-v3-v2 construction and one-shot evaluation protocol

Record this exact SHA as prospective_protocol_freeze_commit.

After the first private semantic read, any change to policy semantics,admission, normalization, reviewer packet/schema, replacement rule, overlaprule, set quotas, selection/tie-break, seal layout, parser/scorer bytes,preregistration logic, Stage P/E entrypoints, status machine, or retry rulesinvalidates continuation. Stop blocked; do not patch and continue under thesame prospective freeze.

Phase B — deploy and qualify the real private boundary

Use only public synthetic fixtures in Phase B.

Build a dedicated internal workload-profiles Container Apps environment ina mechanically selected non-overlapping delegated subnet. Do not attachrestrictive rules to the existing shared subnet.

Prefer a defense-in-depth boundary:

private endpoints for Blob, reviewer backend, and ACR where supported;

NSG rules that prevent direct Internet bypass;

UDR routing remaining external egress through Azure Firewall or anequivalently explicit destination-filtering control;

firewall application/network rules limited to documented Container Appsplatform dependencies and approved Azure endpoints;

explicit deny for all other destinations.

Do not treat NAT Gateway, a route table, or an NSG name as proof of filteredegress. NAT supplies source translation; UDR selects a next hop. Qualificationrequires the actual filtering policy plus runtime positive and negativeprobes.

Deploy one project-owned, private, keyless Azure model backend. Query liveregional model inventory/quota and select the strongest eligible regionaldeployment(s), not the cheapest. Do not use Global Standard, Data Zone,cross-geography, marketplace public endpoints, or the unrelated account.

Where live inventory permits, use distinct pinned deployments/model familiesfor reviewer A and reviewer B and a separately initialized arbiter. If onlyone qualifying model family exists, use isolated jobs/sessions/identities,record the limitation, and do not call the judgments statisticallyindependent.

If regional managed-model governance or quota cannot qualify, thepre-private protocol may select a self-hosted open-weight reviewer backend onprivate serverless T4/A100. Model weights and licenses must be verified,content-addressed, scanned, and staged before any private content read; theruntime must have no Internet egress. Backend selection must be frozen once,before private access, and cannot be switched after observing cases.

Use distinct UAMIs, containers, and minimum RBAC for reviewer A, reviewer B,broker, arbiter, private-set auditor, source custodian, seal custodian,Stage P, prediction sealer, Stage E, receipt exporter, and any ephemeralsynthetic seeder.

Run the production entrypoints end to end on committed public syntheticinputs, including a deterministic disagreement that forces arbitration.

Bind actual resource IDs, private IPs, DNS answers, firewall/NSG/UDRconfiguration hashes, role assignments, job templates, image digests,executions, model/deployment/API versions, and closed receipts.

Required positive runtime probes:

Azure DNS and private DNS resolution;

Blob, ACR, and reviewer hostnames resolve to registered private endpointswhere designed;

exact UAMI token acquisition;

exact permitted Blob lane operations;

one bounded synthetic reviewer call through the private backend;

synthetic A/B/broker/arbiter/auditor workflow;

content-free receipt export.

Required negative runtime probes must make actual short-timeout connection orrequest attempts and must fail:

example.com:443, github.com:443, api.openai.com:443, andchatgpt.com:443;

a fixed public test IP on 443;

the unrelated Azure model account;

an unapproved storage endpoint;

an unapproved VNet IP;

literal-IP, alternate-DNS, host-header, endpoint-override, and direct-publicendpoint bypasses;

cross-lane reads/writes forbidden by the identity matrix.

The public receipt contains only closed test IDs, PASS/FAIL, execution IDs,durations, resource/digest bindings, and aggregate token/cost data. No raw URL,exception, prompt, response, case, label, span, or private identifier may beemitted.

Only after all boundary conditions pass may Phase C begin.

Phase C — authoritative private import, repair, review, and construction

Before the first semantic read:

Re-authenticate the exact 12-object retired-v1 source and byte receipt.

Verify read-only source-custodian scope on the exact authoritative prefix.

Create the append-only access event and update the v1 current-state machine.

Prove no parser/prediction identity or code exists in the source-custodian,reviewer, arbiter, selector, or audit images.

Freeze every private job image by digest and every job template/config hash.

The first authorized semantic read changes current v1 state from the historicalunqualified UNSPENT. Preserve the historical Phase 1.2G/R1 record, but use acurrent state equivalent to:

SEALED / REPAIR_ACCESSED / NEVER_FORMALLY_SCORED / UNSCORABLE / RETIRED_AS_INELIGIBLE

The v1 bytes and namespace remain unchanged. Formal evaluations and predictionsagainst v1 remain zero.

Run the frozen one-way v1-to-v2 import and N1–N6 normalization pipeline.The historical 105 repairable / 15 residual split is an expectation, not atarget. Quarantine every case that fails deterministic validation, has semanticuncertainty, conflicts with the v2 ontology, or cannot satisfy mandatorycorrect handling. Never modify rules to reproduce 105/15.

Build the proposed 120-case set using these rules:

reviewer A and reviewer B independently review every proposed final case,not only migrated disagreements;

reviewers receive case content plus the public ontology/slot packet, but notold/migrated labels, parser code/results, reuse status, or one another'sdecisions;

agreement covers every scoring-relevant field, including typed decision,canonical value/candidates, literal spans and roles, ambiguity/equivalence,extraction strategy, output quality, warnings/failure reasons, stratum, andsubtype;

only disagreements reach the isolated arbiter;

the arbiter sees the case, public packet, and two decisions, but not the oldlabel until adjudication is permanently recorded;

unresolved or compromised cases are ineligible and replaced;

replacement curators receive only a public missing-slot packet and never seethe replaced content/old label or parser behavior;

a separate selector applies the frozen eligibility rule and deterministiccontent-hash tie-break;

generation stops at the preregistered per-slot batch limit; failure to fill aslot yields BLOCKED_ON_SET_REPAIR, not an unbounded retry;

every replacement enters the same A/B/arbiter/private-audit path;

private review outputs remain in isolated private containers; Git receivesonly aggregate counts and hashes.

Final set invariants are exact:

120 unique cases;

strata S01–S12, exactly 10 cases each;

exact subtype-slot completion under the FINAL stratum policy;

80 present, 30 no_answer, 10 ambiguous;

no fourth/research-only class;

zero unresolved decisions;

literal-only admissible spans and internally consistent candidates;

all S06 rightmost-distractor and S11 ambiguity registrations valid;

every case marked eligible, adjudicable, and mandatory;

80 gate-pinned cases in S01/S02/S03/S07/S08/S10/S11/S12;

40 residual exact-conformance cases in S04/S05/S06/S09;

no parser/prediction/performance field in construction artifacts;

zero prohibited within-set/cross-set collision under every registered exact,normalized, numeric-normalized, and template-family rule.

Complete registered collision checks against all public parser-developmentcorpora and the retired parser-v2 holdout through an isolated custodian orauthenticated non-content fingerprints. If the historically unperformedparser-v2 holdout comparison cannot be established without leaking content,block sealing. Authorized reuse from never-evaluated retired v1 is recorded andis not itself a prohibited collision.

Phase D — private audits, create-only seal, and final contract

Derive set facts from actual bytes and require byte-deterministic regeneration.Compile a temporary private contract candidate that binds policy, stratumpolicy, set bytes, facts, planned members, hashes, frozen instrument, parser-v3source hash, and prospective protocol commit. It is not the final contract.

Run two separate pre-seal audits:

Private Audit A reviews all 120 cases and attempts to falsify admission,adjudicability, blind review, arbitration, normalization, replacement,quotas, collision freedom, and aggregate provenance.

Audit B reviews policy immutability, ledger monotonicity, facts derivation,contract determinism, parser isolation, private-output containment,create-only seal semantics, member layout, and listing-witness lifecycle.

Any case rejected under an already frozen rule is quarantined and replaced viathe existing frozen path, followed by complete affected review/facts/auditregeneration. Any finding requiring a frozen executable/rule change blocks theround. Any unresolved BLOCKER or MAJOR blocks sealing.

Use this acyclic lifecycle:

set bytes + facts + planned members
    -> terminal sealed-set manifest
    -> authenticated actual listing witness
    -> one-time final compiled contract
    -> public content-free seal receipt

Sealing requirements:

allocate one never-before-used parser-v3-v2 identity and timestampedprefix;

prove the target prefix and immutable witness/contract paths do not exist;

upload planned members create-only, terminal manifest last;

never overwrite, rename, patch, delete, or resume a terminal identity;

follow only the prospectively frozen infrastructure retry/abandon rule;

list the prefix with an authenticated seal-custodian identity;

re-download and verify every object by size, SHA-256, ETag, and therepository's stronger registered checks;

create the content-free seal-time listing witness from the real authenticatedlisting;

persist the witness create-only outside sealed-set membership;

compile the final contract exactly once outside sealed-set membership,binding the manifest and listing-witness hashes;

regenerate to a temporary path and require byte equality withoutoverwriting the final contract;

persist and verify the final contract create-only;

publish only its hash and closed status receipt.

The expected sealed member count must be derived from the frozen release layoutrather than copied from historical prose. L-32 is satisfied only by the realseal-time listing witness. No semantic review occurs after sealing.

Phase E — post-seal preregistration and evaluation readiness

Begin only from a completely verified sealed v2 set and immutable finalcontract.

Freeze and bind:

sealed set identity, manifest, listing witness, set facts, and final contract;

FINAL policy full-file and semantic hashes;

exact parser-v3 and scorer raw bytes/hashes;

exact Python/dependency lock, OS/base image, image payload manifest, CUDAruntime when used, and immutable evaluation image digest;

exact Stage P and Stage E entrypoints and commands;

Stage P input-only identity/storage matrix;

Stage E labels-plus-sealed-predictions identity/storage matrix;

prediction member layout, completeness rule, create-only prediction seal,authenticated listing, and receipt schemas;

state-machine transitions and evaluation ordinal;

infrastructure-only retry/abandon rules;

binding acceptance criteria and nonbinding diagnostics;

formal result schema and public redaction projection.

Build the final image remotely with ACR Tasks. Use CPU if the frozen parser isCPU-bound; use the qualified T4 profile if the actual frozen execution or modelpath benefits from CUDA. Do not avoid T4 for cost. Pin the final image by digestand scan its payload for unexpected parser variants, alternate scorers,credentials, private files, dynamic imports, alternate entrypoints, and networkclients.

Run full public synthetic rehearsal with the exact production Stage P/E paths,identities mirrored on synthetic containers, image digest, commands, schemas,and state transitions. Mutation tests must prove:

Stage P cannot read labels;

Stage E cannot start before prediction-seal completion;

a partial/duplicate prediction stream cannot seal;

prediction sealing is create-only;

Stage E cannot invoke/import parser code;

Stage P cannot invoke/import scoring-label code or comparator logic;

ordinal cannot reset or increment twice;

a result cannot exist without the unique prediction seal and exact contract;

report-only macro-F1/comparator metrics cannot influence status;

PASS, FAIL, and INVALID are mutually exclusive.

Obtain two independent readiness audits and bounded final-state re-reviews.Then create the immutable preregistration/authorization lock. Once created, nobound byte/config may change.

Phase F — formal single-launch Stage P and Stage E

Only after every readiness condition is PASS:

Stage P — prediction

Atomically transition ordinal 0 from PREREGISTERED toPREDICTION_RUNNING.

Start exactly one private job from the frozen image digest, command,Stage-P UAMI, input-only scope, and exact sealed-set contract.

Run parser v3 on all 120 locked inputs exactly once.

Write the registered prediction members create-only to a new predictionnamespace; emit no predictions publicly.

Verify exact population, uniqueness, schema, ordering, source binding, andmember hashes without labels.

Write the terminal prediction manifest last, list authentically, verify allremote bytes, and seal the prediction stream create-only.

Persist a content-free prediction listing witness and receipt.

Transition to PREDICTION_SEALED.

If an infrastructure failure occurs, follow only the frozen retry rule. If anyprediction-bearing terminal object, completeness evidence, or ambiguous partialstate makes a retry non-permitted, terminate FORMAL_EVALUATION_INVALID.

Stage E — scoring

Prove the sealed prediction stream and exact Stage-P receipt.

Atomically transition from PREDICTION_SEALED to LABELS_OPENED and setformal evaluation ordinal to 1.

Start exactly one Stage-E job using the frozen scorer and Stage-E identity.

Read only the exact labels, sealed predictions, policy, facts, and contractrequired for scoring. Do not run or import parser code.

Compute every binding gate exactly as registered.

Apply the zero-error residual rule to S04/S05/S06/S09 with pooled maximum 0and per-stratum maximum 0.

Require all pinned zero-error gates for the other 80 cases. Together theseimply 120/120 finite-suite conformance; do not add a duplicate percentagethreshold.

Produce PASS only when every binding criterion passes. One eligible-casemismatch produces FAIL, not a discretionary review.

Produce the full three-class answer-presence confusion matrix and per-classprecision/recall/F1 only as nonbinding diagnostics. State that class metricscan hide wrong canonical present values.

Keep parser-v2 comparison FINAL / NOT_RUN / REPORT_ONLY; no comparatorcall, margin, or status reachability is authorized here.

Persist the private complete result create-only and publish only theapproved content-free result projection.

Transition once to terminal EVALUATED_ACCEPTED,EVALUATED_NOT_ACCEPTED, or EVALUATION_INVALID.

No second formal evaluation is permitted. Do not inspect failure cases in thisround beyond the pre-frozen public aggregate/redacted projection. Never exposecase-level errors to a role that can modify parser v3.

Phase G — closure

Bind final state to exact Git commits, Azure resources, job executions,images, set/prediction witnesses, contracts, and result hashes.

Run final public consistency/document/protected checks entirely in Azure.

Obtain final repository/instrument and execution-state audits.

Commit and push the content-free terminal report and generated currentstate.

Verify origin/main equals the reported result SHA.

Stop all jobs, scale to zero, remove only ephemeral seeder/build roles andverified orphan resources, retain immutable evidence and resources neededfor the next scientific gate.

Perform exact safe local cleanup only after remote verification.

7. Canonical cloud architecture

7.1 Private model backend

Create or reuse only a project-owned qualifying model resource inrg-jspace-observation-sea.

Required live properties:

public network disabled and default network action deny;

local/key authentication disabled;

TLS 1.2 or stronger;

private endpoint in the project private-endpoint subnet;

correct private DNS zone/link to vnet-jspace-observation-sea;

managed-identity inference with the minimum inference role;

exact regional model/version/deployment type/capacity/API version pinned;

no raw prompt/completion diagnostics;

stateless inference path for private reviewer calls.

Record current Microsoft service-side processing, abuse monitoring, possibleflagged-content storage/human review, and any actual modified-abuse-monitoringentitlement. Models sold by Azure do not send prompts to OpenAI, but Microsoftservice processing and abuse-monitoring semantics must still be disclosed.

7.2 Egress-controlled worker and evaluation boundary

Use a dedicated non-overlapping subnet and internal workload-profilesenvironment. Prefer private endpoints plus an Azure Firewall/UDR and NSGdefense-in-depth design now that cost is not a blocker.

The runtime boundary must permit only:

exact project private endpoints;

managed identity/Azure control dependencies required by the platform;

exact ACR/image delivery path;

content-free monitoring if indispensable;

no unrestricted Internet destination.

Every platform service tag/FQDN range left open is a disclosed residual egresssurface. Runtime canaries, not design prose, decide qualification. Theenvironment, routes, firewall policy, NSG, DNS, endpoint IPs, and job revisionmust be read back from Azure and hashed into the evidence record.

7.3 CPU/GPU execution placement

ACR Tasks: all public builds, tests, scans, deterministic generation, IaCvalidation, and source/image manifest checks.

CPU Container Apps Jobs: deterministic broker, selectors, migration,validators, facts/compiler, seal/listing/receipt tooling, Stage P/E ifmeasured as CPU-bound.

T4 serverless jobs: self-hosted reviewer inference, CUDA/imagequalification, target/model inference, or Stage P only where the frozenexecutable benefits from GPU.

A100: authorized only when a frozen model cannot fit or execute reliably onT4 and live evidence shows A100 is necessary; document the reason.

No job remains always-on. Scale-to-zero does not relax image, identity,network, or receipt binding.

7.4 Storage lanes and identities

Use distinct private containers and UAMIs for every role. At minimum:

Role

May read

May write

Must not access

source custodian

exact retired-v1 source prefix

v2 private staging only

parser, predictions, unrelated source

reviewer A

assigned blinded case packets

A decisions

old labels, B output, parser

reviewer B

assigned blinded case packets

B decisions

old labels, A output, parser

broker

A/B structured outputs

disagreements

model endpoint, source labels

arbiter

disagreement packets

arbitration

old label before freeze, full unrelated cases, parser

private-set auditor

final candidate + provenance

audit findings

parser results

seal custodian

finalized immutable bytes + seal plan

new create-only v2 namespace/witness

editing cases

Stage P

sealed v2 inputs + frozen parser assets

new prediction namespace

every scoring label, Stage E result

prediction sealer

prediction members

terminal prediction manifest/witness

labels, parser execution

Stage E

sealed predictions + exact labels/policy/contract

one formal result

parser invocation, construction paths

receipt exporter

content-free receipt objects

public projection only

semantic payloads

Use explicit UAMI client IDs. Forbid ambient credential chains. Mechanicallyinspect effective inherited and direct roles, not only intended assignments.Use narrowly scoped custom roles where built-ins grant delete, ownership,delegation, unrelated container access, or broader data access than required.

8. Admission, review, and replacement details

For each case c, admission requires all of:

exactly one registered stratum and required subtype slot;

membership in the frozen three-class ontology and literal-span convention;

one unambiguous mandatory typed decision for a correct instrument;

adjudicable reference decision, canonical value/candidates, selected orequivalent/ambiguous spans, output quality, and every scorer-consumed field;

independent review and arbitration closure with no uncertainty;

ontology validator and frozen scorer/instrument binding acceptance;

admission independent of any parser behavior.

Reject optional stress cases, aspirational cases, disputed semantics, coercedlegacy labels, and parser-selected cases.

The four historical present_unextractable records may never be mechanicallymapped into a live v2 class. They require fresh blind adjudication from contentor replacement. Any nominally repairable case may still be quarantined.

Every admitted case has exactly one private provenance category:

MIGRATED_AND_BLIND_REVIEW_CONFIRMED;

FRESHLY_READJUDICATED_UNDER_V2;

INDEPENDENT_REPLACEMENT.

Only aggregate category/stratum counts may be public.

9. Protected artifacts and immutable semantics

Before editing and after every freeze/result commit:

load PROTECTED_DIGESTS programmatically;

derive its actual membership/count;

hash every protected artifact as raw bytes;

compute policy full-file and semantic hashes;

record the Git tree;

verify the frozen parser/scorer/instrument bytes.

No byte may change in the registered parsers, scorer/instrument, retired-v1contract/manifests/provenance/ancestry, historical gate contracts, or anycurrent protected registry entry. Do not weaken/remove a digest or manufacturea new namespace to disguise drift.

10. Required public artifacts in the repository

Add/update as appropriate:

this exact prompt under docs/prompts/;

a combined staged protocol plus separate machine-readable phase records forboundary, construction/sealing, preregistration, and evaluation;

closed schemas for deployment evidence, runtime canaries, reviewer output,disagreement/arbitration, admission/quarantine, set facts projection, sealreceipt, listing-witness digest receipt, preregistration, prediction receipt,formal result, and terminal assessment;

deterministic generators and --check modes;

Bicep/ARM definitions under infra/azure/ for every live resource;

noninteractive deploy/build/run/readback/cleanup scripts;

ACR Tasks definitions for baseline/focused/full/static/security checks;

CPU and CUDA/T4 images as required, pinned by digest;

production reviewer A/B, broker, arbiter, auditor, migration, selection,validation, sealing, Stage P, prediction sealing, Stage E, and receipt-exportentrypoints;

public synthetic positive, negative, disagreement, failure, partial-upload,partial-prediction, and state-machine fixtures;

canonical access/event ledger and status derivation;

boundary, private-set, seal, readiness, evaluation, and audit reports;

content-free Azure build/job/resource/cost receipts;

current status, thread handoff, run/decision logs, Azure runbook,methods/limitations ledgers, artifact index, and generated current-stateblocks.

Do not duplicate live facts across hand-maintained prose. Canonicalmachine-readable records own facts; prose references them or embedsbyte-reproducible generated blocks.

11. Azure-only validation

11.1 Baseline

Run baseline from exact starting commita99d1e8341af85b8db32cb97a46ed5095c3f7978 in ACR Tasks before editing.

The task must use an exact Git checkout with real .git/.gitignore metadataor a pre-existing committed source manifest explicitly designed for thatabsence. Do not repeat Round 5's four false regressions caused by az acr buildstripping Git metadata, and do not weaken tests to accommodate a damaged buildcontext.

Record source commit/tree, task/run ID, builder/image digest, exact commands,counts, duration, and exit codes. The two known sealed-input environmentalfailures remain explicit baseline failures unless the cloud task is given theregistered private synthetic fixture path; do not convert them into skips.

11.2 Candidate/final checks

Run at minimum, entirely in Azure:

all focused Phase 1.2E/1.2F/1.2G/1.2H/R1/new suites;

full repository suite;

all new boundary, construction, seal, preregistration, Stage P/E, state, andmutation suites;

Python compilation and established lint/static/type checks;

JSON/schema/Bicep/ARM validation and Azure what-if;

deterministic generation --check;

current-state, ledger, policy, contract, and status consistency;

protected and frozen raw-byte checks;

policy semantic-hash verification;

real entrypoint/call-graph binding and parser-isolation checks;

source-tree/image payload manifests;

credential/secret/private-content/private-path leakage scans;

stdout/stderr/diagnostic record leakage scans;

identity/RBAC/inheritance and storage-lane matrices;

deployed-versus-IaC/network/model/job drift checks;

positive/negative egress canaries;

facts/contract/manifest/listing/prediction/result regeneration checks;

git diff --check, changed-file allowlist, large-file check, and completefinal diff review.

Do not skip, xfail, relax, remove, or rename away an existing test. Discloseevery substantive failure, cause, fix, and retry—not only the final green run.The ordinary public unit suite may invoke parser code on committed publicsynthetic fixtures; report this separately.

11.3 Local archive fallback

If ACR cannot clone the exact Git SHA, create only a tracked git archive in around-specific temporary directory. Prove every archive member is tracked andno ignored/private path is present; record tree and archive SHA-256; upload toACR; delete immediately after acceptance. Never upload the working directory,home, .git, Azure profile, logs, caches, or private files.

12. Regression and mutation coverage

Include a valid positive control for every rejection. At minimum reject:

public model/storage/worker access or key/local auth enabled;

wrong account/region/deployment/model/API/private endpoint/DNS;

Global/Data Zone/public endpoint fallback;

wrong job/image/entrypoint/UAMI or ambient credential fallback;

broad/inherited role or cross-lane access;

NAT/UDR/NSG presence substituted for actual egress filtering;

firewall/NSG shadowing or any unexpected negative-canary success;

private content in log, exception, receipt, metric, trace, diagnostic, Git,image, or local artifact;

a checker examining a definition/binding different from the executedhandler, including rebinding and alternate-call-site mutations;

old-label leakage, coercion, self-review, reviewer cross-talk, or unresolvedarbitration;

admission without mandatory adjudicability;

replacement selection influenced by parser behavior;

unbounded replacement retries or post-observation rule changes;

missing/duplicate slot, quota/support mismatch, invalid S06/S11 semantics,or prohibited collision;

policy/protected/parser/scorer drift;

a self-consistent but unauthenticated listing witness;

overwrite, manifest-first, partial/resumed seal, circular provenance, ormismatched contract;

Stage P label permission, Stage E parser permission, pre-seal parser run, orpre-prediction-seal scoring;

partial/duplicate prediction seal, missing execution binding, ordinal reset,second Stage P/E launch, or status reachability from report-only metrics;

READY/PASS with any unknown, FAIL, NOT_ASSESSABLE, unresolved audit finding,or mismatched deployed object.

Use mutation-style negative fixtures for call-site replacement, identitysubstitution, path aliases, symlinks, case folding, rule priority, alternateDNS/endpoint, partial writes, ledger succession, and nested status logic.

13. Independent audits

Auditors operate remotely. Public auditors may inspect only committed publiccode, synthetic fixtures, public resource settings, and content-free receipts.Private auditors run inside the qualified boundary and never return casecontent through Copilot/chat/logs.

Required audits:

Audit A — public methodology and protocol: admission, normalization,review independence, replacement, sealing lifecycle, preregistration,one-shot semantics, and finite-suite claims.

Audit B — Azure security/runtime binding: endpoints, keyless identity,firewall/NSG/UDR, runtime canaries, storage lanes, logs, model governance,and actual deployed-object binding.

Audit C — private set: all 120 cases, provenance, quotas, ontology,adjudicability, collision freedom, and unresolved count.

Audit D — repository/instrument/evaluation readiness: protected bytes,semantic hashes, facts/contract, state machine, image/command/identity,Stage P/E isolation, and public redaction.

Audit E — post-execution closure: unique prediction seal/result,acceptance derivation, counters, ordinal, immutable terminal state, andconsistency of all content-free reports.

Record finding ID, severity, primary evidence, reproduction, disposition, fix,residual limitation, and final-state re-verification. Require one boundedfinal-state re-review after remediation from each applicable auditor. Anyunresolved BLOCKER or MAJOR prevents progression/READY. Do not run an unboundedaudit regress; disclose the stopping boundary and whether the last remediationwas re-reviewed. Self-authored tests are not independent validation.

14. Ledgers and counters

Preserve all historical events exactly and append new events. Never decrement,reset, reclassify, or erase an event to satisfy a rule. Separate control-plane,byte-only, semantic, construction, prediction, scoring, and evaluation events.

At final output report actual counts for:

Azure control-plane reads and resource creates/changes/deletes;

role definitions/assignments created/removed;

ACR builds/tests and Container Apps/GPU executions;

model calls, input/output tokens, GPU-seconds, build minutes, and cost;

positive/negative network probes;

Blob reads/writes by synthetic/private lane;

retired-v1 semantic input and label reads for repair;

private curator/reviewer/arbiter/auditor reads;

v2 cases constructed/reviewed/replaced;

sets, facts, contracts, manifests, listing witnesses, and seals;

Stage P input reads, parser invocations, predictions, prediction members,prediction seals, Stage E label reads, and scoring executions;

comparator predictions and runs;

formal evaluation ordinal and terminal transition.

Before Stage P, required formal counters remain:

labels opened for scoring = 0
parser invocations on private/locked data = 0
candidate predictions = 0
comparator predictions = 0
formal evaluations = 0
formal evaluation ordinal = 0

After a valid completed evaluation:

Stage P launches = 1
complete parser-v3 prediction streams = 1
prediction seals = 1
Stage E launches = 1
formal evaluations = 1
formal evaluation ordinal = 1
comparator runs = 0

Do not call a repair semantic read “scoring.” Do not call byte-only verificationsemantic reading. Do not call a failed/partial formal launch NOT_RUN.

15. Commit and push sequence

Use honest non-force commits on main only while it remains the established,undiverged workflow. Suggested checkpoints:

Freeze cloud-native parser-v3-v2 construction and one-shot evaluation protocol

Qualify private review and evaluation boundary

Construct and seal independently reviewed parser-v3-v2 set

Preregister parser-v3 single-launch evaluation

Record formal parser-v3 evaluation result

The first commit must be pushed before the first private semantic read. Thepreregistration commit must be pushed before Stage P. The formal result commitmay contain only approved content-free result/current-state material about thealready frozen execution.

Before each push, fetch and verify no divergence. Never force-push, amend apublished freeze, reset, rewrite history, discard remote/user work, or use a PRas a substitute. Verify origin/main equals the reported final SHA.

16. Cleanup and retained state

After completion:

stop all active executions and set minimum replicas to zero;

revoke/remove the ephemeral synthetic seeder and temporary build roles;

delete only exact failed/orphan jobs, temporary assignments, mutable tags,and non-evidence resources proven unnecessary;

retain the private endpoints/DNS, egress boundary, immutable images, sealedv2 set, prediction seal, witnesses, contracts, result, required identities,and evidence needed for reproducibility and the next experiment;

never delete or alter retired v1, sealed v2, sealed predictions, or theunique formal result;

report retained monthly cost and the later cleanup trigger.

Then perform the safe local cleanup in §2.2 and report exact removed paths.

17. Final response format

Return, without private case content:

terminal status;

starting SHA/tree and verified starting facts;

protocol freeze, boundary, seal, preregistration, and result commit SHAs;

final branch and verified remote SHA;

confirmation that GitHub was used only as a Git repository;

complete local-versus-Azure execution ledger and local cleanup;

all ACR/job/GPU run IDs, exact source/image digests, commands, and results;

created/changed/deleted/retained Azure resource IDs and tags;

model backend, deployment/model/version/type/capacity/API/quota/governance;

CPU/T4/A100 placement, GPU-seconds, token counts, build minutes, estimatedand actual cost;

private endpoint/DNS/firewall/UDR/NSG evidence and residual egress surface;

positive and negative runtime canary results;

UAMI/RBAC/storage-lane matrix and denied-access results;

policy full-file/semantic hashes before/after;

protected registry-derived count and every before/after raw hash;

retired-v1 authentication, state transition, semantic access counts, andunchanged-byte proof;

expected versus actual migration/quarantine outcomes;

aggregate admitted provenance counts and replacement batches/results;

A/B agreement, arbitration, unresolved, and private-audit counts;

all final 120-case support/stratum/subtype/ontology/coverage invariants;

aggregate overlap/collision results;

set-facts, manifest, listing-witness, final-contract hashes and deterministicverification;

seal identity/prefix, expected/observed member counts, remote byte verdict;

preregistration/authorization-lock schema, hash, state, and readiness gates;

evaluation image/parser/scorer/command/identity hashes and bindings;

Stage P execution, prediction count, seal/listing witness/hash, and labelisolation proof;

Stage E execution, ordinal, binding gate results, and terminal acceptance;

nonbinding confusion-matrix/per-class diagnostics, clearly isolated fromstatus;

parser-v2 comparator state FINAL / NOT_RUN / REPORT_ONLY;

exact baseline/focused/full/static/security results and every substantivefailure/retry;

every audit finding, fix, residual limitation, and re-verification;

complete access/scientific counters before and after Stage P/E;

whether public unit tests invoked parser code on public synthetic fixtures;

final state of parser-v3-v1, parser-v3-v2, prediction stream, and parser v3;

exact limitations and next gate.

If PARSER_V3_VALIDATED_READY_FOR_JSPACE_EXPERIMENT, the exact next gate is:

A separately frozen execution of the repository’s already designed J-space observation experiment on Azure GPU resources, using parser v3 only within the scope justified by the 120-case finite conformance result. Parser validation alone is not a J-space result.

If PARSER_V3_EVALUATED_NOT_ACCEPTED, the exact next gate is a new parserdevelopment lineage that never trains on or inspects case-level v2 evaluationfailures, followed by a separately constructed independent future set. Do notbegin it in this round.

For any earlier blocked/invalid status, state the smallest scientifically validcorrective gate and do not silently continue past the blocker.

18. Official Microsoft references

At execution time, re-check current English official documentation and recordretrieval date/version. The following were current at prompt revision on2026-08-01:

Container Apps networking:https://learn.microsoft.com/en-us/azure/container-apps/networking

Container Apps firewall/NSG integration:https://learn.microsoft.com/en-us/azure/container-apps/firewall-integration

Container Apps UDR control:https://learn.microsoft.com/en-us/azure/container-apps/user-defined-routes

Workload profiles and serverless GPU types:https://learn.microsoft.com/en-us/azure/container-apps/workload-profiles-overview

Workload-profile CLI, including Consumption-GPU-NC8as-T4:https://learn.microsoft.com/en-us/azure/container-apps/workload-profiles-manage-cli

T4/A100 comparison:https://learn.microsoft.com/en-us/azure/container-apps/gpu-types

Managed identities in Container Apps:https://learn.microsoft.com/en-us/azure/container-apps/managed-identity

Azure OpenAI private networking:https://learn.microsoft.com/en-us/azure/foundry-classic/openai/how-to/network

Models sold by Azure data privacy and abuse monitoring:https://learn.microsoft.com/en-us/azure/foundry/responsible-ai/openai/data-privacy

Private Endpoint DNS guidance:https://learn.microsoft.com/en-us/azure/private-link/private-endpoint-dns

Documentation establishes supported mechanisms. Live Azure inventory,read-back, immutable deployment evidence, and runtime positive/negative probesestablish this project's actual state.